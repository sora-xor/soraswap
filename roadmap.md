# SoraSwap Roadmap

Last updated: 2026-06-28

## Current status

The trader roadmap slice is implemented locally across `soraswap` plus `../iroha`. Local acceptance is green for compile/lint, Kotodama tests, simulations, trader UI, contract-console UI/integration, isolated localnet deploy/smoke, and the direct operator `dev-smoke` / `deploy-local` / `smoke-local` flow.
The local release hygiene gate also scans generated deployment/telemetry JSON for raw local path diagnostics, requires retained evidence to stay stable under the shared sensitive-data/runtime-path redactor, keeps public/local deploy and smoke path labels basename/repo-relative, and validates `.gitignore` behavior for generated evidence, `.gitkeep` placeholders, ignored public signer configs, and visible `*.toml.example` templates.

Shipped:
- Canonical trader-event metadata is attached in `../iroha` for the current SoraSwap trader entrypoints, with public module names normalized to `swaps`, `n3x`, `perps`, `farms`, `launchpad`, `options`, and `cover`.
- Torii now exposes the trader read plane needed by the cockpit:
  - `POST /v1/contracts/view/batch`
  - `GET /v1/contracts/rollups/swaps/fills`
  - `GET /v1/contracts/rollups/swaps/candles`
  - `GET /v1/contracts/rollups/trader/activity`
  - `GET /v1/contracts/rollups/trader/account`
- The trader UI now consumes rollups/account endpoints as its primary source for candles, fills, wallet metrics, module cards, and unified activity instead of rebuilding the page from ad hoc view stitching.
- The public `options` surface is unified in the cockpit instead of split `manager` and `factory` tiles.
- Real user-facing action rails now exist in the cockpit for swaps, `n3x`, perps, farms, launchpad, options, and cover.
- The Python proxy and local fixture server both speak the new trader rollup and batch-view surfaces.
- Browser smoke coverage in `tests/trader_ui.spec.js` now exercises the rollup-backed cockpit, explicit public candle `bucket_secs` queries, a capped 120-fill history render, and a signed trader action through the real Python server.
- `../iroha` compiles with the new trader metadata and route registrations (`cargo check -p iroha_torii`).
- Torii has a CID-routed app API gateway for pinned SoraFS route manifests:
  - `GET /v1/app-api/bindings`
  - `GET /v1/app-api/cid/{cid}`
  - `GET|POST /v1/app-api/cid/{cid}/{*path}`
  - `GET|POST /v1/app-api/active/{*path}`
- `make publish-trader-api` now builds the SoraSwap trader route manifest, pins it to SoraFS, writes deployment evidence, fails nonzero unless the report is fully completed, and can optionally bind the CID to a public SoraCloud service config.
- The local trader proxy caps rollup/activity history windows before forwarding requests to Torii, forwards candle bucket sizing only on the candle route, and keeps trader-account rollups authority-only, so browser/API callers cannot accidentally turn the cockpit into an unbounded public-node history scan during longer sessions.

## What is done

### Trader UI
- Swap candles, fills, wallet metrics, module cards, and unified activity now hydrate from dedicated Torii trader rollups.
- The cockpit follows contract-event SSE and falls back to polling when needed.
- Product rails are real for the currently supported trader surfaces instead of summary-only placeholders.

### `../iroha` support
- Canonical trader metadata now lands on supported successful contract calls.
- Trader rollups and batch-view reads are routable over Torii.
- Contract-event indexing remains available for direct event views and SSE.

## What is not done yet

The remaining gap for this slice is live environment validation and evidence, not the local implementation itself.

Still missing:
- Current-chain Taira preflight readiness. `deployments/testnet/preflight.latest.json` was refreshed at `20260708T180543Z` with `status: "blocked"` because public ingress now returns HTTP `502` for native MCP, `/status`, and `/v1/sumeragi/status`, leaving the live block and Sumeragi heights unavailable. Direct-validator diagnostics now retain only `direct-validator-1` through `direct-validator-4` labels, without direct hostnames or IP addresses.
- Completed external RWA compliance references only for an explicit `SORASWAP_ENABLE_RWA_RELEASE=1` market launch; the default DEX-only release now records current-chain RWA evidence as `not_applicable`.
- Production chain setup and signed production evidence after the Taira artifact gate is green. The retained `deployments/production/preflight.latest.json` was refreshed at `20260708T183136Z` with `status: "blocked"` because `config/production/production.client.toml` is missing, the production Torii root and chain id are unavailable, no production `chain.latest.json` exists and preflight now points to `make refresh-production-chain`, `nested_call_probe.latest_exists` is false, `SORASWAP_ALLOW_PRODUCTION_MUTATIONS=1` is absent, and oracle key material is unavailable; it also records the expected warning that production chain fingerprint assembly is unavailable until a production chain id or client config is present.
- Public/production validation for longer history ranges and real user load after the Taira artifact gate is green.

## Next steps

1. Restore Taira public MCP/write health, then rerun `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make taira-preflight` until it is ready before any signed release phase.
2. If launching an RWA market, set `SORASWAP_ENABLE_RWA_RELEASE=1` and record concrete issuer approval, legal review, compliance policy, NAV source, and redemption terms references before the signed release.
3. After Taira is green, run `SORASWAP_ALLOW_PRODUCTION_MUTATIONS=1 make release-production` and push the rollup-backed trader flow through public/production longer-history windows and real load.

## Current Taira evidence

Current refreshed-chain evidence:
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make taira-preflight` refreshed `deployments/testnet/preflight.latest.json` at `20260708T180543Z` with `status: "blocked"`: public ingress returns HTTP `502` for native MCP, `/status`, and `/v1/sumeragi/status`, so live public block and Sumeragi heights are unavailable. Direct-validator diagnostics are retained with ordinal labels only, not direct hostnames or IP addresses.
- `make refresh-testnet-chain` / signed probe setup refreshed `deployments/testnet/chain.latest.json` at `20260628T122755Z` for `https://taira.sora.org`, chain `809574f5-fee7-5e69-bfcf-52451e42d50f`, block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`.
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 SORASWAP_SKIP_IROHA_CLI_BUILD=1 SORASWAP_SKIP_KOTO_TOOL_BUILD=1 make testnet-nested-call-probe` wrote `deployments/testnet/nested_call_probe.20260628T122756Z.json` with `supported: true`: bytes state roundtrip, minimal `call_contract(...)`, and multi-hop nested `AssetOps` relay all completed against `xor#universal`.
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 SORASWAP_SKIP_IROHA_CLI_BUILD=1 SORASWAP_SKIP_KOTO_TOOL_BUILD=1 make taira-preflight` wrote `deployments/testnet/preflight.20260628T123429Z.json` with `status: "ready"`, supported current-chain nested probe evidence, and no blockers or warnings.
- The archived `deployments/testnet/archive/20260625T112040Z-516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5/nested_call_probe.20260624T224644Z.json` remains historical context only; that scratch deploy probe failed with `queue_unresolved_route` / `PRTRY:ROUTE_UNRESOLVED` during `deploy_bytes_probe`, with health `height=9533`, queue depth `149`, saturation true, age saturation unavailable, and view-change cause `quorum_timeout`.
- 2026-06-28 recorded `deployments/testnet/rwa_compliance.latest.json` as `status: "not_applicable"` for the default DEX-only release after current-chain probe/preflight readiness. `make release-checklist` now stops first at missing `deployments/testnet/deploy.latest.json`. `make release-production-checklist` also fails that Taira prerequisite and relays the retained blocked production setup summary.
- 2026-06-27 reran the current local source/UI/simulation batch without public mutations: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` passed. The run rebuilt ABI v1 artifacts for 25 contracts, refreshed `docs/interface_specs/generated.md`, refreshed primitive telemetry at `2026-06-27T08:06:49.191Z`, passed 8 simulation suites / 47 tests, 108 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 36 trader Python tests, and 7 trader Playwright tests.
- 2026-06-27 reran the full isolated localnet release rehearsal with `make test-local-isolated` after the migration-register static gate hardening. Fresh retained local evidence is `deployments/local/chain.20260627T162346Z.json`, `deployments/local/deploy.20260627T161240Z.json`, `deployments/local/contracts.20260627T164747Z.json`, and `deployments/local/smoke.20260627T171505Z.json`; the chain fingerprint records `http://127.0.0.1:18080`, chain `00000000-0000-0000-0000-000000000000`, block-1 hash `e057d45690f6995f1253036df65161ae54766119d0c3fc70c84bd45255ef4ae9`, the bundle receipt covers 25 contracts with `generated_at: "20260627T162346Z"`, and smoke records `status: "completed"`, `environment: "local"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, 10 registered trigger IDs, and 3 active trigger IDs. Cleanup left no isolated local peer running.
- 2026-06-27 reran the foundation isolated localnet rehearsal with `make test-local-foundation-isolated` after preserving the scoped aggregate bundle during retained-evidence restore. The wrapper now publishes foundation-scoped latest copies without clobbering the retained full release evidence; current foundation artifacts are `deployments/local/chain.20260627T123845Z.json`, `deployments/local/deploy.20260627T123631Z.json`, `deployments/local/contracts.20260627T124216Z.json`, `deployments/local/smoke.20260627T124429Z.json`, and `deployments/local/soraswap.foundation.bundle.deploy.json`, covering the 5 foundation contracts and smoke with 27 decoded state integers.
- 2026-06-27 reran focused validation after the migration-register static gate and full isolated local evidence refresh: `git diff --check`, `zsh ./scripts/redact_generated_evidence.sh --check`, and `make check-shell-syntax` passed. The static gate checked 55 shell scripts, 26 Kotodama contract sources, 1,042 documented make-command targets, 98 documented timestamped evidence paths, and 2,142 generated JSON artifacts with 0 redaction drift; read-only `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe and the production checklist relays the retained blocked production setup summary.
- 2026-06-27 hardened shared deploy-record scans so aggregate receipts such as `soraswap.bundle.deploy.json` and `soraswap.foundation.bundle.deploy.json` are never counted as per-contract records or deploy-nonce sources, even if a future platform receipt grows top-level contract-like fields. The contract-console and trader Python suites also cover contract-shaped foundation sidecars in catalog/evidence loading. `make test-public-env-helpers`, `python3 -m unittest tests.test_contract_console`, `python3 -m unittest tests.test_trader_ui`, `make check-shell-syntax`, and `git diff --check` passed; read-only `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe and retained production setup blockers.
- 2026-06-27 added explicit mutation-startup regressions for unsuccessful or sensitive aggregate bundle receipts, so `soraswap.bundle.deploy.json` with `ok: false` or unredacted private-key/token-style diagnostics blocks both the contract console and trader cockpit even when it is timestamped, chain-matching, and contract-shaped. The helper smoke also pins final release-checklist rejection for sensitive retained local bundle receipts and immediate phase-guard rejection for sensitive public bundle receipts. `python3 -m unittest tests.test_contract_console` now passes 111 tests, `python3 -m unittest tests.test_trader_ui` passes 38 tests, `make test-public-env-helpers` passes, `make check-shell-syntax` passes, and `git diff --check` is clean.
- 2026-07-05 hardened mutation-enabled public UI startup so a ready preflight with present oracle key files is still rejected unless `oracle_keypair_verified: true`; this blocks contract-console and trader-cockpit binding on mismatched or unverifiable oracle material. Focused validation passed: `python3 -m unittest tests.test_contract_console` (112 tests), `python3 -m unittest tests.test_trader_ui` (39 tests), and `python3 -m py_compile scripts/serve_contract_console.py scripts/serve_trader_ui.py`.
- 2026-06-27 hardened `make check-shell-syntax` so repo Markdown command snippets cannot reintroduce multi-script `zsh -n` examples that only parse the first script operand. Stale historical validation snippets were collapsed back to the authoritative `make check-shell-syntax` gate, and the helper smoke now pins the new static-check summary line. The make-level static gate, helper smoke, and whitespace check passed after this guard.
- 2026-06-27 moved migration-register production-row validation into the static gate as well as the final release checklist. The gate now rejects missing/empty parity ledgers, reference-only-only ledgers, and any production row that is still `stub`, `adapted`, `blocked`, blank, or typoed before later release checks run; the current register has 23 accepted rows.
- 2026-06-26 tightened the final release checklist's blocked-preflight nested-call hints so wrong-environment, untimestamped, or stale-chain `nested_call_probe.latest.json` artifacts no longer trigger runtime-rollout guidance or stale health output; they now direct operators to refresh signed probe evidence first. Stale saved-chain blockers keep `make refresh-testnet-chain` / `make refresh-production-chain` as the first recovery step and suppress duplicate standalone probe/runtime hints. The status-doc freshness gate uses the same current-probe test, so stale probe timestamps no longer satisfy current Taira documentation evidence. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed with the wrong-environment checklist regression. `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe and print the signed probe refresh command without a runtime-rollout hint.
- 2026-06-26 tightened preflight evidence generation to use that same current-probe test before copying nested-call `summary` or Sumeragi health into `preflight.latest.json`. Wrong-environment, untimestamped, or stale-chain probe artifacts now remain blockers without becoming preflight status evidence. `make test-public-env-helpers` passed with the wrong-environment preflight regression.
- 2026-06-26 tightened shared nested-call probe reuse so runtime support checks require non-empty `generated_at`, selected-environment provenance when supplied, and a full current chain fingerprint match before cached `supported: true` probe evidence can satisfy later public mutating paths. `make test-public-env-helpers` passed with timestamp and wrong-environment helper regressions.
- 2026-06-26 tightened shared deploy-record reuse so nonce planning, deployment skip/reuse, public smoke/catalog liveness checks, and the live alias/code verifier used by readonly smoke plus trader API publication require non-empty `generated_at`, selected-environment provenance when supplied, and a full current chain fingerprint match before using cached per-contract deploy records. `make test-public-env-helpers` passed with timestamp and wrong-environment deploy-record regressions.
- 2026-06-26 tightened shared saved-chain reuse so the current-or-saved chain fingerprint resolver now requires `chain.latest.json` to carry non-empty `generated_at`, selected-environment provenance, and the full `torii_url` / `chain` / `block_1_hash` tuple before the saved chain can feed deployment snapshots or downstream evidence checks. `make test-public-env-helpers` passed with the untimestamped saved-chain regression.
- 2026-06-26 tightened producer-side public snapshot checks so completed `snapshot_check` evidence now requires `contracts.latest.json` to cover the current Kotodama contract set exactly once, and `deploy.latest.json` to carry completed preflight, compile, nested-call-probe, deploy, bootstrap, and deployment-record snapshot phases, signer-readiness proof without debug bypass, and a pointer to the current `contracts.<generated_at>.json` snapshot. Public smoke, console, trader, and trader API reports now degrade when the contracts snapshot is partial/extra/duplicate or the deploy report has a matching snapshot path but any required deploy phase failed or degraded, immediate release phase guards reject duplicate aggregate contract snapshots before advancing, and mutation-enabled console/trader startup refuses duplicate `contracts.latest.json` evidence. `make test-public-env-helpers` passed with partial-contract-snapshot, duplicate-contract-snapshot, degraded-preflight, and degraded-deployment-snapshot regressions.
- 2026-06-26 tightened `make release-taira` and `make release-production` so missing, placeholder, reserved-domain, or control-character RWA external references stop full public release runners before any numbered phase can start when `SORASWAP_ENABLE_RWA_RELEASE=1`; production also stops before the isolated Taira prerequisite in that mode.
- 2026-06-26 extended retained preflight setup summaries to full `make release-taira` failures as well as production, so missing config, mutation consent, oracle material, or enabled RWA references now surface the current blocked preflight blockers before any numbered phase can start.
- 2026-06-26 tightened retained local smoke release evidence so `deployments/local/smoke.latest.json` must carry full-scope critical perps liquidation, intent/vault/operator/margin, RWA, DLMM hook, and conditional-escrow trigger mutation hashes, rejection evidence, stable state snapshots, registered/active trigger evidence, and epoch-auction close evidence. The release helper fixture now rejects sparse local smoke evidence even when it reports `status: "completed"`.
- 2026-06-26 tightened retained local contract evidence so `deployments/local/contracts.latest.json` must not only cover the current Kotodama contract set exactly once, but also include complete selected-environment address, deploy nonce, code-hash, ABI-hash, and matching local chain-fingerprint evidence for every snapshot entry.
- 2026-06-26 tightened retained local deploy evidence so `deployments/local/deploy.latest.json` must prove the full six-phase local rehearsal, exact deploy contract count, chunk-level contract coverage when chunked, and matching non-empty local deploy/contracts chain fingerprints.
- 2026-06-26 tightened retained local smoke provenance so `scripts/smoke_local.sh` writes `chain_fingerprint` into `deployments/local/smoke.latest.json`, and the release gate now requires retained local chain, deploy, contracts, and smoke fingerprints to match before accepting stale local rehearsal evidence, and `smoke-local` now fails before report writing if it cannot derive a non-null chain fingerprint.
- 2026-06-26 reran the current local verification batch after the latest RWA and release-gate hardening: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, `make test-trader-ui`, `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence` passed. The full simulation suite passed 8 suites / 47 tests, the UI/backend suites passed 106 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 35 trader Python tests, and 7 trader Playwright tests, the final redaction pass inspected 2,061 generated JSON artifacts with 0 rewrites, and the expected-fail release checklist state remained limited to the missing current-chain signed Taira nested-call probe.
- 2026-06-26 reran the direct local operator flow without public mutations: `make local-up`, `make deploy-local`, `make dev-build`, `make dev-smoke`, `make smoke-local`, and `make local-down` passed after the local full acceptance batch. Fresh retained local evidence is `deployments/local/chain.20260626T053132Z.json`, `deployments/local/deploy.20260626T052315Z.json`, `deployments/local/contracts.20260626T055356Z.json`, and `deployments/local/smoke.20260626T062028Z.json`; the deploy report records 25 contracts over 6 phases, the contracts snapshot records 25 contracts, and the full smoke report records `status: "completed"`, `environment: "local"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries. Follow-up `make redact-generated-evidence`, `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, `make release-checklist`, and `make release-production-checklist` were rerun; redaction inspected 2,088 generated JSON artifacts with 0 rewrites, the static/helper checks passed, and both release checklists still expected-fail only at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 made the shared public placeholder token patterns, contract-console signer autodiscovery, RWA recorder validator, final release checklist, and immediate release phase guard explicitly reject compact placeholder fragments such as embedded `changeme` / `replaceme` before configs, oracle overrides, or RWA evidence can be used or accepted. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed with recorder, checklist, phase-guard, and signer/config fixture regressions; `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-07-08 reran the non-mutating `make production-preflight` setup check while Taira remained the first production prerequisite. It refreshed `deployments/production/preflight.latest.json` at `20260708T183136Z` with `status: "blocked"`, no production chain fingerprint, no saved production `chain.latest.json`, `nested_call_probe.latest_exists: false`, unavailable oracle verification, and the same required setup blockers for real production config, Torii root, chain id, mutation consent, oracle keys, and signed production nested-call evidence.
- 2026-06-26 reran the focused local/source and evidence hygiene gates after the production preflight refresh: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make check-shell-syntax`, `make test-public-env-helpers`, `python3 -m unittest tests.test_contract_console`, `git diff --check`, and `make redact-generated-evidence` passed. The contract-console suite ran 108 Python tests, the redaction pass inspected 2,089 generated JSON artifacts with 0 rewrites, and `make release-checklist` plus `make release-production-checklist` still expected-fail only at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 tightened `make release-production-checklist` diagnostics so a failed isolated Taira prerequisite still exits at the Taira gate but also prints the retained production preflight status, timestamp, blockers, and warnings from `deployments/production/preflight.latest.json` when available. This keeps the production ordering gate intact while surfacing the production setup backlog in the same read-only command.
- 2026-06-26 tightened RWA recorder setup diagnostics so blocked or warning-bearing `preflight.latest.json` evidence prints a compact selected-environment preflight summary with status, timestamp, blockers, and warnings before the refresh/probe/preflight/reference setup order. The current `make record-production-rwa-compliance` failure now surfaces the retained production preflight blockers without accepting blocked preflight as RWA evidence.
- 2026-06-26 extended the same retained production preflight summary to full `make release-production` setup failures, so missing production config, consent, oracle material, or RWA reference stops still print the recorded production blockers before the static setup recipe and before any numbered phase can start.
- 2026-06-26 routed retained preflight setup-summary blockers and warnings through the shared redactor in full release runners and the standalone production checklist, so diagnostic `file://`, `/Users/...`, and `/tmp/...` paths do not leak into operator-facing setup failures.
- 2026-06-26 applied the same redaction to RWA recorder blocked-preflight failure details and setup summaries, with fixture coverage for raw local path diagnostics in both testnet and production retained preflight evidence.
- 2026-06-26 routed immediate release phase-guard retained blocker, blocked-reason, and summary details through the shared redactor as well, with release-runner fixture coverage for secret-like diagnostic text before phase advancement.
- 2026-06-26 hardened the same phase-guard detail redaction for direct helper sourcing without `scripts/common.sh`, so standalone diagnostics still redact secret-like values and local runtime paths.
- 2026-06-26 tightened contract-console public bridge smoke diagnostics so replay rejection reasons and unexpected proof/message submit responses are explicitly redacted before they are printed or retained in `contract_console_smoke.latest.json`. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; both release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 tightened public stale snapshot diagnostics in readonly smoke, mutating smoke, trader smoke, and contract-console smoke so `snapshot_check.output` is routed through the shared redactor before stderr printing. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. The release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 tightened local and public mutating smoke negative-test rejection evidence so expected rejection output is redacted before it is assigned into `smoke.latest.json.rejection_evidence`. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. Release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 expanded the shared redaction and browser sensitive-key vocabulary to cover OAuth-style `access_token`, `refresh_token`, and `client_secret` names in JSON keys, free-form diagnostics, CLI-style flags, phase-guard fallback scans, contract-console requests, trader requests, and access logs. focused contract-console/trader UI redaction unit tests, `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. Release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-27 expanded the shared redactor and browser access-log sanitizer to cover credentials embedded in URL userinfo plus sensitive URL query or fragment params such as `access_token`, `refresh_token`, `client_secret`, `api_key`, `authorization`, `token`, and `password`, while leaving non-sensitive URL params intact. The phase-guard fallback redactor and sensitive-diagnostic scanner use the same URL rules so hand-edited artifacts with unredacted URL secrets are rejected before release advancement; direct helper coverage now rejects raw URL-secret preflight evidence while accepting already-redacted URL evidence. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after this coverage hardening; redaction inspected 2,089 generated JSON artifacts with 0 rewrites.
- 2026-06-27 added `./scripts/redact_generated_evidence.sh --check` and wired it into `make check-shell-syntax`, so generated deployment and telemetry JSON must already be stable under the shared sensitive-data/runtime-path redactor before static acceptance passes. The helper smoke covers dirty check-mode rejection, clean check-mode success, and the static-check success summary. `./scripts/redact_generated_evidence.sh --check`, `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, and `git diff --check` passed; both the check-only and rewrite scans inspected 2,089 generated JSON artifacts with 0 pending changes or rewrites. Read-only `make release-checklist` and `make release-production-checklist` still expected-fail before any release claim at the missing current-chain signed Taira nested-call probe, with the production checklist also relaying the retained blocked production setup summary.
- 2026-06-27 reconciled the DLMM permissionless surface with release docs: `init_pool(...)` is documented as a generic asset-agnostic instantiation path, while the production SoraSwap deployment remains anchored on `xor#universal` and helper/NFT LP wrappers stay outside the v1 release surface. The helper smoke now pins the README, state layout, and migration-register wording. `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, and `git diff --check` passed after this guardrail update; redaction inspected 2,089 JSON artifacts with 0 rewrites.
- 2026-06-27 hardened `make check-shell-syntax` with a contract-source hygiene scan so accidental non-Kotodama files under `contracts/` fail static acceptance, except the existing shared helper README. The dev check/build/test/schema path, canonical lint/compile wrappers, final release checklist, and full Taira/production release runners now run the same source hygiene guard before contract tooling, public preflight, production-prerequisite, config setup, or live phase checks can advance. The helper smoke now includes `contracts/dlmm/permissionless_pool.md` regressions for the static gate, dev check/build/test/schema, lint/compile wrappers, final checklist, and full release runners. `make test-public-env-helpers`, `make check-shell-syntax`, `make dev-check`, `make dev-schema`, `make dev-test`, `make dev-build`, `make redact-generated-evidence`, and `git diff --check` passed after this guardrail; `make check-shell-syntax` reported 26 contract files checked and both redaction scans inspected 2,089 generated JSON artifacts with 0 changes. Read-only `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe, with the production checklist also relaying the retained blocked production setup.
- 2026-06-27 tightened the Taira state-repair plan workflow so sensitive free-form reason, snapshot-policy, and operator notes are exercised through the existing environment-variable inputs rather than CLI flags in smoke coverage, avoiding process-list exposure while preserving report redaction. The helper usage and operator runbook now warn operators to keep sensitive incident notes out of command-line arguments. a focused env-input repair-plan check, `make test-public-env-helpers`, and `git diff --check` passed after the change.
- 2026-06-26 routed final release-checklist blocked-preflight nested-call probe and health summaries, unsupported nested-probe summaries, nested-call probe stage outputs, `taira-preflight` copied probe summaries, public deploy blocked-probe summaries, trader blocked reasons, contract-console bridge helper output/proof-skip reasons, smoke negative-test unexpected-commit output, shared deploy confirmation-failure output, permission-grant failures, trigger-execute failures, alias-resolution failures, compiler failures, local perps open diagnostics, perps bootstrap init fallback output, Taira state-repair free-form operator inputs, and optional RWA compliance notes through the shared redactor as well. The final checklist and immediate phase guards now also reject stale or hand-edited RWA compliance artifacts whose optional notes would change under that redactor; immediate phase guards reject metadata-bearing phase artifacts with unredacted sensitive diagnostics, and the final checklist rejects otherwise passing target-chain, per-contract, bundle, or local evidence if the shared redactor would rewrite it. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed; redaction inspected 2,089 generated JSON artifacts with 0 rewrites, and both release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 moved release-checklist sensitive-diagnostic scanning earlier for `chain.latest.json`, `preflight.latest.json`, and any retained `nested_call_probe.latest.json`, so dirty blocked-preflight or stale-probe evidence fails before blocker, health, runtime, or status-doc diagnostics are printed. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed; redaction inspected 2,089 generated JSON artifacts with 0 rewrites, and both release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-25 reran the direct local operator path without public mutations: `make lint`, `make dev-test`, `make compile`, `make dev-schema`, `make dev-build`, `make dev-check`, `make local-up`, `make deploy-local`, `make dev-smoke`, `make smoke-local`, and `make local-down` all passed. The current local operator evidence is `deployments/local/deploy.20260625T171955Z.json`, `deployments/local/contracts.20260625T175012Z.json`, and `deployments/local/smoke.20260625T181707Z.json`; the smoke report records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 10 registered trigger entries.
- 2026-06-25 reran `../iroha/configs/soranexus/taira/verify_soraswap_rollout.sh --skip-mcp-check --skip-sorafs-check --skip-trader-app-api-check --skip-nested-call`; the local-only rollout verifier passed after `queue::router::tests::smart_contract_deploy_rule` passed 2 tests and `contract_call_transaction_preserves_three_hop_transfer_authorities` passed 1 test. Public Taira still needs the same runtime bundle rolled out before the signed nested-call probe can clear.
- Local acceptance now also requires `artifacts/telemetry/defi_2026_primitives_latest.json` to prove launch-ready 2026 primitive evidence for intents, the n3x vault, solver operators, DLMM hook orders, margin, and RWA before a release checklist can pass.
- The final release checklist now also requires `docs/release/smart_contract_production_audit.md` and `docs/release/production_readiness_checklist.md` to mention the current retained local chain, deploy, contracts, smoke, and primitive telemetry `generated_at` values, preventing stale local rehearsal summaries from outliving refreshed evidence.
- 2026-06-25 reran the local browser/simulation acceptance batch after generated-evidence hygiene hardening: `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` all passed. The post-simulation redaction pass inspected 2,007 generated JSON artifacts with 0 rewrites, and `make check-shell-syntax` plus `make test-public-env-helpers` now cover the generated-evidence and public-signer ignore policy.
- 2026-06-25 reran `make lint compile dev-schema dev-test`, `make dev-check dev-build`, `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence`; all local gates passed and the generated-evidence redaction pass inspected 2,007 JSON artifacts with 0 rewrites. `make dev-doctor` now reports the `make local-up` setup path before invoking the CLI when the default local profile config is missing, lacks `torii_url`, or points at an unreachable Torii endpoint.
- 2026-06-25 refreshed the same local/browser/simulation gates after the latest path-label hardening: `make test-contract-console` passed 101 Python tests, `make test-trader-ui` passed 32 Python tests plus 7 Playwright tests, `make test-contract-console-ui` passed 11 Playwright tests, `make test-contract-console-integration` passed 1 Python-server integration test, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence` all passed; the redaction pass inspected 2,016 generated JSON artifacts with 0 rewrites. After the read-only chain refresh, `make release-checklist` and `make release-production-checklist` now fail at the missing current-chain signed Taira nested-call probe before post-preflight release artifacts can be generated.
- 2026-06-25 reran the focused public-evidence hygiene gates after aligning the release checklist and RWA recorder missing-chain hints with `make refresh-testnet-chain` / `make refresh-production-chain`: `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence` passed; the redaction pass inspected 2,039 generated JSON artifacts with 0 rewrites. That pass left the release gate failing exactly at the missing current-chain signed Taira nested-call probe, surfaced as a blocked normal preflight.
- 2026-06-25 hardened missing-client-config diagnostics for the read-only chain refresh targets: `make refresh-testnet-chain` and `make refresh-production-chain` now print a repo-relative `next setup:` block before any network work. `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` passed after the change; the real `make refresh-production-chain` now stops with that setup hint because no untracked production client config is present.
- 2026-06-26 hardened read-only chain refresh config validation so `make refresh-testnet-chain` and `make refresh-production-chain` reject wrong-environment configs, placeholder/local/wildcard/example endpoints or key material, Taira configs whose chain id does not match the expected Taira chain unless `SORASWAP_TESTNET_CHAIN_ID` pins an intentional public reset, and production configs that still carry Taira's canonical chain id before any fingerprint fetch or evidence write. Public Taira preflight now applies the same unexpected-chain blocker in its generated report, so a copied config for another public reset cannot produce ready-looking preflight evidence unless the operator pins `SORASWAP_TESTNET_CHAIN_ID`.
- 2026-06-25 hardened `SORASWAP_SKIP_IROHA_CLI_BUILD=1` to strictly reuse existing `iroha`, `split_contract_deploy`, and `kagami` binaries instead of compiling when a helper is missing, and filtered Taira preflight setup hints so a missing nested-call probe prints only the signed probe command. `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 SORASWAP_SKIP_IROHA_CLI_BUILD=1 make taira-preflight` expected-failed at the recorded blocker; `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed, with redaction inspecting 2,042 generated JSON artifacts and rewriting 0. `make release-checklist`, `make release-production-checklist`, and `make record-testnet-rwa-compliance` still stop at the missing current-chain signed nested-call probe.
- 2026-06-25 hardened the RWA recorder diagnostics to print the full refresh/probe/preflight/reference order. A live `make record-testnet-rwa-compliance` still expected-fails because preflight is blocked by the missing signed probe, but its setup block now explicitly includes `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make testnet-nested-call-probe`; `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed after the change.
- 2026-06-26 tightened RWA recorder preflight acceptance so completed RWA evidence cannot be written from a ready-looking preflight that lacks an actual matching saved `chain.latest.json` artifact or current supported nested-call probe evidence. Manual chain JSON and chain-only inputs now have to match the ready preflight plus saved chain evidence instead of bypassing it. The live recorder still expected-fails at the missing current-chain signed Taira probe.
- 2026-06-25 refined release-checklist blocked-preflight diagnostics so the absent-probe case routes to `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make testnet-nested-call-probe` without printing the public runtime rollout verifier. The rollout verifier remains the next diagnostic only after a current probe exists and records unsupported nested-call behavior.
- 2026-06-25 tightened the status-doc freshness gate so blocked Taira preflight with no `nested_call_probe.latest.json` requires the release status docs to mention `nested_call_probe.latest_exists`, not just the current preflight timestamp and block-1 hash. The helper smoke now covers stale absent-probe docs.
- The status-doc freshness gate now applies to the selected public environment: Taira keeps the audit/readiness/devex coverage, and production evidence requires the audit plus readiness checklist to mention the current production block-1 hash and blocked preflight/probe timestamps before the final checklist can advance.
- 2026-06-25 fixed release-checklist jq defaults that could mask explicit `false` fields in blocked preflight/probe evidence. Stale-chain and missing-probe hints now trigger from `saved_snapshot_matches: false` and `latest_exists: false` even without matching blocker text, and unsupported probe hints trigger from `supported: false`.
- 2026-06-25 centralized immediate release phase-guard checks for supported nested-call probe evidence, so preflight, RWA, nested-probe, and mutating-smoke paths now share the same current-chain enforcement and recovery hints for stale saved-chain evidence, missing/stale probe evidence, and unsupported current Taira probes. `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` passed; `make release-checklist` still expected-fails at the missing current-chain signed Taira nested-call probe.
- Bridge replay fallback evidence is now self-validating in retained release artifacts: final checklist and phase guards require replay/duplicate/consumed/proof-overlap or known bridge assertion text on rejected message status, not only `status_kind: "Rejected"`.
- Contract-console bridge status evidence now uses exact status-kind matching across producer smoke, final checklist, and phase guards, so retained artifacts cannot pass with substring values such as `NotApplied`.
- `docs/interface_specs/generated.md` has been refreshed from `make dev-schema` and now reflects the compiled options stale-oracle admin/view entrypoints plus current generated state-key counts.
- 2026-06-25 hardened public post-deploy evidence producers and consumers with a shared deploy-snapshot freshness check. Readonly smoke, mutating smoke, contract-console smoke, trader smoke, and trader API publication now record `snapshot_check` and refuse or downgrade evidence when `contracts.latest.json` / `deploy.latest.json` is missing, stale, wrong-environment, incomplete, or not tied together by deploy snapshot detail; release phase guards and `make release-checklist` require completed `snapshot_check` fields on those post-deploy reports. `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` passed; `make record-testnet-rwa-compliance`, `make release-checklist`, and `make release-production-checklist` still expected-fail at the missing signed Taira nested-call probe.
- 2026-06-25 reran focused release-guard validation after the snapshot-check hardening and latest direct local operator flow: `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, and `make redact-generated-evidence` passed. The redaction pass inspected 2,047 generated JSON artifacts with 0 rewrites; a transient host disk-pressure failure was resolved by clearing generated sibling build output before the passing reruns.
- 2026-06-26 reran focused validation after bridge status-kind hardening and generated interface refresh: `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, `make dev-schema`, `make dev-check`, `make compile`, `make test-contract-console`, `make test-contract-console-ui`, and `make test-trader-ui` passed. The current UI/backend acceptance counts are 106 contract-console Python tests, 11 contract-console Playwright tests, 35 trader UI Python tests, and 7 trader UI Playwright tests. The bridge runbooks now document exact `status_kind` matching and replay-rejection detail requirements, while `make release-checklist` and `make release-production-checklist` still stop at the missing current-chain signed Taira nested-call probe.
- 2026-06-25 reran the full isolated localnet acceptance gate with `make test-local-isolated` after the local smoke schema hardening; deploy, bootstrap, and smoke completed and cleanup left no isolated peer or smoke process running. Fresh local evidence is `deployments/local/deploy.20260625T070345Z.json`, `deployments/local/contracts.20260625T073317Z.json`, and `deployments/local/smoke.20260625T075815Z.json`; the smoke report records top-level `status: "completed"` and `environment: "local"`, 92 transaction-hash slots, 83 decoded state integers, 5 rejection entries, and 8 trigger entries. The release checklist enforces those fields plus local deploy/contracts/smoke freshness ordering and deploy snapshot-to-contracts timestamp agreement; public deploy phases now enforce the same deploy snapshot-to-current-contracts link and validate any aggregate `soraswap.bundle.deploy.json` receipt against `chain.latest.json` plus the current contracts snapshot before release runners can advance.
- 2026-06-25 promoted `make dev-check` and `make dev-build` into formal release-checklist local acceptance and isolated those local acceptance targets from every inherited `SORASWAP_*` variable plus generic chain/account-discriminator env (`CHAIN`, `ACCOUNT_CHAIN_DISCRIMINANT`, and `IROHA_ACCOUNT_CHAIN_DISCRIMINANT`); the helper smoke test now fails on unexpected `SORASWAP_` or chain-discriminator leaks and parses high-risk public/toolchain/chunking/scope-trigger/oracle/assertion selectors to catch future scrub drift. `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` pass after this checklist hardening.
- 2026-06-25 hardened nested release `make` dispatch by clearing inherited `MAKEFLAGS`, `MFLAGS`, `GNUMAKEFLAGS`, `MAKEFILES`, and `MAKEOVERRIDES` before checklist local acceptance, full Taira/production phase targets, and the production Taira prerequisite; full Taira/production phase targets also clear generic `CHAIN`, `ACCOUNT_CHAIN_DISCRIMINANT`, and `IROHA_ACCOUNT_CHAIN_DISCRIMINANT`. The helper smoke now fails if make-control variables or stale generic account-derivation variables reach a nested release target, closing the dry-run/injected-makefile and stale-chain paths for false local acceptance.
- 2026-06-25 extended the same blanket env scrub to both production Taira prerequisite entrypoints: the full `make release-production` runner and the standalone `make release-production-checklist` recursive checklist gate. They pass only the explicit testnet release selector to the inner checklist, and the skip-local-acceptance mode is now reached through a tokenized private recursive script argument instead of exported internal env flags, so production credentials, Torii overrides, alternate tooling, local-coverage knobs, RWA/trader publication controls, mutation-consent env vars, stale internal flags, stale internal private tokens, or stale account-discriminator env cannot leak into the no-production-mutation Taira gate.
- Current-chain release evidence is still absent for `deploy.latest.json`, `contracts.latest.json`, `smoke.latest.json`, `contract_console_smoke.latest.json`, `trader_readonly.latest.json`, `trader.latest.json`, `trader_api_bundle.latest.json`, and `rwa_compliance.latest.json`.

## Near-term acceptance bar

This roadmap slice can be called fully complete when:
- the current SoraSwap trader surfaces emit canonical trader-facing metadata,
- the trader cockpit uses Torii rollups as its primary data source,
- the module rails are real for the supported products,
- local and Taira validation are both green,
- the pinned trader route bundle resolves through public Taira's CID app API gateway,
- the release evidence is generated, retained under the appropriate `deployments/<env>/` evidence directory, and accepted by the release checklist.
