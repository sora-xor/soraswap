# RWA Market State Layout

RWA market state is keyed by `market_id`; redemption requests are keyed by `redemption_id`.

Tracked values:
- controller, share asset, NAV asset, NAV per share, total shares, status
- redemption owner, market, shares, status
