# Taira Deploy Devex Critique

Observed on 2026-05-23 from this worktree while deploying SoraSwap to public Taira.

## Current 2026-07-10 Status
- The May public deployment evidence below is historical and no longer supports a current production claim after the refreshed Taira chain snapshot.
- Current Taira release gate is blocked again, despite retained downstream evidence:
  - Chain evidence `generated_at: "20260709T135055Z"` records `https://taira.sora.org`, chain `809574f5-fee7-5e69-bfcf-52451e42d50f`, and block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`.
  - Preflight evidence `generated_at: "20260709T135317Z"` is `status: "blocked"` because the latest committed block is stale and the Sumeragi worker is idle while the transaction queue is non-empty. No SoraSwap public write may run until validator convergence and finality recover.
  - Nested-call probe evidence `generated_at: "20260708T133317Z"` remains current-chain `supported: true` for state bytes roundtrip, minimal nested `call_contract(...)`, and multi-hop nested AssetOps relay.
  - Deploy evidence `generated_at: "20260709T072723Z"` remains `status: "running"`; retained contracts evidence at `generated_at: "20260709T010900Z"` covers 25 contracts but does not satisfy the release gate until a fresh six-phase deploy completes.
  - DEX-only RWA evidence `generated_at: "20260709T062847Z"` is `status: "not_applicable"` with `rwa_release_enabled: false`; it must be refreshed after the next ready preflight.
  - Readonly smoke evidence `generated_at: "20260709T032707Z"` is completed against the current deploy/contracts snapshots. The retained signed `smoke.latest.json` at `generated_at: "20260709T020326Z"` is completed but still not release-grade because it is missing the required DLMM collect-fees mutation hash.
  - Contract-console evidence `generated_at: "20260709T025646Z"`, readonly trader evidence `generated_at: "20260709T025955Z"`, signed trader evidence `generated_at: "20260709T030104Z"`, and trader API evidence `generated_at: "20260709T030233Z"` are completed against the current deploy/contracts snapshots.
  - The latest signed smoke retry on 2026-07-09 retained diagnostic-only failure evidence at `deployments/testnet/smoke.failed.20260709T033600Z.json` after submitted route-swap, DLMM collect-fees, RWA market update, and options oracle configuration calls, when public finality/write health degraded during visibility wait for an options oracle configuration transaction.
- Current saved chain evidence is `deployments/testnet/chain.20260709T135055Z.json`, chain id `809574f5-fee7-5e69-bfcf-52451e42d50f`, block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`, and Torii root `https://taira.sora.org`.
- Current signed nested-call probe evidence is `deployments/testnet/nested_call_probe.20260708T133317Z.json`. It records `supported: true`: bytes state roundtrip, minimal `call_contract(...)`, and multi-hop nested `AssetOps` relay all completed against `xor#universal`.
- Current Taira preflight evidence is `deployments/testnet/preflight.20260709T135317Z.json` with `status: "blocked"`; signed release phases must wait for stable public finality/write health and a successful fresh preflight.
- Current DEX-only RWA evidence is `deployments/testnet/rwa_compliance.20260709T062847Z.json` with `status: "not_applicable"` and `rwa_release_enabled: false`; completed external RWA references are still required if `SORASWAP_ENABLE_RWA_RELEASE=1` or public smoke later proves RWA launch activity.
- Retained deploy, readonly smoke, console, trader, and trader API evidence from 2026-07-09 now points at the current deploy/contracts snapshots, but `smoke.latest.json` still lacks release-grade DLMM collect-fees evidence and the latest repaired signed-smoke retry failed on public finality. Operators must restore stable write health and rerun signed smoke before treating the retained downstream artifact set as a release candidate.
- The previous `deployments/testnet/archive/20260625T112040Z-516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5/nested_call_probe.20260624T224644Z.json` records `supported: false`: the first scratch `/v1/contracts/deploy` repeatedly returned `queue_unresolved_route` / `PRTRY:ROUTE_UNRESOLVED`. That probe is historical context after the chain refresh and after the current 2026-06-28 signed probe above.
- Local `../iroha` now includes a router regression fix for universal contract deploys that match a non-universal `smartcontract::deploy` policy rule; the rollout verifier also runs the three-hop nested transfer authority canary before public probes. The current public Taira probe above proves that route and nested-transfer behavior on the refreshed chain.
- The older `deployments/testnet/preflight.20260625T143703Z.json`, the 2026-06-27 chain refresh, the earlier 2026-06-28 blocked-preflight/unsupported-probe artifacts, and the earlier ready or blocked 2026-07-07 / `20260708T022604Z` / `20260708T085116Z` / `20260708T091603Z` / `20260708T094517Z` / `20260708T102137Z` / `20260708T153104Z` / `20260708T180543Z` preflight artifacts are superseded by the current ready `20260709T005125Z` preflight and the supported nested probe above.
- `make refresh-testnet-chain` now covers the read-only acceptance step for that reset: it updates only local chain evidence and archives stale generated reports, leaving the signed `testnet-nested-call-probe` as the separate runtime capability check.
- `make taira-preflight` and `make production-preflight` now default `SORASWAP_SKIP_IROHA_CLI_BUILD=1` when unset, so a bounded public health check reuses an existing `iroha` CLI instead of starting a sibling `cargo build`; operators can still set `SORASWAP_SKIP_IROHA_CLI_BUILD=0` explicitly when they want preflight to build a stale or missing CLI.
- `make release-checklist` on 2026-07-09 must fail at signed mutating smoke until Taira public write health is stable enough for a fresh `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet` run to complete with the required DLMM collect-fees evidence. `make release-production-checklist` remains blocked by that Taira prerequisite before any production artifact can count.
- Two 2026-06-29 signed `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet` attempts started after the shared write-health guard passed, but both were interrupted before completion when Sumeragi write health degraded again while polling public transaction visibility (`tx_queue.depth: 1`, `saturated_by_age: true`, `worker_stage: "idle"`). Later diagnostic runs retained `smoke.failed.*.json` sidecars with submitted-call traces and public write-health samples; incomplete contract-console runs retain `contract_console_smoke.failed.*.json` sidecars after any setup/proof/message transaction is submitted; incomplete signed trader runs preserve submitted `route_swap` traces and public write-health samples inside `trader.latest.json`. Those diagnostics preserve root-cause evidence but do not satisfy release gates. The latest 2026-07-09 signed smoke diagnostic stopped with stale committed block age around 699s and queued writes while waiting for transaction visibility. Signed public mutation phases should wait for stable finality/write health before regenerating signed smoke.
- `release-taira` and `release-production` now print numbered `[n/12]` phase banners plus expected and ready evidence paths for each public release target.
- Immediate release phase guards and the final release checklist now require the same current release-ready preflight baseline before deploy, RWA, smoke, contract-console, trader, or trader API evidence can advance or be accepted. That includes mutation consent, native MCP HTTP `200`, `endpoint.health_issues: []` from JSON-readable `/status` and `/v1/sumeragi/status`, signer/account/fee readiness, usable oracle key sources, `oracle_keypair_verified: true`, matching saved chain evidence, current supported nested-call probe evidence, and preflight metadata with `nested_call_probe.latest_exists`, `matches_current_chain`, and `supported` all true.
- `docs/release/taira_operator_runbook.md` now records the SoraSwap-facing Taira rollout inputs: MCP, gas metadata, dataspace manifest hashes, SCCP transparent-proof policy, canary signer handling, direct-node checks, and snapshot repair policy. `make taira-state-repair-plan` writes non-mutating repair-plan evidence before any storage operation.

## Historical 2026-05-23 Evidence
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make taira-preflight` reported `status: "ready"` in the then-current `deployments/testnet/preflight.latest.json`.
- At that time, public Taira reported native Torii MCP `200`, faucet puzzle `200`, block height `621`, chain id `809574f5-fee7-5e69-bfcf-52451e42d50f`, and block-1 hash `1a824af79608802b0999db4efeaa5f070a56f0fced274e2a427aea86770fbd99`.
- The runtime client config is untracked, non-placeholder, and kept outside committed source. Taira preflight derives the deployment authority and native oracle provider from that client config signer by default.
- At that time, public Taira was running the staged `../iroha` release daemon with SHA-256 `b6f9050d6a901762acf4a137fb9f042ec6a6af1c352ad8e2c581e94c426f173c`.
- `deployments/testnet/deploy.latest.json` is `completed`; its `preflight`, `compile`, `nested_call_probe`, `deploy`, `bootstrap_contract_state`, and `deployment_records_snapshot` phases are all `completed`.
- `deployments/testnet/contracts.latest.json` records the full deployed `universal` dataspace contract surface, including n3x, DLMM pool/router, launchpad, referral, automation, farms, risk, perps, options, cover, bridge, intents, vaults, operators, margin, RWA, DLMM hooks, and conditional escrow.
- The then-current nested-call probe recorded `state_bytes_roundtrip_supported == true`, `nested_call_supported == true`, `nested_asset_ops_supported == true`, and `supported == true` against canonical `xor#universal` asset definition id `6TEAJqbb8oEPmLncoNiMRbLEK6tw`. That was May historical evidence and is superseded by the refreshed-chain status above.
- `deployments/testnet/smoke.latest.json` records 91 public mutation hashes and includes evidence for intent, vault, operator, margin, RWA, and DLMM hook flows.
- `deployments/testnet/contract_console_smoke.latest.json` proves the governed bridge route with `route_provenance[0] == 1`; proof and message submissions both reached `Applied`.
- `deployments/testnet/trader_readonly.latest.json` is `completed`, has no missing required trader routes, covers 11 trader read-plane routes, and includes explicit swap-candle bucket sizing in the route evidence.
- `deployments/testnet/trader.latest.json` is `completed`; the final signed swap tx is `e27e9d998c7e498d8348d2898a508d45d4e782d0cb08ace5ba1a0f55fc1130f9`, committed at block height `621`.
- Remote peer logs show all four validators committed block `84611e87db807fabe74b05dab475b473bfcb852ebe969a1eb3b11e40d11cb8cd` at height `621`.

## What Works
- The deploy path now has useful staged evidence: chain fingerprint, nested runtime capability probe, deploy receipt, contract records, full smoke, console smoke, trader readonly, and trader mutation reports.
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make taira-preflight` is the right cheap first release-readiness command. It is non-mutating, but it still catches client-config shape, mutation consent, MCP health, faucet health, current chain fingerprint, signer derivation, signer funding, and nested-call capability before the expensive release path.
- Defaulting the Taira oracle provider to the client config signer removes a needless setup fork. Explicit oracle env vars remain available when an operator really needs a different provider key.
- The nested-call probe now separates codec/state storage, minimal `call_contract(...)`, nested AssetOps relay, and asset-id wiring. That distinction was essential: one failure was an incorrect SoraSwap fallback asset id, not a runtime call bug.
- Public contract-call success now tolerates terminal pipeline `Applied|Committed` when committed transaction lookup cannot decode an older public response. That matches how Taira actually behaves under load.
- Waiting for contract deploy nonce visibility avoids immediate back-to-back public deploy address reuse.
- The deploy and smoke artifacts are concrete enough to debug without rerunning every expensive phase.

## Friction
- The deployment depended on live sibling `../iroha` fixes and rollout operations, not just SoraSwap scripts. Missing Torii gas metadata, trader alias parsing, contract view selector behavior, IVM pointer handling, IVM chain-discriminant handling, and SCCP transparent-proof testnet behavior all surfaced as deployment blockers.
- Taira node rollout is still operationally manual. The SoraSwap runbook now names the required MCP, dataspace, canary signer, gas, SCCP, and snapshot policy checks, but the actual rollout still depends on live sibling `../iroha` operations.
- State repair was the hardest devex gap. A root split at height `621` was ultimately caused by validators starting height `620` from different state roots. The logs originally reported only group counts; a diagnostic build was needed to print parent/post roots and signer groups.
- Snapshot portability is fragile. Copying a healthy peer snapshot to other validators required aligning snapshot verification/signing config, because snapshot signatures are validated against configured snapshot keys, not simply against trusted validator state. That is reasonable security-wise, but operationally obscure during emergency repair.
- The CLI status helper still has mixed semantics around pipeline scopes. Passing `auto` to a pipeline-status route can produce `400`; scripts compensate in places, but the control-plane contract should be clearer.
- Release output still has many materially different phases, but the runners now expose the current phase and evidence paths directly in the console output.
- The May public deployment evidence was green for deployment and smoke, but not a full production-cutover claim by itself. The stricter release checklist still expects `trader_api_bundle.latest.json` and `rwa_compliance.latest.json`, which are intentionally separate release artifacts.

## Recommendations
- Keep MCP `200` as a hard Taira readiness gate. CLI/HTTP paths are useful diagnostics, but native MCP health should remain part of the release claim.
- Keep `docs/release/taira_operator_runbook.md` in sync with `../iroha/configs/soranexus/taira/config.toml` and rollout scripts when MCP, gas, dataspace, SCCP, canary signer, or snapshot policy changes.
- Promote the commit-root split diagnostic from an ad hoc troubleshooting patch into normal warning output, with parent root, post root, signer indexes, and peer ids.
- Promote the plan-only `make taira-state-repair-plan` helper into an apply-capable runtime tool only after the sibling `../iroha` storage layout and snapshot sidecar compatibility rules are approved by runtime operators.
- Keep `taira-preflight` and `release-taira` setup hints in lockstep. When a new runtime input is added, both commands should name the same env vars and artifact paths.
- `release-taira` setup failures now print the retained `deployments/testnet/preflight.latest.json` status, timestamp, blockers, warnings, and health issues when available; keep that diagnostic path redacted and clearly separate from release evidence.
- Keep the numbered phase and evidence-path output in `release-taira` and `release-production` in sync when adding or removing release targets.
- Keep `SORASWAP_XOR_ASSET_DEFINITION_ID` defaulted to Taira's canonical `6TEAJqbb8oEPmLncoNiMRbLEK6tw` until the public `xor#universal` alias is query-visible everywhere.
- Treat `nested_asset_ops_supported == false` as ambiguous until the report's `probe_asset.asset_definition_id` is checked. It can be either a runtime blocker or a bad asset-id wiring bug.
- Do not use the deployment evidence alone for production cutover. Require the production readiness checklist artifacts, especially trader API publish evidence and RWA compliance evidence; external RWA references are mandatory only for an explicit RWA market launch.
