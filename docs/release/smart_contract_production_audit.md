# Smart Contract Production Audit

Date: 2026-06-21; release-evidence notes updated 2026-07-10

Scope:
- `contracts/**/*.ko`
- Local compile/test/deploy entrypoint scripts relevant to contract state bootstrap
- Interface and state-layout documentation for changed surfaces

## Executive Summary

The audit did not leave any known Critical or High smart-contract findings open in the local source tree. The main production-readiness gaps found during review were owner/admin paths that could write invalid risk state, options oracle paths that documented stale-slot rejection without enforcing it everywhere, and governed bridge route provenance that could be rewritten by a later proof-authority activation.

Those findings are fixed in source, covered by Kotodama regressions, and reflected in bootstrap/docs. The full local deployment path now has fresh isolated-localnet evidence for the 25-contract Kotodama surface.

Current Taira release evidence as of 2026-07-10 is blocked before any fresh signed release claim:
- Chain evidence `generated_at: "20260709T135055Z"` records `https://taira.sora.org`, chain `809574f5-fee7-5e69-bfcf-52451e42d50f`, and block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`.
- Preflight evidence `generated_at: "20260709T135317Z"` is `status: "blocked"` because the latest committed block is stale and the Sumeragi worker is idle while the transaction queue is non-empty. No SoraSwap public write may run until the validators converge and write finality is stable.
- Nested-call probe evidence `generated_at: "20260708T133317Z"` remains current-chain `supported: true`, covering state bytes roundtrip, minimal nested `call_contract(...)`, and multi-hop nested AssetOps relay.
- Deploy evidence `generated_at: "20260709T072723Z"` is still `status: "running"`; the retained contracts evidence at `generated_at: "20260709T010900Z"` covers the 25-contract Kotodama surface but cannot make the running deploy release-grade.
- DEX-only RWA evidence `generated_at: "20260709T062847Z"` is `status: "not_applicable"` with `rwa_release_enabled: false`; it must be refreshed after a new ready preflight.
- Readonly smoke evidence `generated_at: "20260709T032707Z"` is completed against the current deploy/contracts snapshots. The retained signed `smoke.latest.json` at `generated_at: "20260709T020326Z"` is completed but still not release-grade because it is missing the required DLMM collect-fees mutation hash; the latest signed retry retained diagnostic-only failed evidence at `generated_at: "20260709T033600Z"` after public finality/write health degraded while waiting for an options oracle configuration transaction.
- Contract-console evidence `generated_at: "20260709T025646Z"`, readonly trader evidence `generated_at: "20260709T025955Z"`, signed trader evidence `generated_at: "20260709T030104Z"`, and trader API evidence `generated_at: "20260709T030233Z"` are completed against the current deploy/contracts snapshots.
- Public preflight now defaults `SORASWAP_SKIP_IROHA_CLI_BUILD=1` when unset, so status checks reuse an existing `iroha` CLI and do not start a sibling `cargo build` unless an operator explicitly sets `SORASWAP_SKIP_IROHA_CLI_BUILD=0`.
- Release phase 12 is fail-closed across the documentation update boundary. Full Taira/production runners require signed SoraSwap RC and exact-candidate pins before any mutation, revalidate them around every phase, and require every fixed phase artifact to replace its pre-phase identity, hash, bytes, and `generated_at`. Atomic mode-`0600` receipts bind the fixed 11-phase mapping to one chain and one deploy/contracts snapshot. The pending checkpoint binds the signed RC/tree, HEAD source identity/type/mode and non-status bytes, staged/worktree status-doc equality, absence of non-ignored untracked source, evidence/timestamps, RWA mode, receipts, and candidate bundle/archive hashes. Resume runs all three Git whitespace checks plus strict nonmutating gates and guarded checkpoint deletion; production also revalidates Taira. Either environment's pending state is a global release lock. Same-user hashes detect corruption and drift but are not an authorization boundary against deliberate rewriting by that user.
- Production closeout now adds a signed external authorization boundary before phase 1 and a real 30-minute observation inside phase 12. The RC-bound trust policy requires distinct SSH Ed25519 keys and independent security/operations signatures; the runtime approval binds chain, RC/source, Iroha/bundle/binary/archive hashes, five distinct authorities, operational controls, monitoring watch sets, fee minimum, and trader API identity, and is reverified around every phase. The observer uses fd-bound nofollow reads, mode/single-link checks, same-origin authenticated Torii status/CID reads, an independent signed-policy monitoring origin, cryptographic peer-set identity, committed-height/commit-QC semantics, 61 samples, advancing finality, zero queue/lane/API failures, oracle/balance/readonly/CID checks, and a fail-closed external all-derivatives pause boundary without claiming that the pause occurred. Focused adversarial coverage is `make test-production-cutover`.
- This does not make production ready by itself. The real tracked `config/production/cutover-trust-policy.json`, its trusted public keys, the mode-`0600` signed runtime approval, real production client config and non-Taira chain snapshot, distinct admin/treasury/bridge inputs, approved fee minimum, independent monitoring endpoint/watch hashes, and live production evidence are external inputs and are currently absent. They must not be synthesized from examples.

Current retained local evidence:
- Local chain evidence `generated_at: "20260708T173108Z"`, deploy evidence `generated_at: "20260708T172811Z"`, contracts evidence `generated_at: "20260708T173745Z"`, and full smoke evidence `generated_at: "20260708T174402Z"` are retained from the 2026-07-08 full isolated local acceptance rerun. The smoke harness paused `soraswap_range_governor_tick`, `soraswap_twamm_tick`, and `soraswap_perps_lifecycle_tick` during deterministic smoke execution, then restored them before writing completed evidence.
- A later local chain/deploy diagnostic pair has `generated_at: "20260710T073509Z"`; its deploy report is `status: "failed"` after stopping during `bootstrap_assets`, so it is not completed local or production evidence and does not replace the July 8 acceptance baseline.
- The exact-candidate verification attempt on 2026-07-11 retained diagnostic-only local chain evidence at `generated_at: "20260711T040919Z"` and deploy evidence at `generated_at: "20260711T040444Z"` with `status: "failed"` after all 25 deploy chunks completed but contract-state bootstrap exposed a hardcoded debug Kagami path. The explicit release `KAGAMI_BIN` path is now propagated through bootstrap; `contracts.latest.json` and `smoke.latest.json` remain the completed July 8 records at `generated_at: "20260708T173745Z"` and `generated_at: "20260708T174402Z"`. The attempt was stopped only through its generated `stop.sh`, and its failed run directory remains diagnostic evidence.
- Current primitive telemetry at `artifacts/telemetry/defi_2026_primitives_latest.json` has `generated_at: "2026-07-11T04:01:38.538Z"`.

Do not declare production cutover complete until the production target has matching production evidence and external operational signoff. The retained Taira deploy/smoke evidence is not production-chain evidence, and Taira finality/write health must stay stable through a fresh ready preflight and signed smoke before any new Taira or production release claim.

## Fixed Findings

### H-001: Options signed oracle settlement did not enforce stale-slot bounds

Impact: A valid old oracle signature could be replayed within monotonic-slot constraints if no fresher value had yet been recorded for the series or shout position.

Status: Fixed.

Changes:
- Added `OptMgrOracleStaleSlots`, `OptFactoryOracleStaleSlots`, and `ShoutOracleStaleSlots`.
- Defaulted each options verifier to `4` slots on init.
- Added owner-only `configure_oracle_stale_slots(...)` and `oracle_stale_slots()` views.
- Enforced `block_height() - oracle_slot <= oracle_stale_slots` on manager/factory/shout signed oracle paths and on manager/shout C2C numeric oracle paths.
- Added `SORASWAP_OPTIONS_ORACLE_STALE_SLOTS` bootstrap reconciliation.

### H-002: Perps `update_market` bypassed registration-grade risk validation

Impact: The owner could update an existing market into invalid or impossible risk state, including bad active flags, negative operational limits, invalid utilisation clamps, or out-of-bounds fee/margin values.

Status: Fixed.

Changes:
- Added shared market-risk parameter validation.
- Reused it for both `register_market(...)` and `update_market(...)`.
- Added regression coverage for accepted valid updates and rejected invalid replacements.

### M-001: Governed bridge route provenance was mutable after activation

Impact: A proof authority could re-activate an already governed route with the same route fields but a different governance message, rewriting the provenance used by release evidence.

Status: Fixed.

Changes:
- Repeated governed activation remains idempotent only for the original `message_id`.
- A different `message_id` on an already governed route now rejects with `route governance mismatch`.
- Added regression coverage for governed route provenance immutability.

### M-002: Options admin setters accepted impossible guard/template state

Impact: Owner-only options setters could configure invalid active flags or BPS thresholds above `10_000`, making later user-facing behavior difficult to reason about.

Status: Fixed.

Changes:
- `update_template(...)` now requires binary `active`.
- `configure_utilisation_guard(...)` now caps activate, deactivate, and pause thresholds to `10_000`.
- `sync_series(...)` now rejects negative expiry slots.
- Added regression coverage for invalid options admin values.

### T-001: Kotodama `expect_reject_as` test harness used stale return arity

Impact: Rejection tests against no-return entrypoints could fail with a test-host arity error instead of observing the intended contract rejection, weakening regression reliability.

Status: Fixed in the neighboring `../iroha` toolchain.

Change:
- `koto_test` now ignores stale return-arity registers for `expect_reject_as` calls because rejected calls never consume return values.

### T-002: Local deploy/smoke gates did not cleanly support foundation-only release evidence

Impact: The local foundation deployability check could be blocked by later-module contracts, missing explicit unit permissions, or stale temporary scoped manifests even when the N3X/DLMM foundation was deployable.

Status: Fixed.

Changes:
- Added `SORASWAP_DEPLOY_SCOPE=foundation` handling for the local deploy wrapper.
- Scoped local foundation smoke trigger expectations to `soraswap_epoch_auction_close`, `soraswap_range_governor_tick`, and `soraswap_escrow_settle`.
- Granted explicit unit `Admin`/`AssetOps` permissions to the local authority and `AssetOps` to deployed contract subject accounts during bootstrap.
- Switched epoch native-close proof to verify the active-trigger index after close.
- Cleaned up temporary foundation manifests on successful scoped deploys.

### T-003: `make dev-test` fell through to runtime CLI config loading

Impact: The manifest-declared Kotodama regression suite could be blocked by unrelated Iroha CLI client-config loading before `koto_test` ran, weakening the local production gate.

Status: Fixed.

Changes:
- Added explicit `test` handling in `scripts/dev_iroha.sh` that reads manifest `[[tests]]` entries and runs each file through `koto_test run`.
- Added public helper smoke coverage with a fake `koto_test` binary to ensure the wrapper test path does not require runtime CLI config.

### T-004: Manifest tooling allowed stale generated contract outputs

Impact: A removed or renamed contract could leave ignored `.to` or manifest outputs under `artifacts/compiled/`, and a manifest typo could reach Kotodama tooling with a less actionable missing-file failure. That weakened local release evidence because generated artifacts and generated interface docs could drift from `iroha.contracts.toml`.

Status: Fixed.

Changes:
- `scripts/dev_iroha.sh check` and `build` now reject missing contract sources before invoking Kotodama tooling.
- `scripts/dev_iroha.sh build` now emits CLI dev-smoke-compatible `.to`, `.manifest.json`, `.interface.json`, `.source-map.json`, and `.budget.json` outputs under `artifacts/compiled/`, verifies that set exactly matches manifest-declared contracts, and fails on missing or stale generated outputs.
- `scripts/dev_iroha.sh test` now rejects missing manifest test paths before invoking `koto_test`.
- `tests/public_env_helper_smoke.sh` now compares `docs/interface_specs/generated.md` headings and interface paths against the manifest and covers missing-source, missing-test-path, and stale compiled-artifact fixtures.

## Verification

Local gates run:
- `make lint`, `make compile`, `make check-shell-syntax`, and `make dev-test` passed on 2026-06-24; all Kotodama contracts linted, every top-level `scripts/*.sh` and `tests/*.sh` zsh wrapper parsed without diagnostics, top-level Python scripts/tests parsed through Python AST without bytecode writes, JavaScript tests, browser UI files, and root JS/CJS/MJS config files parsed with `node --check`, stale root deploy temp manifests were rejected, shell metadata proved `#!/bin/zsh` shebangs, `set -euo pipefail` on executable entrypoints, and no unsourced non-executable shell files, every Makefile shell-script command resolved to an executable file, every Makefile recipe target matched a `.PHONY` entry and every `.PHONY` entry matched a recipe, `lint` and `compile` resolved through the canonical wrapper scripts, literal release-script Make target references resolved to Makefile targets, production script-to-script shell references resolved to executable files, Makefile and production shell references to repo-owned Python/JS entrypoints resolved to real files, Makefile unittest discovery patterns resolved to matching test files, Makefile and package-script `npm run` references resolved to declared `package.json` scripts, dependency-backed package-script commands resolved to declared package dependencies, package-script JS/TS file references resolved to real files, `package-lock.json` root metadata matched `package.json`, `tsconfig.json`, Jest, and Playwright root config patterns resolved to real files, local UI HTML/CSS asset references resolved under `ui/`, local UI JavaScript literal DOM selectors such as `#id` and data-attribute selectors resolved against each app's `index.html`, concrete documented timestamped deployment evidence and telemetry artifact paths resolved to real files, repo-contained Markdown links resolved to real files or directories, fenced and inline repo-markdown `make` examples resolved to Makefile targets, repo-markdown `npm run` examples resolved to declared package scripts, repo-markdown `scripts/` and `tests/` shell/Python/JS path references resolved to real files, contracts compiled to ABI v1 artifacts, and the manifest-declared Kotodama regression suite passed. The compile wrapper also proved the generated artifact set is exactly 25 manifest contracts times `.to`, `.manifest.json`, `.interface.json`, `.source-map.json`, and `.budget.json`, with stale compiled outputs rejected as compiled artifact drift.
- `make dev-schema` passed on 2026-06-24.
- `make test-public-env-helpers`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, `make test-trader-ui`, `make simulate-build`, `make simulate-smoke`, and `make simulate-full` passed on 2026-06-24; coverage included generated interface doc drift checks, manifest missing-source/missing-test-path fixtures, stale compiled-artifact fixtures, 101 contract-console unittest cases, 11 contract-console Playwright tests, the real Python fixture-server integration test, 32 trader UI backend tests, 7 trader Playwright tests, TypeScript simulation typechecking, 1 simulation smoke case, and 8 full simulation suites with 47 tests. The three Playwright-backed make targets also passed when launched concurrently after fixture-backed suites were moved off the shared static UI server.
- `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` were rerun on 2026-06-25 after generated-evidence hygiene hardening and all passed. The post-simulation generated-evidence redaction pass inspected 2,007 deployment/telemetry JSON artifacts with 0 rewrites, `make check-shell-syntax` passed with the full generated-evidence path scan, and `make test-public-env-helpers` passed with coverage for the generated-evidence and public-signer `.gitignore` policy.
- Full simulation telemetry was refreshed at `artifacts/telemetry/defi_2026_primitives_latest.json` with `generated_at: "2026-07-06T02:43:56.316Z"` plus intent, n3x vault, solver-operator, hook-order, margin, and RWA evidence.
- `make test-local-isolated` passed again on 2026-06-27 after the migration-register static gate hardening. That historical retained local evidence pointed at `deployments/local/chain.20260627T162346Z.json`, `deployments/local/deploy.20260627T161240Z.json`, `deployments/local/soraswap.bundle.deploy.json` with `generated_at: "20260627T162346Z"`, `deployments/local/contracts.20260627T164747Z.json`, and `deployments/local/smoke.20260627T171505Z.json`. The chain fingerprint is local `http://127.0.0.1:18080`, chain `00000000-0000-0000-0000-000000000000`, block-1 hash `e057d45690f6995f1253036df65161ae54766119d0c3fc70c84bd45255ef4ae9`; smoke records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, 10 registered trigger IDs, and 3 active trigger IDs. Cleanup left no isolated local peer running.
- `make test-local-foundation-isolated` passed again on 2026-06-27 after scope-aware foundation snapshot hardening. It wrote `deployments/local/chain.20260627T123845Z.json`, `deployments/local/deploy.20260627T123631Z.json`, `deployments/local/contracts.20260627T124216Z.json`, and `deployments/local/smoke.20260627T124429Z.json`, while leaving the retained full `deploy.latest.json`, `contracts.latest.json`, and `smoke.latest.json` on the 25-contract full isolated run. The scoped `deployments/local/soraswap.foundation.bundle.deploy.json` has `generated_at: "20260627T123845Z"` and matches the five-contract foundation snapshot; smoke records `smoke_scope: "foundation"`, 92 transaction-hash slots, 79 view results, 27 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- Follow-up validation after the migration-register static gate and full isolated local evidence refresh passed: `git diff --check`, `zsh ./scripts/redact_generated_evidence.sh --check`, and `make check-shell-syntax`. The static gate checked 55 shell scripts, 26 Kotodama contract sources, 1,042 documented make-command targets, 98 documented timestamped evidence paths, and 2,142 generated JSON artifacts with 0 redaction drift; read-only release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- Follow-up validation also hardened aggregate deploy-receipt handling so `soraswap.bundle.deploy.json` and `soraswap.foundation.bundle.deploy.json` are never treated as per-contract records or deploy-nonce sources, even if a future platform receipt grows top-level contract-like fields. `make test-public-env-helpers`, `python3 -m unittest tests.test_contract_console`, `python3 -m unittest tests.test_trader_ui`, `make check-shell-syntax`, and `git diff --check` passed; the current static gate checks 1,042 documented make-command targets and 2,142 generated JSON artifacts with 0 redaction drift.
- Follow-up validation now also rejects unsuccessful or sensitive aggregate bundle receipts in mutation-enabled public UI startup checks. `soraswap.bundle.deploy.json` with `ok: false` or unredacted private-key/token-style diagnostics blocks both contract-console and trader-cockpit binding even if its timestamp, selected environment, chain fingerprint, and contract set match. The helper smoke also covers final release-checklist rejection for sensitive retained local bundle receipts and immediate phase-guard rejection for sensitive public bundle receipts. `python3 -m unittest tests.test_contract_console` passes 111 tests, `python3 -m unittest tests.test_trader_ui` passes 38 tests, and `make test-public-env-helpers` passes.
- Follow-up validation now rejects mutation-enabled public UI startup when preflight has present oracle key files but does not prove `oracle_keypair_verified: true`, blocking contract-console and trader-cockpit binding on mismatched or unverifiable oracle material. Focused validation passed on 2026-07-05: `python3 -m unittest tests.test_contract_console` (112 tests), `python3 -m unittest tests.test_trader_ui` (39 tests), and `python3 -m py_compile scripts/serve_contract_console.py scripts/serve_trader_ui.py`.
- Follow-up validation now applies the same verified-oracle preflight baseline to DEX-only and enabled-RWA compliance evidence. `record-testnet-rwa-compliance`, `record-production-rwa-compliance`, and their immediate release phase guards reject ready-looking preflight evidence unless it also proves mutation consent, native MCP HTTP `200`, signer/account/fee readiness, usable oracle key sources, and `oracle_keypair_verified: true`.
- Follow-up validation now extends that current release-ready preflight baseline to downstream release phases as well: deploy, RWA, readonly/signed smoke, contract-console, readonly/signed trader, and trader API publication cannot advance from the phase runner unless `preflight.latest.json` is ready, warning-free, matched to saved chain evidence, no older than the current supported nested-call probe, backed by verified oracle keypair evidence, and records latest/current/supported nested-call probe metadata. Focused validation passed on 2026-07-05: `make test-public-env-helpers`, `make check-shell-syntax`, `./scripts/redact_generated_evidence.sh --check`, and `git diff --check`.
- Follow-up validation now requires release-ready preflight evidence to record an explicit empty public write-health issue list plus JSON-ready `/status` and `/v1/sumeragi/status` health snapshots. Mutation-enabled preflight writes `endpoint.health_issues`, blocks non-JSON health endpoints, and downstream release gates, RWA evidence, producer snapshot checks, plus console/trader UI startup all reject ready-looking preflight evidence when that list is nonempty or the retained health snapshots are missing.
- Follow-up validation now also applies that baseline to producer-side public deploy/contracts snapshot checks and the final release checklist. Standalone smoke, contract-console, trader, and trader API producers degrade their `snapshot_check`, and `make release-checklist` rejects preflight before release artifacts, when preflight is ready-looking but missing verified oracle proof, saved-chain agreement, or actual current supported nested-call probe evidence. Focused validation passed on 2026-07-05: `make test-public-env-helpers`, `make check-shell-syntax`, `./scripts/redact_generated_evidence.sh --check`, and `git diff --check`.
- Follow-up validation hardened the static Markdown command scan so documented validation snippets cannot use multi-script `zsh -n` commands that parse only the first script operand. Historical snippets now point back to the authoritative `make check-shell-syntax` gate, and helper-smoke coverage pins the new `documented zsh no-exec` summary line. The make-level static gate, helper smoke, and whitespace check passed after this guard.
- Follow-up validation also moved migration-register production-row checks into the static gate. The static pass now rejects missing/empty parity ledgers, reference-only-only ledgers, and any production row that is still `stub`, `adapted`, `blocked`, blank, or typoed before release checklist evidence evaluation; the current register has 23 accepted rows.
- Follow-up validation now also rejects stale release-status docs before the public release checklist reaches target-chain evidence. `make check-shell-syntax` checks that the release audit and readiness checklist mention the current retained local chain/deploy/contracts/smoke `generated_at` values, the current `defi_2026_primitives_latest.json` telemetry timestamp, and the canonical primitive telemetry path; helper-smoke coverage rejects a ready-looking status doc that labels `perps_shared_risk_latest.json` as primitive telemetry.
- `make test-local-isolated` passed again on 2026-07-05 after the automation queue lifecycle hardening and sibling runtime lane-payload ownership fix. That run wrote `deployments/local/chain.20260705T121514Z.json`, `deployments/local/deploy.20260705T120540Z.json`, `deployments/local/contracts.20260705T123945Z.json`, and `deployments/local/smoke.20260705T125541Z.json`; the local fingerprint is chain `00000000-0000-0000-0000-000000000000` with block-1 hash `8fe0c85e46e1ac5e5026548fc6680ca884b046f6493aa463284826f8e7b338f7`. The smoke report records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 80 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- `make test-local-isolated` passed again on 2026-06-25 after aggregate bundle receipt hardening; retained local evidence now points at `deployments/local/deploy.20260625T094958Z.json`, `deployments/local/soraswap.bundle.deploy.json` with `generated_at: "20260625T095836Z"`, `deployments/local/contracts.20260625T102142Z.json`, and `deployments/local/smoke.20260625T104746Z.json`. The smoke report records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries, and the local bundle receipt matches the contract snapshot by key, address, deploy nonce, code hash, and ABI hash.
- The direct local operator flow was rerun on 2026-06-25 without public mutations: `make lint`, `make dev-test`, `make compile`, `make dev-schema`, `make dev-build`, `make dev-check`, `make local-up`, `make deploy-local`, `make dev-smoke`, `make smoke-local`, and `make local-down` all passed. The run wrote `deployments/local/deploy.20260625T171955Z.json`, `deployments/local/contracts.20260625T175012Z.json`, and `deployments/local/smoke.20260625T181707Z.json`; the smoke report records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 10 registered trigger entries.
- Focused release-guard validation was rerun on 2026-06-25 after snapshot-check hardening: `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, and `make redact-generated-evidence` passed. The redaction pass inspected 2,047 generated JSON artifacts with 0 rewrites; a transient host disk-pressure failure was resolved by clearing generated sibling build output before the passing reruns.
- Focused release-guard validation was rerun on 2026-06-26 after bridge status-kind hardening and generated interface refresh: `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, `make dev-schema`, `make dev-check`, `make compile`, `make test-contract-console`, `make test-contract-console-ui`, and `make test-trader-ui` passed. The current UI/backend acceptance counts are 106 contract-console Python tests, 11 contract-console Playwright tests, 35 trader UI Python tests, and 7 trader UI Playwright tests. The release docs and helper suite now describe exact contract-console bridge `status_kind` matching, including rejection of substring statuses such as `NotApplied`, and the generated interface spec includes the options oracle stale-slot admin/view surface for manager, factory, and shout option contracts.
- The direct local operator flow passed again on 2026-06-26 without public mutations after the isolated localnet acceptance batch: `make local-up`, `make deploy-local`, `make dev-build`, `make dev-smoke`, `make smoke-local`, and `make local-down` all completed. Fresh retained local evidence now points at `deployments/local/chain.20260626T053132Z.json`, `deployments/local/deploy.20260626T052315Z.json` with 25 deployed contracts over 6 phases, `deployments/local/contracts.20260626T055356Z.json` with 25 completed contract snapshots, and `deployments/local/smoke.20260626T062028Z.json` with `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries. `make local-down` stopped the local Nexus, and no SoraSwap localnet or smoke process remained. Follow-up `make redact-generated-evidence`, `git diff --check`, `make check-shell-syntax`, and `make test-public-env-helpers` passed, with redaction inspecting 2,088 generated JSON artifacts with 0 rewrites.
- Focused release-guard validation was rerun on 2026-06-26 after aligning compact embedded `changeme` / `replaceme` placeholder checks across shared public config/oracle validation, contract-console signer autodiscovery, the RWA recorder, final release checklist, and immediate phase guard. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed; `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe.
- `make test-local-foundation-isolated` passed on a fresh localnet after the full isolated pass; evidence included N3X mint/redeem, DLMM route swap/fee collection/remove-position, epoch auction native close, DLMM range governor tick state, and conditional escrow by-call trigger execution.
  - Deploy report: `deployments/local/deploy.20260624T033431Z.json`
  - Contract snapshot: `deployments/local/contracts.20260624T033955Z.json`
  - Smoke report: `deployments/local/smoke.20260624T034202Z.json`
  - Smoke contents: 92 transaction-hash slots, 79 view results, 27 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- `make test-local-isolated` reran the local acceptance bundle on a fresh full-scope localnet, covering the full deploy bundle plus active smoke paths across N3X, DLMM, launchpad/referral/farms/automation, risk/perps/options/cover, bridge, intents, vaults, operators, margin, RWA, DLMM hooks, epoch auction, and conditional escrow. The checklist now checks refreshed Taira evidence before repeating the expensive local acceptance bundle, so missing public artifacts fail fast until the mutating public gate is authorized.
  - Deploy report: `deployments/local/deploy.20260624T004623Z.json`
  - Contract snapshot: `deployments/local/contracts.20260624T011531Z.json`
  - Smoke report: `deployments/local/smoke.20260624T014031Z.json`
  - Smoke contents: 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- The direct local operator flow was also refreshed after aligning compiled artifacts with top-level CLI dev-smoke freshness checks.
  - Dev smoke: passed against the freshly compiled CLI-compatible artifacts.
  - Deploy report: `deployments/local/deploy.20260624T145222Z.json`
  - Contract snapshot: `deployments/local/contracts.20260624T152105Z.json`
  - Smoke report: `deployments/local/smoke.20260624T154810Z.json`
  - Smoke contents: 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.

Required before public release:
- Public target evidence: `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make release-taira` for Taira, or `SORASWAP_ALLOW_PRODUCTION_MUTATIONS=1 make release-production` for a production target.
- Saved Taira chain evidence from 2026-06-25 and 2026-06-27 is historical after the 2026-06-28 chain refresh below.
- The previous signed nested-call probe, `deployments/testnet/archive/20260625T112040Z-516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5/nested_call_probe.20260624T224644Z.json`, is archived historical evidence after the chain refresh. It produced `supported: false` because `/v1/contracts/deploy` returned `queue_unresolved_route` / `PRTRY:ROUTE_UNRESOLVED` for the first scratch probe contract, with height 9533, queue depth 149, count saturation true, age saturation unavailable, and view-change cause `quorum_timeout`.
- The local sibling `../iroha` router now fixes the reproduced universal-contract-deploy case by materializing a Native AMX plan when a universal deploy matches a non-universal `smartcontract::deploy` routing rule; the rollout verifier runs both `cargo test -p iroha_core queue::router::tests::smart_contract_deploy_rule --lib` and `cargo test -p iroha_core contract_call_transaction_preserves_three_hop_transfer_authorities --lib` before public probes. The current 2026-06-28 public Taira probe below proves that route and nested-transfer behavior on the refreshed chain.
- 2026-06-24 reran `../iroha/configs/soranexus/taira/verify_soraswap_rollout.sh --skip-mcp-check --skip-sorafs-check --skip-trader-app-api-check --skip-nested-call`; the local-only rollout verifier passed after `queue::router::tests::smart_contract_deploy_rule` passed 2 tests and `contract_call_transaction_preserves_three_hop_transfer_authorities` passed 1 test.
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 SORASWAP_SKIP_IROHA_CLI_BUILD=1 make taira-preflight` from 2026-06-25 is historical after the current 2026-07-06 ready preflight and current supported nested-call probe below.
- The repo now exposes `make refresh-testnet-chain` and `make refresh-production-chain` for read-only chain snapshot refresh. These targets archive stale generated evidence and update `chain.latest.json` without public mutation consent; nested-call probe evidence still requires the separate signed probe target. Missing untracked client configs now stop before network work with a repo-relative `next setup:` block, and 2026-06-26 hardening makes the read-only refresh targets reject wrong-environment configs, placeholder/local/wildcard/example endpoints or key material, Taira configs whose chain id does not match the expected Taira chain unless `SORASWAP_TESTNET_CHAIN_ID` pins an intentional public reset, and production configs that still carry Taira's canonical chain id before any fingerprint fetch or evidence write. Public Taira preflight now applies the same unexpected-chain blocker in its generated report, so a copied config for another public reset cannot produce ready-looking preflight evidence unless the operator pins `SORASWAP_TESTNET_CHAIN_ID`.
- 2026-06-29 reran `make release-checklist` and `make release-production-checklist` after the current deploy/contracts/readonly-smoke refresh. `make release-checklist` now stops first at `smoke.latest.json` missing signed mutating smoke transaction evidence; the production checklist fails that Taira prerequisite and also relays the retained blocked production setup summary.
- 2026-07-07 refreshed saved Taira chain evidence to `deployments/testnet/chain.20260707T001026Z.json`; `chain.latest.json` records `https://taira.sora.org`, chain `809574f5-fee7-5e69-bfcf-52451e42d50f`, and block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`.
- 2026-06-28 wrote current signed nested-call probe evidence at `deployments/testnet/nested_call_probe.20260628T122756Z.json` with `supported: true`: bytes state roundtrip, minimal `call_contract(...)`, and multi-hop nested `AssetOps` relay all completed against `xor#universal`.
- 2026-07-07 refreshed preflight to `deployments/testnet/preflight.20260707T001005Z.json`; it records the current block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`, the current supported nested-call probe `deployments/testnet/nested_call_probe.20260628T122756Z.json`, `status: "ready"`, and `endpoint.health_issues: []`. The public write-health evaluator now permits stale committed-block age only when queues are empty and QC is aligned, while still blocking stale age with queued/saturated writes, QC lag, pending finality, view-change, or zero committed blocks.
- 2026-07-06 refreshed current-chain DEX-only RWA evidence at `deployments/testnet/rwa_compliance.20260706T220307Z.json` with `status: "not_applicable"` and `rwa_release_enabled: false`.
- 2026-07-07 retained a failed Taira deploy attempt at `deployments/testnet/deploy.20260707T001026Z.json`: after a second operator-side volatile consensus quarantine recovered public finality to aligned QC and empty queues, preflight and compile completed again, but the deploy phase was interrupted after exposing follow-on direct-deploy tooling gaps: confirmation progress logs contaminated JSON artifact publication for newly applied contracts, and duplicate direct retries hit rejected alias/code-hash conflicts from the earlier partial deploy. `deploy.latest.json` is therefore not release-grade deploy evidence. The current contracts snapshot is `deployments/testnet/contracts.latest.json` (`generated_at: "20260706T222558Z"`) with `status: "completed"` and 25 contract snapshots, but deploy release evidence remains blocked until deploy, bootstrap, and deployment-record snapshot phases complete.
- 2026-06-29 refreshed `deployments/testnet/smoke.20260629T110845Z.json`, but it is not release-grade signed mutating smoke evidence because it has no signed mutation transaction hashes and no embedded `readonly_verification`.
- 2026-06-29 attempted `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet` twice after the shared public write-health guard passed. Both runs were interrupted before completion when Sumeragi write health degraded again while polling public transaction visibility: `tx_queue.depth: 1`, `saturated_by_age: true`, and `worker_stage: "idle"`.
- 2026-06-29 reran `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet` after three clean read-only health samples. The script preserved `deployments/testnet/smoke.latest.json` for signed mutating evidence, wrote the prerequisite readonly pass to `deployments/testnet/smoke.readonly.20260629T114909Z.json`, and auto-aborted with exit 75 when transaction `d346f803d73bd9cf581b78e6cc28bed59625a05ff96464aa0bbf3cccdd3f817f` saw age-only queue pressure at `oldest_queued_age_ms: 3497`. The local guard now uses `SORASWAP_PUBLIC_WRITE_HEALTH_AGE_MAX_MS=30000` plus a short public-health retry window, so transient runtime age-pressure or queue-depth flaps do not abort waits by themselves. A later read-only sampler still flapped, first blocking at queue size `22` / depth `16` before recovering, so no additional signed write was started.
- 2026-06-29 latest public ingress sampled HTTP `200` for `/status`, `/v1/sumeragi/status`, and `/v1/mcp`; Sumeragi recovered to `tx_queue.depth: 0` with matching committed/highest QC height `15401`, but `/status` was still non-JSON binary data. No signed public mutation retry was started from that state because the shared public write-health guard still requires JSON-readable public health.
- 2026-07-01 reran current-state checks after the Kotodama `transfer_asset(..., DataSpaceId)` update. `SORASWAP_SKIP_KOTO_TOOL_BUILD=1 make lint`, `SORASWAP_SKIP_KOTO_TOOL_BUILD=1 make compile`, `make test-public-env-helpers`, and `git diff --check` passed. `make release-checklist` still fails at missing signed mutating Taira smoke evidence, and `make release-production-checklist` still fails that Taira prerequisite while reporting the retained blocked production setup. Public Taira is JSON-readable on `/status`, `/v1/sumeragi/status`, and `/v1/mcp`, with an empty write queue, but the shared write-health probe still exits 75 because committed blocks are stale (`blocks: 21995`, `time_since_last_block_ms` about `57145969`, Sumeragi idle in `pending_finality`). Signed smoke, console, signed trader, and trader API evidence remain unsafe to regenerate until finality recovers.
- 2026-07-07 reran oracle/release-helper hardening validation after adding direct-validator and direct-port preflight diagnostics: `make test-public-env-helpers`, `make check-shell-syntax`, `./scripts/redact_generated_evidence.sh --check`, and `git diff --check` passed before the final deploy retry. `make smoke-testnet-readonly` still stops before live reads because `deployments/testnet/deploy.latest.json` is failed and no longer represents completed deploy/bootstrap/snapshot evidence. `make release-checklist` must still stop at the failed `deployments/testnet/deploy.20260707T001026Z.json` and stale downstream evidence, so signed public mutation phases must remain paused until deploy evidence completes against recovered Taira finality. The retained readonly smoke and readonly trader reports still reference the older 20260629 deploy/contracts pair with `generated_at` values `20260630T125317Z` and `20260629T131137Z`; signed smoke is retained at `20260629T110845Z`, while contract-console, signed trader, and trader API reports reference older 20260628 snapshots with retained `generated_at` values `20260629T004141Z`, `20260629T004421Z`, and `20260629T011348Z`.
- 2026-07-06 hardened public deploy failure retention so failed deploy attempts retain `deploy.failed.latest.json` and timestamped `deploy.failed.<generated_at>.json` diagnostics, while any failed `deploy.latest.json` remains diagnostic-only until a later completed deploy replaces it. This keeps failed phase detail available without satisfying release evidence.
- 2026-07-06 reran the local source, simulation, UI/backend, and browser batch without public mutations: `make lint`, `make compile`, `make dev-check`, `make dev-build`, `make dev-test`, `make dev-schema`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` passed. The run rebuilt ABI v1 artifacts for 25 contracts, refreshed `docs/interface_specs/generated.md`, refreshed primitive telemetry at `2026-07-06T02:43:56.316Z`, passed 8 simulation suites / 47 tests, 116 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 42 trader Python tests, and 7 trader Playwright tests.
- 2026-07-06 reran `SORASWAP_SKIP_LOCALNET_TOOL_BUILD=1 make test-local-isolated`. The isolated local deploy/bootstrap/smoke rehearsal completed and cleaned up its temporary peer. Fresh local evidence is `deployments/local/chain.20260706T030716Z.json`, `deployments/local/deploy.20260706T025740Z.json`, `deployments/local/contracts.20260706T033445Z.json`, and `deployments/local/smoke.20260706T035549Z.json`; the chain fingerprint is local `http://127.0.0.1:18080`, chain `00000000-0000-0000-0000-000000000000`, and block-1 hash `42b81957874a62362426dc07cedff4cffe8219936fca938af080dc6a88c4c7a9`. Deploy covers 25 contracts over 6 phases, contracts covers 25 completed snapshots, and full smoke records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 80 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- 2026-07-05 reran the local source, simulation, UI/backend, and browser batch without public mutations: `make lint`, `make compile`, `make dev-check`, `make dev-build`, `make dev-test`, `make dev-schema`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` passed. The run rebuilt ABI v1 artifacts for 25 contracts, refreshed `docs/interface_specs/generated.md`, refreshed primitive telemetry at `2026-07-05T23:51:42.036Z`, passed 8 simulation suites / 47 tests, 112 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 39 trader Python tests, and 7 trader Playwright tests.
- 2026-07-05 reran `SORASWAP_SKIP_LOCALNET_TOOL_BUILD=1 make test-local-isolated` after lifecycle timing hardening. The isolated local deploy/bootstrap/smoke rehearsal completed and cleaned up the temporary peer. That historical retained local evidence pointed at `deployments/local/chain.20260705T225243Z.json`, `deployments/local/deploy.20260705T224249Z.json`, `deployments/local/contracts.20260705T232110Z.json`, and `deployments/local/smoke.20260705T234131Z.json`; the chain fingerprint is local `http://127.0.0.1:18080`, chain `00000000-0000-0000-0000-000000000000`, and block-1 hash `c9aa7b74233d5e0abb979edf1509c47b24019a49f9b365e354facc8733d8fc33`. Deploy covers 25 contracts over 6 phases, contracts covers 25 completed snapshots, and full smoke records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 80 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries. Current primitive telemetry is `artifacts/telemetry/defi_2026_primitives_latest.json` with `generated_at: "2026-07-06T02:43:56.316Z"`. Follow-up `./scripts/redact_generated_evidence.sh --check` and `git diff --check` passed, with redaction inspecting 2,643 JSON artifacts. `make release-checklist` and `make release-production-checklist` still fail before any release claim because Taira deploy evidence is failed after renewed public finality/write-health degradation; the production checklist also reports the retained missing production config, chain, mutation consent, oracle key, and production nested-call evidence blockers.
- 2026-06-27 reran focused validation for the refresh/docs update: `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, and `zsh ./scripts/redact_generated_evidence.sh --check` passed. The static gate checked 55 shell scripts, 26 Kotodama contract sources, 1,010 documented make-command targets, 74 documented timestamped evidence paths, and 2,106 generated JSON artifacts with 0 redaction drift. This still does not replace the required signed Taira nested-call probe or any production evidence.
- 2026-06-27 reran the current local source/UI/simulation batch without public mutations: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` passed. The run rebuilt ABI v1 artifacts for 25 contracts, refreshed `docs/interface_specs/generated.md`, refreshed primitive telemetry at `2026-06-27T08:06:49.191Z`, passed 8 simulation suites / 47 tests, 108 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 36 trader Python tests, and 7 trader Playwright tests. This still does not replace the required signed Taira nested-call probe or any production evidence.
- 2026-06-26 reran local source, dev-wrapper, simulation, UI/backend, and helper verification after the RWA/preflight hardening: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, `make test-trader-ui`, `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence` passed. The full simulation suite passed 8 suites / 47 tests, the UI/backend suites passed 106 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 35 trader Python tests, and 7 trader Playwright tests, and the generated-evidence redaction pass inspected 2,061 JSON artifacts with 0 rewrites; this does not replace production evidence.
- 2026-06-26 reran the direct local operator path without public mutations after the isolated localnet acceptance gate: `make local-up`, `make deploy-local`, `make dev-build`, `make dev-smoke`, `make smoke-local`, and `make local-down` completed. Fresh local evidence is `deployments/local/chain.20260626T053132Z.json`, `deployments/local/deploy.20260626T052315Z.json`, `deployments/local/contracts.20260626T055356Z.json`, and `deployments/local/smoke.20260626T062028Z.json`; deploy/contracts cover 25 contracts and smoke records 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries. Follow-up `make redact-generated-evidence`, `git diff --check`, `make check-shell-syntax`, and `make test-public-env-helpers` passed, and the redaction pass inspected 2,088 JSON artifacts with 0 rewrites. `make release-checklist` and `make release-production-checklist` were rerun and still expected-fail at the missing current-chain signed Taira nested-call probe; this still does not replace the missing signed public probe or production evidence.
- 2026-06-26 hardened the shared release phase guard for saved chain artifacts so direct `chain.latest.json` field checks require non-empty `generated_at`, `torii_url`, `chain`, and `block_1_hash`. The shared current-or-saved chain fingerprint resolver now applies the same timestamp rule before saved chain evidence can feed deployment snapshots or downstream evidence checks. The direct helper regression now rejects untimestamped saved-chain artifacts. `make test-public-env-helpers`, `make check-shell-syntax`, `make lint`, `make compile`, `make dev-schema`, `make dev-test`, `make dev-check`, `make dev-build`, `git diff --check`, and `make redact-generated-evidence` passed after the guard change; redaction inspected 2,068 generated JSON artifacts with 0 rewrites. Release and production checklists still stop at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 tightened immediate release phase recovery hints for nested-call probe artifacts. Wrong-environment or untimestamped `nested_call_probe.latest.json` now directs operators to refresh signed probe evidence, while the sibling-runtime rollout verifier hint is reserved for current selected-environment probe evidence that proves unsupported nested-call behavior. `make test-public-env-helpers` passed with a wrong-environment unsupported-probe regression.
- 2026-06-26 tightened final release-checklist blocked-preflight hints to use the same current-probe test before printing nested-call health or runtime-rollout guidance. Wrong-environment, untimestamped, or stale-chain `nested_call_probe.latest.json` artifacts now point to the signed probe refresh command instead of the sibling-runtime rollout verifier. Stale saved-chain blockers keep `make refresh-testnet-chain` / `make refresh-production-chain` as the first recovery step and suppress duplicate standalone probe/runtime hints. The status-doc freshness gate also treats non-current probe files as absent, so stale probe timestamps no longer satisfy current Taira documentation evidence. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed with the wrong-environment checklist regression. `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe and print the signed probe refresh command without a runtime-rollout hint.
- 2026-06-26 tightened preflight evidence generation to use the same current-probe rule before copying nested-call `summary` or Sumeragi health into `preflight.latest.json`. Wrong-environment, untimestamped, or stale-chain probe artifacts now remain blockers without becoming preflight status evidence. `make test-public-env-helpers` passed with the wrong-environment preflight regression.
- 2026-06-26 tightened shared nested-call probe reuse so runtime support checks require non-empty `generated_at`, selected-environment provenance when supplied, and a full current chain fingerprint match before cached `supported: true` probe evidence can satisfy later public mutating paths. `make test-public-env-helpers` passed with timestamp and wrong-environment helper regressions.
- 2026-06-26 tightened shared deploy-record reuse so nonce planning, deployment skip/reuse, public smoke/catalog liveness checks, and the live alias/code verifier used by readonly smoke plus trader API publication require non-empty `generated_at`, selected-environment provenance when supplied, and a full current chain fingerprint match before using cached per-contract deploy records. `make test-public-env-helpers` passed with timestamp and wrong-environment deploy-record regressions.
- 2026-06-26 tightened producer-side public snapshot checks so completed `snapshot_check` evidence now requires `contracts.latest.json` to cover the current Kotodama contract set exactly once, and `deploy.latest.json` to carry completed preflight, compile, nested-call-probe, deploy, bootstrap, and deployment-record snapshot phases, signer-readiness proof without debug bypass, and a pointer to the current `contracts.<generated_at>.json` snapshot. Public smoke, console, trader, and trader API reports now degrade when the contracts snapshot is partial/extra/duplicate or the deploy report has a matching snapshot path but any required deploy phase failed or degraded, immediate release phase guards reject duplicate aggregate contract snapshots before advancing, and mutation-enabled console/trader startup refuses duplicate `contracts.latest.json` evidence. `make test-public-env-helpers` passed with partial-contract-snapshot, duplicate-contract-snapshot, degraded-preflight, and degraded-deployment-snapshot regressions.
- 2026-06-26 tightened deployment evidence production so release-grade `local`, `testnet`, and `production` deployment snapshots, bundle materialization, alias recovery, and legacy single-contract deploy records require a complete current-or-saved chain fingerprint before writing evidence. Public bundle submission now rejects missing chain provenance before invoking the deploy helper, while local aggregate bundle receipts remain allowed to omit `chain_fingerprint`. Deployment snapshots now reject per-contract records whose chain fingerprint does not match the snapshot chain, and the local release gate requires every `contracts.latest.json` contract entry to carry the snapshot chain fingerprint. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed after the hardening.
- 2026-06-26 tightened full public release runner startup checks so `make release-taira` and `make release-production` validate external RWA reference env vars for missing, placeholder, local/wildcard endpoint, reserved-domain, and control-character values before any numbered release phase can run when `SORASWAP_ENABLE_RWA_RELEASE=1`; production performs the same check before its isolated Taira prerequisite in that mode.
- Retained production preflight evidence is not release evidence yet: `deployments/production/preflight.latest.json` was refreshed at `20260708T183136Z` with `status: "blocked"`, no production chain fingerprint, no saved production `chain.latest.json`, and `nested_call_probe.latest_exists: false`; oracle keypair verification is unavailable because no production oracle key material is configured. The recorded blockers are the missing real `config/production/production.client.toml`, unavailable production Torii root and chain id, missing production `chain.latest.json` with the next read-only step `make refresh-production-chain`, unreachable placeholder native MCP URL, missing production mutation consent, missing oracle key material, and missing production nested-call probe evidence (`SORASWAP_ALLOW_PRODUCTION_MUTATIONS=1 make production-nested-call-probe`); the report also records the expected warning that production chain fingerprint assembly is unavailable until a production chain id or client config is present.
- 2026-06-26 reran focused local/source and generated-evidence hygiene after the production preflight refresh: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make check-shell-syntax`, `make test-public-env-helpers`, `python3 -m unittest tests.test_contract_console`, `git diff --check`, and `make redact-generated-evidence` passed. The contract-console unit suite ran 108 Python tests, redaction inspected 2,089 generated JSON artifacts with 0 rewrites, and both release checklists still expected-fail at the missing current-chain signed Taira nested-call probe before production artifact validation can advance.
- 2026-07-07 tightened `make release-production-checklist` failure diagnostics further: after the Taira prerequisite is green, a missing production `chain.latest.json` now also prints the retained production preflight status, generated timestamp, blockers, warnings, and health issues. This is diagnostic only and does not turn retained blocked production preflight into release evidence.
- 2026-06-26 tightened RWA recorder setup diagnostics the same way: blocked or warning-bearing preflight artifacts now print status, target environment, generated timestamp, blockers, warnings, and health issues before the required refresh/probe/preflight/reference order. This is diagnostic only; blocked production preflight still cannot produce `rwa_compliance.latest.json`.
- 2026-06-26 extended the retained production preflight diagnostic to full `make release-production` setup failures. Missing config, production mutation consent, oracle key material, or enabled RWA references still stop before the isolated Taira prerequisite and before any production phase; the extra preflight summary only exposes the existing blocker list.
- 2026-06-26 extended the same startup diagnostic to `make release-taira`: setup failures now print the retained Taira preflight status, generated timestamp, blockers, warnings, and health issues when available, while still stopping before any signed release phase.
- 2026-06-26 routed final release-checklist blocked-preflight nested-call probe and health summaries, unsupported nested-probe summaries, nested-call probe stage outputs, `taira-preflight` copied probe summaries, public deploy blocked-probe summaries, trader blocked reasons, contract-console bridge helper output/proof-skip reasons, smoke negative-test unexpected-commit output, shared deploy confirmation-failure output, permission-grant failures, trigger-execute failures, alias-resolution failures, compiler failures, local perps open diagnostics, perps bootstrap init fallback output, Taira state-repair free-form operator inputs, and optional RWA compliance notes through the shared redactor, closing the remaining unredacted retained-summary paths found in release startup diagnostics. The final checklist and immediate phase guards also reject stale or hand-edited RWA compliance artifacts whose optional notes would change under that redactor; immediate phase guards reject metadata-bearing phase artifacts with unredacted sensitive diagnostics, and the final checklist rejects otherwise passing target-chain, per-contract, bundle, or local evidence if the shared redactor would rewrite it. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; both release checklists still stop at the missing current-chain signed Taira nested-call probe.
- 2026-06-27 tightened preflight evidence capture itself: `make taira-preflight` and `make production-preflight` now redact blocked-preflight blockers, warnings, and diagnostic endpoint fields before stdout/report persistence, including URL userinfo in Torii roots. The helper smoke covers credentialed Torii URL fixtures and rejects raw URL secrets in both command output and `preflight.latest.json`.
- 2026-06-26 tightened contract-console public bridge smoke diagnostics so replay rejection reasons and unexpected proof/message submit responses are explicitly redacted before they are printed or retained in `contract_console_smoke.latest.json`. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; both release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 tightened public stale snapshot diagnostics in readonly smoke, mutating smoke, trader smoke, and contract-console smoke so `snapshot_check.output` is routed through the shared redactor before stderr printing. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. Both release checklists remain blocked on the missing current-chain signed Taira nested-call probe.
- 2026-06-26 tightened local and public mutating smoke negative-test rejection evidence so expected rejection output is redacted before it is assigned into `smoke.latest.json.rejection_evidence`. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. Both release checklists remain blocked on the missing current-chain signed Taira nested-call probe.
- 2026-06-26 expanded the shared redaction and browser sensitive-key vocabulary to cover OAuth-style `access_token`, `refresh_token`, and `client_secret` names in JSON keys, free-form diagnostics, CLI-style flags, phase-guard fallback scans, contract-console requests, trader requests, and access logs. focused contract-console/trader UI redaction unit tests, `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. Both release checklists remain blocked on the missing current-chain signed Taira nested-call probe.
- 2026-06-27 expanded the shared redactor and browser access-log sanitizer to cover credentials embedded in URL userinfo plus sensitive URL query or fragment params such as `access_token`, `refresh_token`, `client_secret`, `api_key`, `authorization`, `token`, and `password`, while leaving non-sensitive URL params intact. The phase-guard fallback redactor and sensitive-diagnostic scanner use the same URL rules so hand-edited artifacts with unredacted URL secrets are rejected before release advancement; direct helper coverage now rejects raw URL-secret preflight evidence while accepting already-redacted URL evidence. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after this coverage hardening; redaction inspected 2,089 generated JSON artifacts with 0 rewrites.
- 2026-06-27 added `./scripts/redact_generated_evidence.sh --check` and wired that check-only redaction scan into `make check-shell-syntax`, so generated deployment and telemetry JSON must already be stable under the shared sensitive-data/runtime-path redactor before static acceptance passes. The helper smoke covers dirty check-mode rejection, clean check-mode success, and the static-check success summary. `./scripts/redact_generated_evidence.sh --check`, `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, and `git diff --check` passed; both the check-only and rewrite scans inspected 2,089 JSON artifacts with 0 pending changes or rewrites. Read-only `make release-checklist` and `make release-production-checklist` still expected-fail before any release claim at the missing current-chain signed Taira nested-call probe, with the production checklist also relaying the retained blocked production setup summary.
- 2026-06-27 reconciled the DLMM permissionless surface with release docs: `init_pool(...)` is documented as a generic asset-agnostic instantiation path, while the production SoraSwap deployment remains anchored on `xor#universal` and helper/NFT LP wrappers stay outside the v1 release surface. The helper smoke now pins the README, state layout, and migration-register wording. `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, and `git diff --check` passed after this guardrail update; redaction inspected 2,089 JSON artifacts with 0 rewrites.
- 2026-06-27 hardened `make check-shell-syntax` with a contract-source hygiene scan so accidental non-Kotodama files under `contracts/` fail static acceptance, except the existing shared helper README. The dev check/build/test/schema path, canonical lint/compile wrappers, final release checklist, and full Taira/production release runners now run the same source hygiene guard before contract tooling, public preflight, production-prerequisite, config setup, or live phase checks can advance. The helper smoke now includes `contracts/dlmm/permissionless_pool.md` regressions for the static gate, dev check/build/test/schema, lint/compile wrappers, final checklist, and full release runners. `make test-public-env-helpers`, `make check-shell-syntax`, `make dev-check`, `make dev-schema`, `make dev-test`, `make dev-build`, `make redact-generated-evidence`, and `git diff --check` passed after this guardrail; `make check-shell-syntax` reported 26 contract files checked and both redaction scans inspected 2,089 generated JSON artifacts with 0 changes. Read-only `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe, with the production checklist also relaying the retained blocked production setup.
- 2026-06-27 tightened the Taira state-repair plan workflow so sensitive free-form reason, snapshot-policy, and operator notes are exercised through the existing environment-variable inputs rather than CLI flags in smoke coverage, avoiding process-list exposure while preserving report redaction. The helper usage and operator runbook now warn operators to keep sensitive incident notes out of command-line arguments. a focused env-input repair-plan check, `make test-public-env-helpers`, and `git diff --check` passed after the change.
- 2026-06-26 moved final release-checklist sensitive-diagnostic scanning earlier for `chain.latest.json`, `preflight.latest.json`, and any retained `nested_call_probe.latest.json`. Dirty blocked-preflight or stale-probe evidence now fails before the checklist prints redacted blocker, health, runtime, or status-doc diagnostics, while clean blocked evidence still reaches the signed-probe recovery hints. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed; redaction inspected 2,089 generated JSON artifacts with 0 rewrites, and both release checklists still stop at the missing current-chain signed Taira nested-call probe.
- 2026-06-25 refreshed local acceptance after the latest release-path hardening: console/trader Python and Playwright suites, simulation build/smoke/full suites, shell/static checks, public env helper smoke, whitespace checks, and generated-evidence redaction all passed; the redaction pass inspected 2,016 generated JSON artifacts with 0 rewrites. These runs do not change the public release blocker above.
- 2026-06-25 reran the focused release-evidence hygiene checks after the read-only chain-refresh hint hardening: `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence` passed; the redaction pass inspected 2,039 generated JSON artifacts with 0 rewrites. These runs do not change the public release blocker above.
- 2026-06-25 reran `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` after hardening the refresh-target missing-config diagnostics. The real `make refresh-production-chain` now fails with setup instructions because no untracked production client config exists.
- 2026-06-25 reran `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 SORASWAP_SKIP_IROHA_CLI_BUILD=1 make taira-preflight`, `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` after strict CLI-helper skip hardening and preflight hint filtering; the redaction pass inspected 2,042 generated JSON artifacts with 0 rewrites. That pass left release-checklist, production-checklist, and RWA-recording commands failing at the missing current-chain signed nested-call probe without advancing public release evidence.
- 2026-06-25 centralized supported nested-call probe enforcement in the immediate release phase guard, so preflight, RWA, nested-probe, and mutating-smoke paths share the same current-chain and `supported: true` checks plus recovery hints that distinguish stale saved-chain evidence, missing/stale probe evidence, and unsupported current Taira probe artifacts. `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` passed; `make release-checklist` still stops at the missing current-chain signed nested-call probe.
- 2026-06-26 tightened `make record-testnet-rwa-compliance` so completed RWA evidence cannot be written from a ready-looking preflight that lacks an actual matching saved `chain.latest.json` artifact or current supported nested-call probe evidence. Manual `SORASWAP_RWA_COMPLIANCE_CHAIN_JSON` and chain-only inputs now have to match the ready preflight plus saved chain evidence instead of bypassing them. The live command still expected-fails at the missing current-chain signed probe and does not write release RWA evidence.
- 2026-06-25 also reran `make test-local-isolated` after tightening the local smoke writer and final checklist; the full local deploy/bootstrap/smoke rehearsal completed with `deployments/local/deploy.20260625T070345Z.json`, `deployments/local/contracts.20260625T073317Z.json`, and `deployments/local/smoke.20260625T075815Z.json`, then cleaned up the isolated peer and smoke process. The refreshed local smoke evidence carries top-level `status: "completed"` and `environment: "local"` with 92 transaction-hash slots, and the checklist now rejects stale local contracts/smoke timestamps or deploy snapshot details that do not point at the current contracts artifact. The same deploy snapshot-to-contracts timestamp agreement is now enforced for public release evidence by both the final checklist and the per-phase release guard, and any aggregate `soraswap.bundle.deploy.json` receipt present in public evidence must be successful, timestamped, selected-environment-scoped, path-clean, free of unredacted sensitive diagnostics, matched to `chain.latest.json`, and matched to the current contracts snapshot.
- Current-chain Taira release-checklist blockers now center on signed mutating smoke: retained `preflight.latest.json` is ready at `20260709T005125Z`, deploy/contracts/console/trader/API evidence are refreshed against the current chain, and the nested-call probe remains supported, but `smoke.latest.json` at `20260709T020326Z` is missing the required DLMM collect-fees transaction hash. The repaired signed-smoke retry retained diagnostic-only failed evidence at `20260709T033600Z` after public finality/write health degraded with queued writes during transaction visibility polling. Future failed signed smoke attempts retain diagnostic-only `smoke.failed.*.json` sidecars, future failed contract-console attempts retain diagnostic-only `contract_console_smoke.failed.*.json` sidecars, and future failed signed trader attempts retain `mutation.submitted_calls` plus `public_write_health` in `trader.latest.json`; these diagnostics preserve submitted-call traces and health samples but do not satisfy the release gate. Completed external RWA references are required only for an explicit RWA market launch.

## Production Readiness Notes

- Local source is hardened for the audited findings, but public production readiness depends on the deployment evidence for the exact target chain and contract addresses.
- Keep the oracle freshness setting explicit in bootstrap evidence. The default is `4`; override with `SORASWAP_OPTIONS_ORACLE_STALE_SLOTS` only when the oracle cadence and chain finality assumptions justify it.
- Treat any future ABI v1 limitation in `call_contract(...)` or test-host behavior as a tooling blocker and patch the narrowest `../iroha` layer, as done for `expect_reject_as`.
