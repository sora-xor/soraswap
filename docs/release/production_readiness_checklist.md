# Production Readiness Checklist

This checklist is the repo-wide release gate for SoraSwap. Taira is the canonical signed pre-production environment.

## Current Taira Evidence Baseline
- Chain evidence `generated_at: "20260709T135055Z"` records `https://taira.sora.org`, chain `809574f5-fee7-5e69-bfcf-52451e42d50f`, and block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`.
- Preflight evidence `generated_at: "20260709T135317Z"` is `status: "blocked"` because the latest committed block is stale and the Sumeragi worker is idle while the transaction queue is non-empty. Signed release phases remain prohibited until validator convergence and write finality recover.
- Nested-call probe evidence `generated_at: "20260708T133317Z"` remains current-chain `supported: true`, covering state bytes roundtrip, minimal nested `call_contract(...)`, and multi-hop nested AssetOps relay.
- Deploy evidence `generated_at: "20260709T072723Z"` remains `status: "running"`; retained contracts evidence at `generated_at: "20260709T010900Z"` covers 25 contracts but is not a coherent release snapshot until a fresh six-phase deploy completes.
- DEX-only RWA evidence `generated_at: "20260709T062847Z"` is `status: "not_applicable"` with `rwa_release_enabled: false`; refresh it after the next ready preflight.
- Readonly smoke evidence `generated_at: "20260709T032707Z"` is completed against the current deploy/contracts snapshots. The retained signed `smoke.latest.json` at `generated_at: "20260709T020326Z"` is completed but still not release-grade because it is missing the required DLMM collect-fees mutation hash; the latest signed retry retained diagnostic-only failed evidence at `generated_at: "20260709T033600Z"` after public finality/write health degraded while waiting for an options oracle configuration transaction.
- Contract-console evidence `generated_at: "20260709T025646Z"`, readonly trader evidence `generated_at: "20260709T025955Z"`, signed trader evidence `generated_at: "20260709T030104Z"`, and trader API evidence `generated_at: "20260709T030233Z"` are completed against the current deploy/contracts snapshots.
- Public preflight commands now default `SORASWAP_SKIP_IROHA_CLI_BUILD=1` when unset, so Taira/production status checks do not start a sibling `cargo build` unless an operator explicitly sets `SORASWAP_SKIP_IROHA_CLI_BUILD=0`.
- Production cutover is still separate and additionally waits on a ready current Taira preflight: production config, chain snapshot, mutation consent, signer/oracle custody, production nested-call evidence, and production release evidence must be completed before any production claim.
- Taira and production require `SORASWAP_RELEASE_EXPECTED_GIT_SHA` for a signed SoraSwap RC plus the signed exact-Iroha-candidate pins before phase 1 and again around every phase. Each exact artifact path is snapshotted before dispatch and must be replaced with a new file identity, hash, bytes, and fresh `generated_at`; phase receipts must match the fixed 11-phase mapping, one chain fingerprint, and one deploy/contracts snapshot. Atomic mode-`0600` journals/checkpoints reject unsafe paths, duplicate/missing receipts, symlinks, hard links, wrong modes, stale artifacts, source drift, and cross-environment concurrent closeout. The checkpoint also binds exhaustive bundle/archive provenance and staged/worktree status-doc equality. Only the explicit resume targets may complete after staged status-doc-only updates, redaction/static gates, `git diff --cached --check`, `git diff --check`, and `git diff HEAD --check`; resume never repeats signed mutations or local E2E, and production revalidates Taira.

## Current Local Evidence Baseline
- Local chain evidence `generated_at: "20260708T173108Z"`, deploy evidence `generated_at: "20260708T172811Z"`, contracts evidence `generated_at: "20260708T173745Z"`, and full smoke evidence `generated_at: "20260708T174402Z"` are retained from the 2026-07-08 full isolated local acceptance rerun.
- A later local chain/deploy diagnostic pair has `generated_at: "20260710T073509Z"`; its deploy report is `status: "failed"` after stopping during `bootstrap_assets`, so it is not completed local or production evidence and does not replace the July 8 acceptance baseline.
- The 2026-07-11 exact-candidate attempt retained diagnostic-only chain evidence at `generated_at: "20260711T040919Z"` and failed deploy evidence at `generated_at: "20260711T040444Z"`; it completed all 25 deploy chunks, then stopped during contract-state bootstrap when a hardcoded debug Kagami path ignored the explicit candidate release binary. That path is fixed and covered by helper regressions. The current completed contracts and smoke records remain `generated_at: "20260708T173745Z"` and `generated_at: "20260708T174402Z"` until the full isolated rerun succeeds.
- Current primitive telemetry at `artifacts/telemetry/defi_2026_primitives_latest.json` has `generated_at: "2026-07-11T04:01:38.538Z"`.

## Required Local Acceptance
- `make lint`
- `make compile`
- `make dev-check`
- `make dev-build`
- `make check-shell-syntax`
- `make dev-test`
- `make dev-schema`
- `make test-public-env-helpers`
- `make test-contract-console`
- `make test-contract-console-ui`
- `make test-contract-console-integration`
- `make test-trader-ui`
- `make simulate-build`
- `make simulate-smoke`
- `make simulate-full`
- `make test-local-isolated`
- `make test-local-foundation-isolated`

Latest local evidence:
- 2026-06-24 reran `make lint`, `make compile`, and `make dev-test`; all Kotodama contracts linted, compiled to ABI v1 artifacts, and the manifest-declared Kotodama regression suite passed. The manifest wrapper also rejects missing contract sources, missing test paths, and compiled artifact drift under `artifacts/compiled/`.
- 2026-06-24 reran `make dev-schema`, `make test-public-env-helpers`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, `make test-trader-ui`, `make simulate-build`, `make simulate-smoke`, and `make simulate-full`; the local evidence includes generated interface doc drift checks, 101 contract-console unittest cases, 11 contract-console Playwright tests, the real Python fixture-server integration test, 32 trader UI backend tests, 7 trader Playwright tests, TypeScript simulation typechecking, 1 simulation smoke case, and 8 full simulation suites with 47 tests.
- 2026-06-24 aligned the Makefile `lint` and `compile` targets with the canonical repo entrypoints, so `make lint` dry-runs through `scripts/lint_contracts.sh` and `make compile` dry-runs through `scripts/compile_contracts.sh` while preserving the existing `dev-check` and `dev-build` aliases. The wrapper scripts resolve their default manifest from the repo root, so direct absolute invocation does not depend on the caller's working directory.
- 2026-06-24 added `make check-shell-syntax` as the release-gate shell and repo-script parser entrypoint. It walks every top-level `scripts/*.sh` and `tests/*.sh` zsh wrapper and treats parser diagnostics as failures, avoiding ineffective multi-script `zsh -n` patterns where zsh only parses the first script path. It also parse-checks top-level Python scripts/tests through Python AST parsing without writing bytecode, parse-checks JavaScript tests, browser UI files, and root JS/CJS/MJS config files with `node --check`, rejects stale root deploy temp manifests left by interrupted foundation or chunked app deploys, rejects non-Kotodama files under `contracts/` except the shared helper README, validates the migration register has at least one `ported` production row and no non-ported production rows, verifies `#!/bin/zsh` shebangs plus `set -euo pipefail` on executable entrypoints and limits non-executable shell files to sourced helper libraries, verifies every Makefile shell-script command references an existing executable file, requires every Makefile recipe target to be listed in `.PHONY` and every `.PHONY` entry to have a recipe, enforces the canonical `lint`/`compile` wrapper entrypoints, validates literal release-script Make target references such as `run_target ...`, `make -C ...`, and `local_acceptance_targets`, checks production script-to-script shell references for missing or non-executable command targets while allowing sourced helper libraries to remain non-executable, checks Makefile and production shell references to repo-owned Python/JS entrypoints, validates Makefile unittest discovery patterns, validates Makefile and package-script `npm run` references against `package.json` scripts, validates dependency-backed package-script commands, validates package-script JS/TS file references, validates `package-lock.json` root metadata against `package.json` when the lockfile exists, validates `tsconfig.json`, Jest, and Playwright root config patterns against real files, validates actual `.gitignore` behavior for generated deployment evidence, tracked `.gitkeep` placeholders, ignored public signer configs, and visible `*.toml.example` templates, validates local UI HTML/CSS asset references under `ui/`, validates local UI JavaScript literal DOM selectors such as `#id` and data-attribute selectors against each app's `index.html`, validates concrete documented timestamped deployment evidence and telemetry artifact paths against real files, checks release-status docs mention the current retained local chain/deploy/contracts/smoke `generated_at` values plus the current `defi_2026_primitives_latest.json` telemetry timestamp and canonical path, scans all generated deployment and telemetry JSON under `deployments/` and `artifacts/telemetry/` for raw local path diagnostics and fails if the shared sensitive-data/runtime-path redactor would change any retained JSON artifact, validates repo-contained Markdown links against real files or directories, validates fenced and inline repo-markdown `make` examples against the Makefile target set, validates repo-markdown `npm run` examples against `package.json` scripts, rejects repo-markdown `zsh -n` examples that name multiple shell-script operands, and rejects stale repo-markdown `scripts/` or `tests/` path references for shell, Python, and JS files.
- 2026-06-24 reran `make lint compile dev-test dev-schema` after the public shell hardening; lint, ABI v1 compile, Kotodama regression tests, and generated interface schema completed successfully.
- `artifacts/telemetry/defi_2026_primitives_latest.json` was refreshed with `generated_at: "2026-07-06T02:43:56.316Z"` plus intent, n3x vault, solver-operator, hook-order, margin, and RWA evidence.
- 2026-07-08 reran the full `make test-local-isolated` path after adding deterministic local smoke pausing for the native perps lifecycle tick. The run completed deploy, contract-state bootstrap, deployment-record snapshot, full smoke, and cleanup against a fresh isolated localnet; no `tmp/iroha-localnet-verify` peer remained afterward.
- Current retained full isolated evidence is `deployments/local/chain.20260708T173108Z.json`, `deployments/local/deploy.20260708T172811Z.json`, `deployments/local/contracts.20260708T173745Z.json`, and `deployments/local/smoke.20260708T174402Z.json`. The local chain fingerprint records `http://127.0.0.1:18083`, chain `00000000-0000-0000-0000-000000000000`, and block-1 hash `5a68fe281896328a6ec2177ac77fe908451b4e0a8c3ded36df957ffc99123755`; deploy covers 25 contracts over 6 phases and contracts covers 25 completed snapshots.
- The current local smoke report records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 80 view results, 83 decoded state integers, 5 rejection evidence entries, 10 registered trigger IDs, and 9 active trigger IDs. It paused `soraswap_range_governor_tick`, `soraswap_twamm_tick`, and `soraswap_perps_lifecycle_tick` during deterministic smoke execution, then restored them before writing completed evidence.
- 2026-06-27 reran `make test-local-foundation-isolated` after making deployment-record snapshots scope-aware and protecting retained full local evidence from foundation-only runs. It wrote `deployments/local/chain.20260627T123845Z.json`, `deployments/local/deploy.20260627T123631Z.json`, `deployments/local/contracts.20260627T124216Z.json`, and `deployments/local/smoke.20260627T124429Z.json`, plus foundation-scoped `chain.foundation.latest.json`, `deploy.foundation.latest.json`, `contracts.foundation.latest.json`, `smoke.foundation.latest.json`, and `soraswap.foundation.bundle.deploy.json` copies; `deploy.latest.json`, `contracts.latest.json`, and `smoke.latest.json` still point at the 25-contract full isolated run above.
- Current foundation-only evidence covers `n3x.n3x_hub`, `dlmm.dlmm_pool`, `dlmm.dlmm_router`, `batch_amm.epoch_auction`, and `escrow.conditional_escrow`; the chain fingerprint records block-1 hash `539b19fdf2af7d0f1e3e33ed39125bb9c5ffeeb114afc1fc9639713fcfcfdedb`, the foundation bundle has `generated_at: "20260627T123845Z"` and matches the five-contract snapshot by key, address, nonce, code hash, and ABI hash, and the smoke report records `smoke_scope: "foundation"`, 92 transaction-hash slots, 79 view results, 27 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- 2026-06-27 hardened shared deploy-record scans so aggregate receipts such as `soraswap.bundle.deploy.json` and `soraswap.foundation.bundle.deploy.json` are never counted as per-contract records or deploy-nonce sources, even if a future platform receipt grows top-level contract-like fields. The contract-console and trader Python suites also cover contract-shaped foundation sidecars in catalog/evidence loading. `make test-public-env-helpers`, `python3 -m unittest tests.test_contract_console`, `python3 -m unittest tests.test_trader_ui`, `make check-shell-syntax`, and `git diff --check` passed; read-only `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe and retained production setup blockers.
- 2026-06-27 added explicit aggregate bundle failure regressions for public UI startup. A timestamped, selected-environment-scoped, chain-matching `soraswap.bundle.deploy.json` with `ok: false` or unredacted private-key/token-style diagnostics now blocks both mutation-enabled contract-console and trader-cockpit binding. The helper smoke also covers final release-checklist rejection for sensitive retained local bundle receipts and immediate phase-guard rejection for sensitive public bundle receipts. `python3 -m unittest tests.test_contract_console` passes 111 tests, `python3 -m unittest tests.test_trader_ui` passes 38 tests, `make test-public-env-helpers` passes, `make check-shell-syntax` passes, and `git diff --check` is clean.
- 2026-07-05 hardened mutation-enabled public UI startup so a ready preflight with present oracle key files is still rejected unless `oracle_keypair_verified: true`; this blocks contract-console and trader-cockpit binding on mismatched or unverifiable oracle material. Focused validation passed: `python3 -m unittest tests.test_contract_console` (112 tests), `python3 -m unittest tests.test_trader_ui` (39 tests), and `python3 -m py_compile scripts/serve_contract_console.py scripts/serve_trader_ui.py`.
- 2026-06-27 hardened `make check-shell-syntax` so repo Markdown command snippets cannot reintroduce multi-script `zsh -n` examples that only parse the first script operand. Stale historical validation snippets were collapsed back to the authoritative `make check-shell-syntax` gate, and the helper smoke now pins the new static-check summary line. The make-level static gate, helper smoke, and whitespace check passed after this guard.
- 2026-06-27 moved migration-register production-row validation into the static gate as well as the final release checklist. The gate now rejects missing/empty parity ledgers, reference-only-only ledgers, and any production row that is still `stub`, `adapted`, `blocked`, blank, or typoed before later release checks run; the current register has 23 accepted rows.
- 2026-06-25 reran the full `make test-local-isolated` path; the run completed deploy, contract-state bootstrap, deployment-record snapshot, full smoke, and cleanup against the current sibling `../iroha` runtime.
- Full isolated deploy report: `deployments/local/deploy.20260625T094958Z.json`; aggregate bundle receipt: `deployments/local/soraswap.bundle.deploy.json` with `generated_at: "20260625T095836Z"`; contract snapshot: `deployments/local/contracts.20260625T102142Z.json`; smoke report: `deployments/local/smoke.20260625T104746Z.json`.
- Full isolated evidence records 25 deployed contract snapshots, a path-clean, sensitive-diagnostic-clean timestamped local bundle receipt matching the contract snapshot by key/address/nonce/code hash/ABI hash, and smoke evidence with top-level `status: "completed"` plus `environment: "local"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- 2026-06-25 reran the direct local operator flow without public mutations: `make lint`, `make dev-test`, `make compile`, `make dev-schema`, `make dev-build`, `make dev-check`, `make local-up`, `make deploy-local`, `make dev-smoke`, `make smoke-local`, and `make local-down` all passed. The run wrote `deployments/local/deploy.20260625T171955Z.json`, `deployments/local/contracts.20260625T175012Z.json`, and `deployments/local/smoke.20260625T181707Z.json`; the smoke report records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 10 registered trigger entries.
- 2026-06-25 reran focused local release-guard validation after snapshot-check hardening: `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, and `make redact-generated-evidence` passed. The redaction pass inspected 2,047 generated JSON artifacts with 0 rewrites; the only transient failure was host disk pressure from generated sibling build output, which was cleared before the passing reruns.
- 2026-06-26 reran focused release-guard validation after bridge status-kind hardening and generated interface refresh: `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, `make dev-schema`, `make dev-check`, `make compile`, `make test-contract-console`, `make test-contract-console-ui`, and `make test-trader-ui` passed. The current UI/backend acceptance counts are 106 contract-console Python tests, 11 contract-console Playwright tests, 35 trader UI Python tests, and 7 trader UI Playwright tests. The helper suite now pins exact contract-console bridge `status_kind` matching, including rejection of substring statuses such as `NotApplied`, and generated interface docs include the options oracle stale-slot admin/view entrypoints for manager, factory, and shout option contracts.
- 2026-07-06 reran the local source, simulation, UI/backend, and browser batch without public mutations: `make lint`, `make compile`, `make dev-check`, `make dev-build`, `make dev-test`, `make dev-schema`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` passed. The run rebuilt ABI v1 artifacts for 25 contracts, refreshed `docs/interface_specs/generated.md`, refreshed primitive telemetry at `2026-07-06T02:43:56.316Z`, passed 8 simulation suites / 47 tests, 116 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 42 trader Python tests, and 7 trader Playwright tests.
- 2026-07-06 reran `SORASWAP_SKIP_LOCALNET_TOOL_BUILD=1 make test-local-isolated`. Fresh retained local evidence now points at `deployments/local/chain.20260706T030716Z.json`, `deployments/local/deploy.20260706T025740Z.json`, `deployments/local/contracts.20260706T033445Z.json`, and `deployments/local/smoke.20260706T035549Z.json`; the local fingerprint records `http://127.0.0.1:18080`, chain `00000000-0000-0000-0000-000000000000`, and block-1 hash `42b81957874a62362426dc07cedff4cffe8219936fca938af080dc6a88c4c7a9`. The smoke report records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 80 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- 2026-06-27 reran the current local source/UI/simulation batch without public mutations: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` passed. The run rebuilt ABI v1 artifacts for 25 contracts, refreshed `docs/interface_specs/generated.md`, refreshed primitive telemetry at `2026-06-27T08:06:49.191Z`, passed 8 simulation suites / 47 tests, 108 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 36 trader Python tests, and 7 trader Playwright tests.
- 2026-06-24 reran the direct local operator flow after aligning `make compile` with CLI dev-smoke freshness checks: `make dev-smoke`, `make deploy-local`, and `make smoke-local` all passed against a fresh local Nexus. The refreshed local reports are `deployments/local/deploy.20260624T145222Z.json`, `deployments/local/contracts.20260624T152105Z.json`, and `deployments/local/smoke.20260624T154810Z.json`; the smoke report records 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries.
- Historical 2026-06-24 foundation evidence is retained at `deployments/local/deploy.20260624T033431Z.json`, `deployments/local/contracts.20260624T033955Z.json`, and `deployments/local/smoke.20260624T034202Z.json`.
- An earlier 2026-06-22 full `make test-local-isolated` refresh reached `deployments/local/deploy.20260622T141803Z.json` but was interrupted after local permissioned consensus stalled at height 18 with `inflight commit exceeded timeout`; that partial report is historical only, and the isolated wrapper now bounds major steps with `SORASWAP_ISOLATED_*_TIMEOUT_SECS`.
- This completed local pass depends on the sibling `../iroha` Torii contract-bundle alias activation wait and localnet runtime selected by the wrapper. It proves the local release path with that runtime, but public Taira and production still require current target-chain release evidence before any production claim.

Latest Taira preflight evidence:
- 2026-06-21 stale April Taira evidence was archived to `deployments/testnet/archive/20260621T173506Z-8c02c14d9aad9ee0e9b3bc566ef0e1247cc562767e024dced764ff46423b2b5b`.
- 2026-07-07 refreshed saved Taira chain evidence to `deployments/testnet/chain.20260707T001026Z.json`; `chain.latest.json` records `https://taira.sora.org`, chain `809574f5-fee7-5e69-bfcf-52451e42d50f`, and block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`.
- 2026-06-28 wrote current signed nested-call probe evidence at `deployments/testnet/nested_call_probe.20260628T122756Z.json` with `supported: true`: bytes state roundtrip, minimal `call_contract(...)`, and multi-hop nested `AssetOps` relay all completed against `xor#universal`.
- 2026-07-07 refreshed preflight to `deployments/testnet/preflight.20260707T001005Z.json`; it records `status: "ready"`, current-chain supported nested probe evidence from `deployments/testnet/nested_call_probe.20260628T122756Z.json`, the current block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`, and `endpoint.health_issues: []`.
- 2026-07-06 refreshed current-chain DEX-only RWA evidence at `deployments/testnet/rwa_compliance.20260706T220307Z.json` with `status: "not_applicable"` and `rwa_release_enabled: false`; completed external RWA references remain required for an enabled RWA launch.
- 2026-07-07 retained a failed Taira deploy attempt at `deployments/testnet/deploy.20260707T001026Z.json`: after a second operator-side volatile consensus quarantine recovered public finality to aligned QC and empty queues, preflight and compile completed again, but the deploy phase was interrupted after exposing follow-on direct-deploy tooling gaps: confirmation progress logs contaminated JSON artifact publication for newly applied contracts, and duplicate direct retries hit rejected alias/code-hash conflicts from the earlier partial deploy. `deploy.latest.json` is not release-grade deploy evidence. The current contracts snapshot is `deployments/testnet/contracts.latest.json` (`generated_at: "20260706T222558Z"`) with `status: "completed"` and 25 contract snapshots, but downstream readonly smoke/trader evidence still references the older completed `deployments/testnet/deploy.20260629T062733Z.json` / `contracts.20260629T065658Z.json` pair and must be refreshed after deploy evidence is completed again.
- 2026-06-29 refreshed `deployments/testnet/smoke.20260629T110845Z.json`, but it is not release-grade signed mutating smoke evidence because it has no signed mutation transaction hashes and no embedded `readonly_verification`.
- 2026-06-29 attempted `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet` twice after the shared public write-health guard passed. Both runs were interrupted before completion when Sumeragi write health degraded again while polling transaction visibility: `tx_queue.depth: 1`, `saturated_by_age: true`, and `worker_stage: "idle"`.
- 2026-06-29 reran `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet` after three clean read-only public health samples. The mutating smoke kept its prerequisite readonly report isolated in `deployments/testnet/smoke.readonly.20260629T114909Z.json` / `smoke.readonly.latest.json`, left `deployments/testnet/smoke.latest.json` unchanged, and then auto-aborted with exit 75 when transaction `d346f803d73bd9cf581b78e6cc28bed59625a05ff96464aa0bbf3cccdd3f817f` saw age-only public queue pressure at `oldest_queued_age_ms: 3497`. The local guard now treats age-only pressure as stale only at or above `SORASWAP_PUBLIC_WRITE_HEALTH_AGE_MAX_MS=30000` and confirms degraded public-health samples through `SORASWAP_PUBLIC_WRITE_HEALTH_RETRY_COUNT` / `SORASWAP_PUBLIC_WRITE_HEALTH_RETRY_DELAY_SECS`, matching the sibling rollout checker's interpretation while avoiding immediate aborts on transient queue flaps.
- 2026-06-29 reran read-only Taira rollout checks after the guard tuning: `../iroha/configs/soranexus/taira/check_mcp_rollout.sh --skip-local --public-root https://taira.sora.org --skip-write-canary` passed, and three public health samples were clean before the next signed smoke.
- 2026-06-30 latest retained readonly smoke is `deployments/testnet/smoke.readonly.latest.json` (`generated_at: "20260630T125317Z"`); it completed against `contracts.20260629T065658Z.json` plus `deploy.20260629T062733Z.json`, not the current `contracts.latest.json` (`generated_at: "20260706T222558Z"`) / failed `deploy.20260707T001026Z.json` evidence. The last signed smoke attempt had submitted transaction `29356c270110f431f603987a835a05970bbfc6881eb6b532be9cd0906a4fc5d7` and stopped with exit 75 after sustained public write-health degradation: `oldest_queued_age_ms: 53399`, `tx_queue.depth: 1`, `saturated_by_age: true`, and `worker_stage: "idle"`. `smoke.latest.json` remained unchanged and still lacks release-grade signed mutating evidence.
- 2026-06-29 hardened failed signed smoke diagnostics so future incomplete mutating runs write diagnostic-only `deployments/testnet/smoke.failed.latest.json` plus timestamped `smoke.failed.<timestamp>.json` sidecars with submitted contract-call traces and the latest public write-health sample. These failed sidecars preserve root-cause evidence but do not satisfy `make release-checklist`.
- 2026-06-29 hardened failed contract-console diagnostics so future incomplete `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make test-contract-console-testnet` runs write diagnostic-only `deployments/testnet/contract_console_smoke.failed.latest.json` plus timestamped sidecars after any setup, proof, or bridge-message transaction is submitted. These failed sidecars preserve submitted-call traces, proof/message submission state, and public write-health samples but do not satisfy `make release-checklist`.
- 2026-06-29 hardened failed signed trader diagnostics so future incomplete `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet-trader` runs keep submitted `route_swap` call traces and the latest public write-health sample inside `deployments/testnet/trader.latest.json`; a non-completed trader report remains release-blocking.
- 2026-06-29 latest retained non-mutating trader route evidence is `deployments/testnet/trader_readonly.20260629T131137Z.json`; it has `status: "completed"`, `snapshot_check.status: "completed"`, 11 required route probes, and no missing required routes, but it still references `contracts.20260629T065658Z.json` / `deploy.20260629T062733Z.json` and must be refreshed after the current deploy/contracts evidence set is complete.
- 2026-06-29 latest read-only public samples returned HTTP `200` for `https://taira.sora.org/status`, `/v1/sumeragi/status`, and `/v1/mcp`, so public ingress had recovered from the earlier `502`. `/v1/sumeragi/status` recovered to `height: 15401`, `commit_qc_height: 15401`, `highest_qc_height: 15401`, `tx_queue.depth: 0`, `oldest_queued_age_ms: 0`, and `saturated_by_age: false`, but `/status` still returned non-JSON binary data. No signed smoke retry was started because the shared public write-health guard requires JSON-readable public health plus stable finality/write health before contract-source or payload debugging can be meaningful.
- 2026-07-01 reran the release gates and a read-only public health sample. `make release-checklist` still stops first because `deployments/testnet/smoke.latest.json` lacks signed mutating smoke transaction evidence, and `make release-production-checklist` still fails that Taira prerequisite before relaying the retained blocked production setup summary. `https://taira.sora.org/status`, `/v1/sumeragi/status`, and `/v1/mcp` are JSON-readable with HTTP `200`, and the write queue is clear (`queue_size: 0`, `tx_queue.depth: 0`, `saturated_by_age: false`), but public finality remains unsafe for signed mutations: `/status` reports `blocks: 21995`, `time_since_last_block_ms: 57145969`, and Sumeragi is idle in `pending_finality`; the shared write-health probe exited 75 at `time_since_last_block_ms: 57113352`. No signed smoke, console, trader, or trader API mutation should run until committed blocks advance and the write-health guard stays green for the whole write run.
- Earlier on 2026-06-29, a post-tuning read-only sample still blocked at queue size `22`, `tx_queue_depth: 16`, `saturated_by_age: true`, and `worker_stage: "drain_rbc_chunks"` before the next two samples recovered to queue depth `0`; that flaky window was not used for signed smoke.
- `make refresh-testnet-chain` was run on 2026-06-25 after live Taira reported a new block-1 hash. It archived stale generated testnet evidence to `deployments/testnet/archive/20260625T112040Z-516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5` and wrote `deployments/testnet/chain.20260625T112041Z.json`; `chain.latest.json` now records chain `809574f5-fee7-5e69-bfcf-52451e42d50f` with block-1 hash `516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5`.
- 2026-06-27 read-only chain evidence, the blocked `deployments/testnet/preflight.20260625T143703Z.json` report, and earlier 2026-06-28 blocked-preflight/unsupported-probe artifacts are now historical and superseded by the current 2026-07-09 Taira baseline and supported probe above.
- The previous `deployments/testnet/archive/20260625T112040Z-516802f7faa58ebd78e33f39a39d59e8352bce8969dbcbbb956959507efd5ab5/nested_call_probe.20260624T224644Z.json` is historical after that archive. It recorded `supported: false`; the first scratch contract deploy returned `queue_unresolved_route` / `PRTRY:ROUTE_UNRESOLVED`, with height 9533, queue depth 149, count saturation true, age saturation unavailable, and view-change cause `quorum_timeout`.
- Local `../iroha` now carries a router fix for this class of deploy-route failure: universal contract deployments matched by a non-universal `smartcontract::deploy` routing rule build a Native AMX plan with universal as coordinator and the policy dataspace as participant. The fix is validated by `cargo test -p iroha_core queue::router::tests::smart_contract_deploy_rule --lib`, and the same rollout verifier also runs `cargo test -p iroha_core contract_call_transaction_preserves_three_hop_transfer_authorities --lib` so the nested `AssetOps` transfer path stays covered before public probes. Current Taira evidence proves the route and nested transfer behavior on the refreshed chain.
- 2026-06-25 reran the local-only sibling rollout verifier with MCP, SoraFS, trader app-api, and nested public probes skipped. It passed both router deploy-rule regressions and the three-hop nested-transfer authority regression, so the remaining nested-call blocker is public Taira ingress/runtime/transaction-visibility evidence, not local contract-source behavior.
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 SORASWAP_SKIP_IROHA_CLI_BUILD=1 make taira-preflight` from 2026-06-25 is historical after the current 2026-07-06 ready preflight and current supported nested-call probe.
- 2026-06-25 added explicit read-only chain snapshot refresh targets. `make refresh-testnet-chain` and `make refresh-production-chain` fetch the live block-1 fingerprint, archive stale local generated evidence under `deployments/<env>/archive/<utc>-<block1-hash>/`, and write fresh `chain.latest.json` plus timestamped chain evidence without requiring public mutation consent. They intentionally do not refresh the signed nested-call probe, and now print a `next setup:` block with the example copy path, `SORASWAP_CLIENT_CONFIG`, and rerun target when the required untracked client config is missing.
- 2026-06-26 hardened read-only chain refresh config validation so `make refresh-testnet-chain` and `make refresh-production-chain` reject wrong-environment configs, placeholder/local/wildcard/example endpoints or key material, Taira configs whose chain id does not match the expected Taira chain unless `SORASWAP_TESTNET_CHAIN_ID` pins an intentional public reset, and production configs that still carry Taira's canonical chain id before any fingerprint fetch or evidence write. Public Taira preflight now applies the same unexpected-chain blocker in its generated report, so a copied config for another public reset cannot produce ready-looking preflight evidence unless the operator pins `SORASWAP_TESTNET_CHAIN_ID`.
- 2026-06-28 refreshed the current-chain Taira nested-call probe and preflight, then recorded DEX-only RWA compliance evidence as `not_applicable` with `SORASWAP_ENABLE_RWA_RELEASE=0`. `make release-checklist` now clears the RWA gate and stops first at missing `deployments/testnet/deploy.latest.json`; the production checklist fails that Taira prerequisite and also relays the retained blocked production setup summary.
- 2026-06-27 reran focused local acceptance for the refresh/docs update: `git diff --check`, `make check-shell-syntax`, `make test-public-env-helpers`, and `zsh ./scripts/redact_generated_evidence.sh --check` passed. The static gate checked 55 shell scripts, 26 Kotodama contract sources, 1,010 documented make-command targets, 74 documented timestamped evidence paths, and 2,106 generated JSON artifacts with 0 redaction drift.
- 2026-06-27 reran focused validation after the migration-register static gate and full isolated local evidence refresh: `git diff --check`, `zsh ./scripts/redact_generated_evidence.sh --check`, and `make check-shell-syntax` passed. The static gate checked 55 shell scripts, 26 Kotodama contract sources, 1,042 documented make-command targets, 98 documented timestamped evidence paths, and 2,142 generated JSON artifacts with 0 redaction drift. Read-only `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe; the production checklist also relays the retained blocked production setup summary.
- 2026-06-24 hardened public rollout scripts so release/debug toggles fail fast on malformed values: deploy/init/bootstrap controls, signer-ready bypasses, preflight nested-probe skip controls, contract-console standalone bridge-proof assertions, and readonly bootstrap-state assertions all require their documented `0`/`1` or `auto` domains before config or network work starts. Exported internal release-checklist prerequisite flags and the private prerequisite token are rejected outright; the production prerequisite uses the script's tokenized private internal argument after env scrub instead of trusting operator env.
- 2026-06-25 hardened nested release `make` dispatch so `make release-checklist`, `make release-taira`, `make release-production`, and the production Taira prerequisite clear `MAKEFLAGS`, `MFLAGS`, `GNUMAKEFLAGS`, `MAKEFILES`, and `MAKEOVERRIDES` before recursive targets, and the full release phase runners also clear generic `CHAIN`, `ACCOUNT_CHAIN_DISCRIMINANT`, and `IROHA_ACCOUNT_CHAIN_DISCRIMINANT` before each phase target. This prevents stale account derivation, dry-run flags, injected makefiles, or inherited command-line overrides from making local acceptance or phase dispatch look green without executing the target. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this dispatch hardening.
- 2026-06-24 hardened public config parsing so `chain`, `torii_url`, `public_key`, and `private_key` string keys are recognized with normal TOML whitespace, including single-quoted literal strings, keeping the production release and public-wrapper Taira-chain blockers plus signer/oracle derivation effective for whitespace-padded client configs. Single-quoted placeholder config values are rejected as templates, commented template placeholders left in copied config files are ignored, and bracketed setup-hint oracle override placeholders are rejected before release runners proceed. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this config-parsing hardening.
- 2026-06-24 aligned generated CLI-compatible configs and bootstrap temporary signer configs with the shared config readers, so `SORASWAP_TORII_URL` overrides and `[account]`-scoped domains/keys carry into both `iroha_cli --config` materialization and contract-subject signer calls, with flat keys retained only as a legacy fallback. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this bootstrap config hardening.
- 2026-06-25 isolated the production Taira prerequisite checks in both the full runner and standalone production checklist by clearing every inherited `SORASWAP_*` variable plus generic chain/account-discriminator variables before passing the explicit testnet release selector to the inner checklist. The prerequisite remains a checklist-only read gate even when the operator shell already has production credentials, public endpoint overrides, alternate tooling, local-coverage knobs, RWA/trader publication controls, mutation consent, or stale account-discriminator env exported. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this prerequisite isolation.
- 2026-06-24 aligned the final checklist with the phase guards for trader and perps evidence: trader route probes must carry account-scoped query parameters, signed swap evidence must use numeric amount/slippage fields and prove the expected XOR-to-USDT direction, trader API CID probes must report numeric attempt/success/manifest-match counts, and perps liquidation counters must be numeric positive values. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this gate hardening.
- 2026-06-24 hardened public console smoke helpers so `SORASWAP_CONTRACT_CONSOLE_PORT` must be a valid TCP port, local contract-console smoke HTTP calls use `SORASWAP_CONTRACT_CONSOLE_HTTP_TIMEOUT_SECS`, and the opt-in live SCCP smoke uses the shared `SORASWAP_TORII_READ_MAX_TIME_SECS` bound. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this timeout hardening.
- 2026-06-24 hardened direct browser-app entrypoints so `make contract-console CONTRACT_CONSOLE_ARGS="--port ..."` and `make trader-ui TRADER_UI_ARGS="--port ..."` reject non-TCP ports before server bind. `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` pass after this parser hardening.
- 2026-06-24 isolated Playwright static-server startup so only the static contract-console browser suite binds the shared UI server while fixture-backed contract-console integration and trader browser suites set `SORASWAP_PLAYWRIGHT_STATIC_SERVER=0` with distinct test ports. Malformed Playwright static-server flags and ports now fail before browser startup. `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` pass when launched concurrently after this test-harness hardening.
- 2026-06-24 replaced shell temp files in shared/public paths with explicit `${TMPDIR:-/tmp}/...XXXXXX` templates, including generated signer configs, contract-console smoke logs, trader public response bodies, Koto build logs, and public onboarding/faucet/SNS response bodies, and tightened local/public smoke cleanup for trigger-completion capture files. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this portability hardening.
- 2026-06-24 moved release evidence publication for deploy reports, preflight, nested-call probes, RWA compliance, deployment snapshots, smoke, contract-console, trader, trader API, and Taira repair-plan reports through a shared same-directory temp-file plus `mv` JSON writer, so interrupted runs cannot leave malformed `*.latest.json` artifacts. 2026-07-06 tightened public deploy failure retention so failed deploy attempts write `deploy.failed.latest.json` plus `deploy.failed.<generated_at>.json`; any failed `deploy.latest.json` remains diagnostic-only until a later completed deploy replaces it. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this artifact publication hardening.
- 2026-06-24 extended the same atomic JSON publication path to platform bundle receipts, materialized per-contract deploy records, and adjacent deployment manifests, and added cleanup for chunked app-deploy subset manifests when subset generation fails. `make check-shell-syntax` and `git diff --check` pass after this deploy-evidence hardening.
- 2026-06-24 tightened the shared atomic JSON writer and deploy-report updater so failed same-directory publishes also remove their temporary JSON files. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this cleanup hardening.
- 2026-06-24 hardened `make taira-state-repair-plan` so donor/target storage paths must exist, duplicate targets are rejected after path normalization, height and state-root inputs must have production-shaped values, supplied trace/status sidecar paths must exist, status sidecars must parse as JSON, and empty snapshot directories are reported as present instead of conflated with missing snapshot directories. `make test-public-env-helpers` passes after this operator-evidence hardening.
- 2026-07-08 hardened the Taira repair-plan volatile-quarantine surface for the one-block-ahead finality stall: optional dist/runtime/SHA inputs now verify the executable runtime digest before evidence publication, explicit Torii-port overrides opt into the volatile repair input set, ports are normalized to decimal before duplicate checks, queue/RBC transient candidates are inventoried for state-repair mode, volatile-only plans can now omit donor/target storage and record `repair_mode: "volatile_consensus_quarantine"` without rsync/copy actions, platform-scoped operator action templates default to Darwin for the current Taira host, and read-only probe templates are separated from mutating recovery actions. `make test-public-env-helpers`, `make check-shell-syntax`, `./scripts/redact_generated_evidence.sh --check`, and `git diff --check` pass after this operator-recovery evidence hardening; read-only `make release-checklist` still fails at current Taira preflight/write-health and therefore blocks public release evidence refresh.
- 2026-07-08 tightened public preflight fingerprint evidence so `chain.fingerprint_available` is true only when the live Torii root, non-empty chain id, and block-1 hash are all present. Production preflight now records missing production chain id as an unavailable fingerprint warning instead of emitting a partial chain fingerprint from a reachable placeholder or overridden Torii endpoint.
- 2026-06-24 hardened public mutating smoke numeric controls so malformed amount, bps, slot, scan-limit, pool-bin, fee, deadline, and claim-slot overrides fail before signed public contract calls. `make test-public-env-helpers` passes after this scenario-input hardening.
- 2026-06-24 hardened the release checklist migration-ledger guard so `docs/parity/migration_register.md` is a required nonempty artifact and at least one production row must be `ported`; a missing, reference-only-only, or blank-status register can no longer support a release claim. `make test-public-env-helpers` passes after this gate hardening.
- 2026-06-24 aligned the mutation-enabled contract-console and trader startup gates with the release migration-ledger guard, so signed public UI sessions refuse to bind when the ledger is missing, empty, lacks a `ported` production row, or still contains non-reference production rows outside `ported`. `python3 -m unittest tests.test_contract_console` and `python3 -m unittest tests.test_trader_ui` pass after this evidence-gate hardening.
- 2026-06-24 aligned the shared release phase guard with the migration-ledger rule, so `release-taira` and `release-production` reject each known phase artifact before advancing when the ledger is missing, empty, reference-only-only, has blank status cells, or contains non-ported production rows. `make test-public-env-helpers` passes after this phase-gate hardening.
- 2026-06-24 hardened shared post-deploy phase guards so smoke, contract-console, trader, and trader API evidence must reference current timestamped `contracts.latest.json` and `deploy.latest.json` snapshots whose top-level status is `completed`; matching degraded or untimestamped aggregate snapshots no longer let a phase advance. 2026-06-25 added producer-side snapshot checks so readonly smoke, signed smoke, contract-console smoke, trader smoke, and trader API publication cannot stamp completed evidence, or continue into signed write paths, when deploy snapshot evidence is missing or stale. `make test-public-env-helpers` passes after this snapshot-gate hardening.
- 2026-07-05 extended the immediate phase guards so deploy, RWA, smoke, contract-console, trader, and trader API phases all require a current release-ready `preflight.latest.json` baseline, including mutation consent, native MCP HTTP `200`, signer/account/fee readiness, usable oracle key sources, `oracle_keypair_verified: true`, matching saved chain evidence, current supported nested-call probe evidence, and preflight metadata with `nested_call_probe.latest_exists`, `matches_current_chain`, and `supported` all true. `make test-public-env-helpers`, `make check-shell-syntax`, `./scripts/redact_generated_evidence.sh --check`, and `git diff --check` pass after this downstream preflight-gate hardening.
- 2026-07-05 aligned the producer-side public deploy/contracts snapshot check and final release checklist with that same baseline, so standalone readonly smoke, signed smoke, contract-console, trader, trader API producers, and `make release-checklist` cannot stamp or accept completed evidence unless the selected environment has release-ready preflight evidence plus an actual current supported nested-call probe artifact. The helper smoke now covers a false `oracle_keypair_verified` preflight and missing/stale probe evidence as degraded snapshot or checklist failures.
- Follow-up validation now requires release-ready preflight evidence to record `endpoint.health_issues: []` plus JSON-ready `/status` and `/v1/sumeragi/status` health snapshots. Preflight blocks on non-JSON health endpoints and other shared public write-health issues, while phase guards, RWA evidence, producer-side snapshot checks, mutation-enabled UI startup, and `make release-checklist` reject ready-looking preflight reports with recorded health issues or missing health snapshots. Focused coverage includes non-JSON status preflight, forged-ready checklist/RWA/phase-guard fixtures, and console/trader UI negative tests.
- 2026-06-24 aligned the contract-console and trader evidence readers with the release aggregate-snapshot gate, so public signed UI startup now rejects `contracts.latest.json` unless it records `status: "completed"` and read-only catalogs ignore incomplete aggregate snapshots. `make test-contract-console` and `make test-trader-ui` pass after this snapshot-status hardening.
- 2026-06-24 aligned the public UI startup gate with the release deploy-report phase guard, so mutation-enabled contract-console and trader sessions now reject `deploy.latest.json` when a required deploy phase is missing/incomplete or the preflight signer-ready check was bypassed. `make test-contract-console` passes after this deploy-evidence hardening.
- 2026-06-24 aligned the contract-console/trader evidence chain matcher with the release Torii provenance rule, so copied public evidence from a different endpoint is rejected even when chain id and block-1 hash match. `make test-contract-console` passes after this fingerprint hardening.
- 2026-06-24 aligned mutation-enabled contract-console and trader startup with the release preflight/nested-call prerequisite, so signed public UI sessions now require current ready `preflight.latest.json` evidence and a supported `nested_call_probe.latest.json` before binding. `make test-contract-console` and `make test-trader-ui` pass after this readiness-gate hardening.
- 2026-06-24 tightened that UI prerequisite so `preflight.latest.json` must itself record that the latest nested-call probe exists, matches the current chain, and is supported; a separately valid probe file no longer masks stale preflight acceptance. `make test-contract-console` and `make test-trader-ui` pass after this readiness-gate hardening.
- 2026-06-24 aligned public UI signer auto-discovery with the shell release gates, so default testnet/production signer configs with missing, local, example, `.test`, `.invalid`, `.localhost`, or reserved `example.com/.org/.net` Torii endpoints, or missing/placeholder signer key material, are not auto-bound, while explicit public signer configs may omit Torii and rely on deployment evidence, or ignore reserved Torii URLs with a warning. The Python UI signer reader now also mirrors the shell `[account]`-scoped key precedence with flat-key legacy fallback. `python3 -m unittest tests.test_contract_console` and `python3 -m unittest tests.test_trader_ui` pass after this signer-template hardening.
- 2026-06-24 hardened RWA compliance evidence so recorder input, existing-artifact reuse, release checklist validation, and phase guards all reject external reference strings with embedded control characters in addition to placeholders and reserved domains.
- 2026-06-24 tightened the RWA release phase guard so RWA evidence must match `chain.latest.json`, the current ready preflight must still match that saved chain, and the current nested-call probe must be present and supported before the runner can advance.
- 2026-06-24 hardened shell release helper error handling so upstream call/deploy/view, bootstrap/SNS and asset-alias fallback, manifest-build, contract-app deploy, and trader API publication error text is normalized and redacted for private-key, token, authorization, secret, and mnemonic key/flag forms before stderr or failure evidence can persist it. JSON diagnostics preserve JSON shape while applying the same redaction to string values and object keys, so command-line fragments embedded inside maps do not persist secrets. The same redaction pass now normalizes local `/Users/...`, `/tmp/...`, `/private/tmp/...`, `/var/folders/...`, and `/private/var/folders/...` paths, including `file://` URI forms, into basename-scoped `[local-path]/...` or `[runtime-path]/...` labels before diagnostic text is persisted. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this shell evidence-redaction hardening.
- 2026-06-24 hardened public trader smoke evidence so route response bodies are redacted and capped before non-JSON or oversized diagnostics are copied into `trader_readonly.latest.json` or `trader.latest.json`.
- 2026-06-24 hardened trader API publication evidence so SoraFS pin/registry summary and response files are redacted before reuse, CID probe response bodies and retained probe body/error files are redacted and capped before they are written to release evidence, and successful probe reports keep only the manifest plus route fields required by the release guards. 2026-06-25 tightened the standalone publication command so degraded trader API evidence is still written for diagnosis but exits nonzero unless the top-level report status is `completed`, and the producer now downgrades the report when `contracts.latest.json` or `deploy.latest.json` is missing, stale, wrong-environment, not completed, or not matched by deploy snapshot detail.
- 2026-06-24 aligned browser POST rejection, operator confirmation previews, backend response echoes, and shell release evidence redaction on the same normalized credential vocabulary, including private-key, secret, mnemonic, token/API-key, authorization, password, and passphrase forms at any nesting level.
- 2026-06-24 hardened local console and trader access logs so sensitive query-parameter values, including JSON-like query payloads using that same credential vocabulary, are redacted and long non-sensitive query values are truncated before request lines are written to stderr.
- 2026-06-24 hardened public release evidence, RWA compliance recorder output, Taira repair-plan evidence, and release-checklist diagnostics so report path labels are repo-relative under the checkout and basename-only for outside runtime paths, reducing local path leakage without changing which files the scripts actually read. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this path-label hardening.
- 2026-06-25 hardened the final public release checklist and shared phase guards so required evidence artifacts, including per-contract deploy records, manifests, and the local primitive telemetry artifact where checked by the final checklist, fail if diagnostic string values or object keys still contain raw local `/Users/...`, `/tmp/...`, `/private/tmp/...`, `/var/folders/...`, or `/private/var/folders/...` paths, including `file://` URI forms; stale diagnostics must be regenerated with the shared redaction helpers before release evidence can pass. The contract-console and trader backend catalogs now apply the same basename-scoped path labels to signer derivation warnings before exposing them through local JSON APIs.
- 2026-06-25 tightened deploy evidence freshness so the final public release checklist and immediate release phase guard both reject a `deploy.latest.json` report whose `deployment_records_snapshot.detail.snapshot` path does not name the current `contracts.<generated_at>.json` timestamp. This prevents a refreshed aggregate contracts snapshot from being paired with an older deploy report that points at different deployment-record evidence.
- 2026-06-25 hardened aggregate bundle receipt provenance so newly written `soraswap.bundle.deploy.json` receipts carry `generated_at`, selected `environment`, and the current chain fingerprint when available, public release gates reject the optional aggregate receipt if it is not successful, path-clean, timestamped, selected-environment-scoped, matched to `chain.latest.json`, or contract-for-contract identical to the current `contracts.latest.json` snapshot, retained local acceptance rejects the optional local aggregate receipt if it is untimestamped, wrong-environment, path-leaking, sensitive-diagnostic-bearing, or contract-for-contract stale against local `contracts.latest.json`, and mutation-enabled console/trader startup rejects the same receipt when it contains raw local path diagnostics or unredacted sensitive diagnostics.
- 2026-06-25 extended that path-label discipline to public/local deploy and smoke success lines, release-runner evidence lists, bootstrap start messages, local-up/dev-wrapper diagnostics, preflight/release setup hints, oracle/config validation failures, empty contract-app manifest diagnostics, contract-deploy HTTP failure labels, generated-evidence archive labels, and testnet signer-funding hints, with static coverage in `make test-public-env-helpers`.
- 2026-06-25 added `make redact-generated-evidence`, applied it to all generated deployment/telemetry JSON under `deployments/` and `artifacts/telemetry/`, and extended `make check-shell-syntax` so the local acceptance gate scans that full generated evidence set for raw local path diagnostics. The redaction pass inspected 1,993 JSON artifacts and rewrote the 641 whose redacted JSON values changed; `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` pass after this generated-evidence hygiene hardening. The redaction script now also supports `--check`, and `make check-shell-syntax` invokes that check-only mode so retained generated evidence fails the static gate when it would change under the shared redactor.
- 2026-06-25 reran the local browser and simulation acceptance batch after generated-evidence hygiene hardening: `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, and `make test-trader-ui` all passed. The post-simulation redaction pass inspected 2,007 generated JSON artifacts with 0 rewrites, and `make check-shell-syntax` now also validates the generated-evidence ignore contract for local, staging, testnet, and production deployment directories plus their tracked `.gitkeep` placeholders. The same root metadata gate keeps real testnet/production `*.toml` signer configs ignored while allowing `*.toml.example` templates to remain visible for review.
- 2026-06-25 reran `make lint compile dev-schema dev-test`, `make dev-check dev-build`, `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence`; lint, ABI v1 compile, generated interface schema, manifest Kotodama regression tests, direct dev-wrapper lint/build aliases, public-env helper smoke, shell/repo metadata checks, and whitespace checks all passed, while the generated-evidence redaction pass inspected 2,007 JSON artifacts with 0 rewrites. `make dev-doctor` now fails fast with a `make local-up` hint when the default local profile config is missing, lacks `torii_url`, or points at an unreachable Torii endpoint.
- 2026-06-25 refreshed the repo-owned acceptance layer again: `make test-contract-console` passed 101 Python tests, `make test-trader-ui` passed 32 Python tests plus 7 Playwright tests, `make test-contract-console-ui` passed 11 Playwright tests, `make test-contract-console-integration` passed 1 Python-server integration test, `make simulate-build`, `make simulate-smoke`, and `make simulate-full` passed with 8 simulation suites / 47 tests, `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` passed, and `make redact-generated-evidence` inspected 2,016 generated JSON artifacts with 0 rewrites. After the read-only chain refresh, `make release-checklist` and `make release-production-checklist` stop at the blocked normal preflight caused by missing `deployments/testnet/nested_call_probe.latest.json` before post-preflight release artifacts can be generated.
- 2026-06-25 reran the focused evidence-flow checks after adding read-only chain refresh hints to the RWA recorder and missing-chain release checklist diagnostics: `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence` passed; the redaction pass inspected 2,039 generated JSON artifacts with 0 rewrites. That pass left the release checklist and production checklist stopped on the blocked normal preflight caused by missing `deployments/testnet/nested_call_probe.latest.json` before any public release claim.
- 2026-06-25 reran the shell/static and public-env helper gates after hardening missing-config diagnostics for the read-only refresh targets: `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` passed. A real `make refresh-production-chain` now stops before network work with a repo-relative `next setup:` block because `config/production/production.client.toml` is absent. `SORASWAP_SKIP_IROHA_CLI_BUILD=1` now strictly reuses existing `iroha`, `split_contract_deploy`, and `kagami` binaries and fails with a path-clean diagnostic when the requested helper is missing instead of compiling during operator preflight/repair flows.
- Retained production preflight evidence is still setup-blocked: `deployments/production/preflight.latest.json` was refreshed at `20260708T183136Z` with `status: "blocked"`, no production chain fingerprint, no saved production `chain.latest.json`, and `nested_call_probe.latest_exists: false`; oracle keypair verification is unavailable because no production oracle key material is configured. Its blockers are the missing real `config/production/production.client.toml`, unavailable production Torii root and chain id, missing saved production `chain.latest.json` with the next read-only step `make refresh-production-chain`, unreachable placeholder native MCP URL, missing production mutation consent, missing oracle key material, and missing production nested-call probe evidence (`SORASWAP_ALLOW_PRODUCTION_MUTATIONS=1 make production-nested-call-probe`); it also records the expected warning that production chain fingerprint assembly is unavailable until a production chain id or client config is present.
- 2026-06-25 refreshed the read-only Taira preflight with `SORASWAP_SKIP_IROHA_CLI_BUILD=1` after the strict skip hardening. The command now reports only the missing signed nested-call probe blocker without the unrelated client-config setup block. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed; the redaction pass inspected 2,042 generated JSON artifacts with 0 rewrites. `make release-checklist`, `make release-production-checklist`, and `make record-testnet-rwa-compliance` still stop at the same missing current-chain signed nested-call probe.
- 2026-06-25 hardened the RWA recorder setup hint so missing-chain or blocked-preflight diagnostics now print the full required order: read-only chain refresh, signed nested-call probe, final preflight, then external RWA references. A live `make record-testnet-rwa-compliance` still expected-fails at the missing current-chain signed nested-call probe and now includes `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make testnet-nested-call-probe` in the setup block; `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed after the diagnostic update.
- 2026-06-26 tightened the RWA recorder's preflight acceptance so a handcrafted or stale `preflight.latest.json` with only `status: "ready"` and a chain fingerprint can no longer produce completed RWA evidence. The recorder now requires the actual saved `chain.latest.json` artifact to exist, match the preflight fingerprint, record the selected environment, and be accompanied by current supported nested-call probe evidence before external RWA references are accepted. Manual `SORASWAP_RWA_COMPLIANCE_CHAIN_JSON` values and chain-only evidence are assertions against that ready preflight plus saved chain evidence, not substitutes for them; `make record-testnet-rwa-compliance` still expected-fails at the missing current-chain signed probe.
- 2026-06-26 reran the local source, dev-wrapper, simulation, UI/backend, and public-helper gates after the RWA/preflight hardening: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make simulate-build`, `make simulate-smoke`, `make simulate-full`, `make test-contract-console`, `make test-contract-console-ui`, `make test-contract-console-integration`, `make test-trader-ui`, `make check-shell-syntax`, `make test-public-env-helpers`, `git diff --check`, and `make redact-generated-evidence` passed. The full simulation suite passed 8 suites / 47 tests; the UI/backend suites passed 106 contract-console Python tests, 11 contract-console Playwright tests, 1 console integration test, 35 trader Python tests, and 7 trader Playwright tests; the final redaction pass inspected 2,061 generated JSON artifacts with 0 rewrites. `make release-checklist` and `make release-production-checklist` still stop at the missing current-chain signed Taira nested-call probe before any release claim.
- 2026-06-26 reran the direct local operator flow without public mutations after the isolated localnet acceptance batch. `make local-up`, `make deploy-local`, `make dev-build`, `make dev-smoke`, `make smoke-local`, and `make local-down` passed, writing `deployments/local/chain.20260626T053132Z.json`, `deployments/local/deploy.20260626T052315Z.json` with 25 contracts over 6 phases, `deployments/local/contracts.20260626T055356Z.json` with 25 completed contract snapshots, and `deployments/local/smoke.20260626T062028Z.json` with `status: "completed"`, `environment: "local"`, `smoke_scope: "full"`, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries. `make local-down` stopped the local Nexus, and no SoraSwap localnet or smoke process remained. Follow-up `make redact-generated-evidence`, `git diff --check`, `make check-shell-syntax`, and `make test-public-env-helpers` passed; redaction inspected 2,088 generated JSON artifacts with 0 rewrites. `make release-checklist` and `make release-production-checklist` were rerun and still expected-fail only at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 aligned the shared public placeholder token patterns, contract-console signer autodiscovery, RWA recorder validator, final release checklist, and immediate release phase guard on compact embedded `changeme` / `replaceme` fragments, so configs, oracle overrides, and RWA references are rejected before evidence writing or release acceptance. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed with recorder, checklist, phase-guard, and signer/config fixture regressions; `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-07-08 reran the non-mutating `make production-preflight` setup check while Taira remained the first production prerequisite. It refreshed `deployments/production/preflight.latest.json` at `20260708T183136Z` with `status: "blocked"`, no production chain fingerprint, no saved production `chain.latest.json`, `nested_call_probe.latest_exists: false`, unavailable oracle verification, and the same required setup blockers for real production config, Torii root, chain id, mutation consent, oracle keys, and signed production nested-call evidence.
- 2026-06-26 reran focused local/source and generated-evidence hygiene after that production preflight refresh: `make lint`, `make compile`, `make dev-test`, `make dev-schema`, `make dev-check`, `make dev-build`, `make check-shell-syntax`, `make test-public-env-helpers`, `python3 -m unittest tests.test_contract_console`, `git diff --check`, and `make redact-generated-evidence` passed. The contract-console unit suite ran 108 Python tests, redaction inspected 2,089 generated JSON artifacts with 0 rewrites, and `make release-checklist` / `make release-production-checklist` still stop before any release claim at the missing current-chain signed Taira nested-call probe.
- 2026-07-07 tightened standalone production checklist diagnostics so a missing production `chain.latest.json` after a green isolated Taira prerequisite prints the retained production preflight status, timestamp, blockers, warnings, and health issues when `deployments/production/preflight.latest.json` exists. Operators now see the production setup backlog in the same read-only checklist run without treating blocked preflight as release evidence.
- 2026-06-26 tightened RWA recorder setup diagnostics so missing-chain or blocked-preflight failures include the selected `preflight.latest.json` status, target environment, timestamp, blockers, warnings, and health issues whenever a blocked or warning-bearing preflight artifact exists. `make record-production-rwa-compliance` still fails before external references while the production preflight is blocked, but now surfaces the same production setup blockers as the production checklist.
- 2026-06-26 extended the retained setup summary to full `make release-taira` startup failures. Missing Taira config, mutation consent, oracle material, or RWA references still stop before `[1/12]`, but the runner now prints retained Taira preflight blockers, warnings, and health issues when `deployments/testnet/preflight.latest.json` exists and is blocked.
- 2026-06-26 routed final release-checklist blocked-preflight nested-call probe and health summaries, unsupported nested-probe summaries, nested-call probe stage outputs, `taira-preflight` copied probe summaries, public deploy blocked-probe summaries, trader blocked reasons, contract-console bridge helper output/proof-skip reasons, smoke negative-test unexpected-commit output, shared deploy confirmation-failure output, permission-grant failures, trigger-execute failures, alias-resolution failures, compiler failures, local perps open diagnostics, perps bootstrap init fallback output, Taira state-repair free-form operator inputs, and optional RWA compliance notes through the shared redactor, matching the retained blocker/warning handling already used by release runners and RWA recorder diagnostics. The final checklist and immediate phase guards also reject stale or hand-edited RWA compliance artifacts whose optional notes would change under that redactor; immediate phase guards reject metadata-bearing phase artifacts with unredacted sensitive diagnostics, and the final checklist rejects otherwise passing target-chain, per-contract, bundle, or local evidence if the shared redactor would rewrite it. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 tightened contract-console public bridge smoke diagnostics so replay rejection reasons and unexpected proof/message submit responses are explicitly redacted before they are printed or retained in `contract_console_smoke.latest.json`. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; both release checklists still expected-fail at the missing current-chain signed Taira nested-call probe.
- 2026-06-26 tightened public stale snapshot diagnostics in readonly smoke, mutating smoke, trader smoke, and contract-console smoke so `snapshot_check.output` is routed through the shared redactor before stderr printing. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. Release checklists still cannot advance until the current-chain signed Taira nested-call probe exists.
- 2026-06-26 tightened local and public mutating smoke negative-test rejection evidence so expected rejection output is redacted before it is assigned into `smoke.latest.json.rejection_evidence`. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. Release checklists still cannot advance until the current-chain signed Taira nested-call probe exists.
- 2026-06-26 expanded the shared redaction and browser sensitive-key vocabulary to cover OAuth-style `access_token`, `refresh_token`, and `client_secret` names in JSON keys, free-form diagnostics, CLI-style flags, phase-guard fallback scans, contract-console requests, trader requests, and access logs. focused contract-console/trader UI redaction unit tests, `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; redaction inspected 2,089 generated JSON artifacts with 0 rewrites. Release checklists still cannot advance until the current-chain signed Taira nested-call probe exists.
- 2026-06-27 expanded the shared redactor and browser access-log sanitizer to cover credentials embedded in URL userinfo plus sensitive URL query or fragment params such as `access_token`, `refresh_token`, `client_secret`, `api_key`, `authorization`, `token`, and `password`, while leaving non-sensitive URL params intact. The phase-guard fallback redactor and sensitive-diagnostic scanner use the same URL rules so hand-edited artifacts with unredacted URL secrets are rejected before release advancement; direct helper coverage now rejects raw URL-secret preflight evidence while accepting already-redacted URL evidence. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after this coverage hardening; redaction inspected 2,089 generated JSON artifacts with 0 rewrites.
- 2026-06-27 tightened `make taira-preflight` and `make production-preflight` producers so blocked preflight blockers, warnings, and diagnostic endpoint fields are redacted before they are printed or retained in `preflight.latest.json`. The helper smoke now feeds credentialed Torii URLs through preflight and asserts raw URL userinfo does not reach stdout or the retained report.
- 2026-06-27 added `./scripts/redact_generated_evidence.sh --check` and wired it into `make check-shell-syntax`, so retained deployment and telemetry JSON must be stable under the shared sensitive-data/runtime-path redactor before the static gate passes. The helper smoke now covers dirty check-mode failures, clean check-mode success, and the new static-check success line. `./scripts/redact_generated_evidence.sh --check`, `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, and `git diff --check` passed; both check-only and rewrite scans inspected 2,089 generated JSON artifacts with 0 pending changes or rewrites. Read-only `make release-checklist` and `make release-production-checklist` still expected-fail before any release claim at the missing current-chain signed Taira nested-call probe, with the production checklist also relaying the retained blocked production setup summary.
- 2026-06-27 reconciled the DLMM permissionless surface with the release docs: the state layout, README, and migration register now distinguish generic asset-agnostic `init_pool(...)` instances from the production SoraSwap pool anchored on `xor#universal`, and they keep helper/NFT LP wrappers outside the v1 release surface. The helper smoke pins this wording. `make check-shell-syntax`, `make test-public-env-helpers`, `make redact-generated-evidence`, and `git diff --check` passed after the doc guardrail update; redaction inspected 2,089 generated JSON artifacts with 0 rewrites.
- 2026-06-27 hardened `make check-shell-syntax` with a contract-source hygiene scan so accidental non-Kotodama files under `contracts/` fail static acceptance, except the existing shared helper README. The dev check/build/test/schema path, canonical lint/compile wrappers, final release checklist, and full Taira/production release runners now run the same source hygiene guard before contract tooling, public preflight, production-prerequisite, config setup, or live phase checks can advance. The helper smoke now includes `contracts/dlmm/permissionless_pool.md` regressions for the static gate, dev check/build/test/schema, lint/compile wrappers, final checklist, and full release runners. `make test-public-env-helpers`, `make check-shell-syntax`, `make dev-check`, `make dev-schema`, `make dev-test`, `make dev-build`, `make redact-generated-evidence`, and `git diff --check` passed after this guardrail; `make check-shell-syntax` reported 26 contract files checked and both redaction scans inspected 2,089 generated JSON artifacts with 0 changes. Read-only `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe, with the production checklist also relaying the retained blocked production setup.
- 2026-06-26 hardened the reusable release phase guard for saved chain artifacts so `chain.latest.json` must include non-empty `generated_at` as well as `torii_url`, `chain`, and `block_1_hash` even when a caller invokes the field helper directly. The shared current-or-saved chain fingerprint resolver now follows the same timestamp rule before using `chain.latest.json` to feed deployment snapshots or evidence checks. The helper smoke now covers the direct untimestamped-chain rejection. `make test-public-env-helpers`, `make check-shell-syntax`, `make lint`, `make compile`, `make dev-schema`, `make dev-test`, `make dev-check`, `make dev-build`, `git diff --check`, and `make redact-generated-evidence` passed after this guard hardening; redaction inspected 2,068 generated JSON artifacts with 0 rewrites. `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe before any release claim.
- 2026-06-26 tightened nested-call probe recovery hints in the immediate release phase guard. A wrong-environment or untimestamped `nested_call_probe.latest.json` now points to the signed probe refresh command instead of the sibling-runtime rollout verifier, even if the stale artifact also records `supported: false`; runtime rollout is reserved for current selected-environment probe evidence that actually proves unsupported behavior. `make test-public-env-helpers` passed with the new wrong-environment unsupported-probe regression.
- 2026-06-26 applied the same current-probe discipline to final release-checklist blocked-preflight hints. The checklist now prints nested-call health and runtime-rollout guidance only when `nested_call_probe.latest.json` is timestamped, records the selected environment, and matches `chain.latest.json`; a stale or wrong-environment unsupported probe routes the operator back to `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make testnet-nested-call-probe`. Stale saved-chain blockers keep `make refresh-testnet-chain` / `make refresh-production-chain` as the first recovery step and suppress duplicate standalone probe/runtime hints. The status-doc freshness gate also treats non-current probe files as absent, so stale probe timestamps no longer satisfy current Taira documentation evidence. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed with the wrong-environment checklist regression. `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe and print the signed probe refresh command without a runtime-rollout hint.
- 2026-06-26 moved final release-checklist sensitive-diagnostic scanning earlier for `chain.latest.json`, `preflight.latest.json`, and any retained `nested_call_probe.latest.json`. Dirty blocked-preflight or stale-probe evidence now fails before the checklist prints redacted blocker, health, runtime, or status-doc diagnostics; clean blocked evidence still reaches the existing recovery hints. `make test-public-env-helpers`, `make check-shell-syntax`, `git diff --check`, and `make redact-generated-evidence` passed after the change; `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed Taira nested-call probe, with the production checklist also printing the retained production setup blockers.
- 2026-06-26 tightened the preflight producer to match that current-probe rule. `make taira-preflight` / `make production-preflight` no longer copy `summary` or Sumeragi health from wrong-environment, untimestamped, or stale-chain `nested_call_probe.latest.json` artifacts into `preflight.latest.json`; those artifacts remain blockers until a selected-environment current-chain signed probe is refreshed. `make test-public-env-helpers` passed with the wrong-environment preflight regression.
- 2026-06-26 tightened the shared nested-call probe current-chain helper used by runtime support checks. Cached probe reuse now requires non-empty `generated_at`, selected-environment provenance when an environment is supplied, and a full `torii_url` / `chain` / `block_1_hash` match before a supported `nested_call_probe.latest.json` can satisfy public mutating smoke or release setup. `make test-public-env-helpers` passed with timestamp and wrong-environment helper regressions.
- 2026-06-26 tightened the shared deploy-record current-chain helper the same way. Cached per-contract deploy records now need non-empty `generated_at`, selected-environment provenance when an environment is supplied, and a full current chain fingerprint match before they can be reused for nonce planning, deployment skip/reuse, public smoke/catalog liveness checks, or the live alias/code verifier used by readonly smoke and trader API publication. `make test-public-env-helpers` passed with timestamp and wrong-environment deploy-record regressions.
- 2026-06-26 tightened producer-side public snapshot checks so `snapshot_check.status: "completed"` now requires `contracts.latest.json` to cover the current Kotodama contract set exactly once, and `deploy.latest.json` to report completed preflight, compile, nested-call-probe, deploy, bootstrap, and deployment-record snapshot phases, signer-readiness proof without debug bypass, and a pointer to the current `contracts.<generated_at>.json` snapshot. Public smoke, contract-console, trader, and trader API reports now degrade before publication when the contracts snapshot is partial/extra/duplicate or the deploy report points at the right snapshot path but any required deploy phase is degraded, immediate release phase guards reject duplicate aggregate contract snapshots before advancing, and mutation-enabled console/trader startup refuses duplicate `contracts.latest.json` evidence. `make test-public-env-helpers` passed with partial-contract-snapshot, duplicate-contract-snapshot, degraded-preflight, and degraded-deployment-snapshot regressions.
- 2026-06-25 refined blocked-preflight release-checklist diagnostics so absent `nested_call_probe.latest.json` prints `next signed probe: SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make testnet-nested-call-probe` and suppresses the runtime-rollout verifier until an actual current probe exists and proves unsupported behavior. Live `make release-checklist` and `make release-production-checklist` still expected-fail at the missing current-chain signed nested-call probe, but now route the operator to the first required signed evidence step before rollout triage.
- 2026-06-25 tightened the Taira status-doc freshness gate: when preflight is blocked and no `nested_call_probe.latest.json` exists, `docs/release/smart_contract_production_audit.md`, `docs/release/production_readiness_checklist.md`, and `docs/release/taira_devex_critique.md` must explicitly mention `nested_call_probe.latest_exists` instead of passing with only the preflight timestamp and block-1 hash. `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed after adding the absent-probe doc regression.
- 2026-06-25 fixed release-checklist jq false handling for blocked-preflight hints: explicit `chain.saved_snapshot_matches: false`, `nested_call_probe.latest_exists: false`, and probe `supported: false` are now tested directly instead of being masked by `// true` defaults. The helper smoke now covers stale-chain and missing-probe hints driven only by explicit false evidence fields, and `make test-public-env-helpers`, `make check-shell-syntax`, and `git diff --check` passed after the fix.
- 2026-06-25 centralized the immediate release phase guard's supported nested-call probe checks so the preflight, RWA, nested-probe, and mutating-smoke guard paths share the same current-chain and `supported: true` enforcement. Preflight guard failures now split stale saved-chain recovery from missing/stale probe recovery, while unsupported current Taira probe artifacts print the sibling `../iroha` rollout verifier hint from the phase guard as well as from the final checklist. `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` passed; `make release-checklist` still expected-fails at the missing current-chain signed nested-call probe.
- 2026-06-25 tightened bridge replay evidence validation so the final checklist and immediate phase guard reject `replay_reject` contract-console reports whose rejected message status lacks replay, duplicate, consumed-message, proof-overlap, or known bridge assertion text. A generic `status_kind: "Rejected"` alone is no longer sufficient retained evidence for the replay fallback.
- 2026-06-25 tightened contract-console bridge status validation so producer smoke, final checklist, and immediate phase guards require exact `status_kind` values (`Applied`, `Committed`, `Skipped`, or `Rejected` in the appropriate path); substring matches such as `NotApplied` are rejected.
- 2026-06-25 refreshed `docs/interface_specs/generated.md` through `make dev-schema` so the generated interface schema includes the options stale-oracle admin/view entrypoints and current state-key counts from the compiled manifests.
- 2026-06-26 aligned the contract-console and operator runbooks with the exact bridge evidence rules: apply path requires proof and message statuses of `Applied` or `Committed`, while replay fallback requires replay-like rejection detail instead of a generic `Rejected` record.
- 2026-06-25 reran the full `make test-local-isolated` localnet rehearsal after aligning the local smoke report schema. It completed deploy, bootstrap, and smoke without leaving the isolated peer or smoke process running, writing `deployments/local/deploy.20260625T070345Z.json` with completed deploy/bootstrap/snapshot phases, `deployments/local/contracts.20260625T073317Z.json` with 25 completed contract snapshots, and `deployments/local/smoke.20260625T075815Z.json` with top-level `status: "completed"` and `environment: "local"`, 92 transaction-hash slots, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries. `make release-checklist` now enforces those local smoke schema fields for retained local evidence.
- 2026-06-25 reran `make test-local-isolated` again after aggregate bundle receipt hardening. The first attempt hit host disk pressure while committing block 235; after removing generated Rust incremental build caches under `../iroha/target`, the same local isolated run resumed, completed, and cleaned up the isolated peer. Fresh retained evidence now points at `deployments/local/deploy.20260625T094958Z.json`, `deployments/local/soraswap.bundle.deploy.json` with `generated_at: "20260625T095836Z"`, `deployments/local/contracts.20260625T102142Z.json`, and `deployments/local/smoke.20260625T104746Z.json`; the smoke report records full scope, 92 transaction-hash slots, 79 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries. A direct aggregate comparison passed against the current local contracts snapshot by key, address, deploy nonce, code hash, and ABI hash.
- 2026-07-05 reran `SORASWAP_SKIP_LOCALNET_TOOL_BUILD=1 make test-local-isolated` after lifecycle timing hardening. That historical retained local evidence pointed at `deployments/local/chain.20260705T225243Z.json`, `deployments/local/deploy.20260705T224249Z.json`, `deployments/local/contracts.20260705T232110Z.json`, and `deployments/local/smoke.20260705T234131Z.json`; the local fingerprint records `http://127.0.0.1:18080`, chain `00000000-0000-0000-0000-000000000000`, and block-1 hash `c9aa7b74233d5e0abb979edf1509c47b24019a49f9b365e354facc8733d8fc33`. The smoke report records `environment: "local"`, `status: "completed"`, `smoke_scope: "full"`, 92 transaction-hash slots, 80 view results, 83 decoded state integers, 5 rejection evidence entries, and 8 trigger evidence entries. Primitive telemetry is current at `artifacts/telemetry/defi_2026_primitives_latest.json` with `generated_at: "2026-07-06T02:43:56.316Z"`.
- 2026-06-25 promoted `make dev-check` and `make dev-build` into the formal Required Local Acceptance set and hardened the final checklist so each local acceptance target runs with every inherited `SORASWAP_*` variable plus generic chain/account-discriminator variables cleared. The helper smoke fixture now fails if any unexpected `SORASWAP_` env var or generic chain discriminator leaks into the local `make` calls and includes a parser audit for high-risk public, chain, Torii, toolchain, chunking, scope/trigger, oracle, assertion, RWA, and trader-public selectors; `make check-shell-syntax`, `make test-public-env-helpers`, and `git diff --check` pass after this release-checklist hardening.
- 2026-06-24 aligned the contract-console security review with the current public mutation startup guard: the documented security posture now includes migration-ledger gating, preflight acceptance of the current nested-call probe, completed deploy phases, completed contract snapshots, per-contract manifests, and Torii URL provenance.
- 2026-06-24 aligned the README trader cockpit public-mutation section with the shared startup guard, including migration-ledger gating, per-contract manifests, exact current-contract coverage, stale-record rejection, and Torii URL provenance.
- 2026-06-23 hardened the SoraSwap release gates so `chain.latest.json` must carry non-empty `torii_url`, `chain`, and `block_1_hash` fields, required release artifacts with `chain_fingerprint` must match all three fields, and the final preflight phase guard must verify the ready preflight against the actual saved chain artifact plus the current supported nested-call probe artifact. `make test-public-env-helpers`, `make lint`, `make check-shell-syntax`, and `git diff --check` pass after this local gate change.
- 2026-06-23 strengthened the sibling `../iroha` Taira rollout verifier and bundle builder so the default local preflight runs both `cargo test -p iroha_core queue::router::tests::smart_contract_deploy_rule --lib` and `cargo test -p iroha_core contract_call_transaction_preserves_three_hop_transfer_authorities --lib`; both focused canaries pass locally against `CARGO_TARGET_DIR=/tmp/soraswap-iroha-target`.
- 2026-06-25 reran `../iroha/configs/soranexus/taira/verify_soraswap_rollout.sh --skip-mcp-check --skip-sorafs-check --skip-trader-app-api-check --skip-nested-call`; the local-only rollout verifier passed after the smart-contract deploy router regression passed with 2 tests and the three-hop nested-transfer authority canary passed with 1 test.
- Current-chain Taira artifacts are mostly refreshed, but the write-health-safe release gate cannot finish until signed smoke completes: `chain.latest.json` is current at `20260709T032622Z`; `preflight.latest.json` is ready at `20260709T005125Z`; `nested_call_probe.latest.json` is supported at `20260708T133317Z`; `deploy.latest.json` is completed at `20260709T005139Z`; `contracts.latest.json` is completed at `20260709T010900Z`; DEX-only `rwa_compliance.latest.json` is retained current-chain `not_applicable` at `20260709T030420Z`; `smoke.readonly.latest.json` is completed at `20260709T032707Z`; `smoke.latest.json` is retained at `20260709T020326Z` but is missing the required DLMM collect-fees transaction hash; the latest diagnostic signed smoke failure is `smoke.failed.latest.json` at `20260709T033600Z`; `contract_console_smoke.latest.json`, `trader_readonly.latest.json`, `trader.latest.json`, and `trader_api_bundle.latest.json` are refreshed with retained `generated_at` values `20260709T025646Z`, `20260709T025955Z`, `20260709T030104Z`, and `20260709T030233Z`. Signed mutation phases remain blocked until public finality/write health is stable enough for a fresh signed smoke run to complete.

## Required Taira Gate
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make release-taira`

Equivalent expanded sequence:
- `make refresh-testnet-chain` when accepting a live Taira block-1 reset before preflight
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make taira-preflight`
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make testnet-nested-call-probe`
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make taira-preflight`
- `make record-testnet-rwa-compliance`
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make deploy-testnet`
- `make smoke-testnet-readonly`
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet`
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make test-contract-console-testnet`
- `make smoke-testnet-trader-readonly`
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make smoke-testnet-trader`
- `SORASWAP_ALLOW_TESTNET_MUTATIONS=1 make publish-trader-api`
- `make release-checklist`

## Module Readiness Rules
- `docs/parity/migration_register.md` is the canonical release ledger.
- The migration register must be present, nonempty, parse into at least one `ported` production row, and have no non-reference release rows outside `ported`.
- Every non-reference-only row must be `ported`.
- `blocked`, `stub`, and `adapted` are not acceptable for a production claim.
- The shared derivatives group (`risk_vault`, `perps`, `options`, `cover`) is one sign-off and one rollback decision.

## Evidence Requirements
- `deployments/testnet/chain.latest.json` matches the current Taira fingerprint and records `environment: "testnet"`, `generated_at`, `torii_url`, `chain`, and `block_1_hash`.
- Every required release artifact that records `chain_fingerprint` must include non-empty `torii_url`, `chain`, and `block_1_hash` values matching `chain.latest.json`; copied evidence from another public endpoint is rejected even if the chain id and block-1 hash match.
- Required public evidence artifacts, including per-contract deploy records and manifests, plus the optional aggregate bundle receipt when present, the required local chain/deploy/contracts/smoke reports, and primitive telemetry artifact must not contain raw local filesystem path diagnostics in string values or object keys, such as `/Users/...`, `/tmp/...`, `/private/tmp/...`, `/var/folders/...`, `/private/var/folders/...`, or `file://` URIs pointing at those locations, and must not contain unredacted sensitive diagnostics; release evidence must use repo-relative labels under this checkout or basename-scoped runtime path labels.
- `docs/release/smart_contract_production_audit.md`, `docs/release/production_readiness_checklist.md`, and `docs/release/taira_devex_critique.md` mention the current Taira block-1 hash, and when preflight is blocked they also mention the current blocked `preflight.latest.json.generated_at` plus either the current `nested_call_probe.latest.json.generated_at` value when a latest probe exists or the fact that `nested_call_probe.latest.json` is absent. For production evidence, `docs/release/smart_contract_production_audit.md` and `docs/release/production_readiness_checklist.md` must likewise mention the current production block-1 hash and blocked preflight/probe evidence for the selected production chain.
- `docs/release/smart_contract_production_audit.md` and `docs/release/production_readiness_checklist.md` mention the current retained local `chain.latest.json.generated_at`, `deploy.latest.json.generated_at`, `contracts.latest.json.generated_at`, `smoke.latest.json.generated_at`, and primitive telemetry `generated_at` values so the final checklist cannot pass with stale local rehearsal writeups.
- `docs/release/smart_contract_production_audit.md`, `docs/release/production_readiness_checklist.md`, and `docs/release/taira_devex_critique.md` mention the current retained Taira chain, preflight, nested-call probe, deploy, contracts, RWA, readonly smoke, signed smoke, contract-console, readonly trader, signed trader, and trader API `generated_at` values so the static shell gate catches stale public release writeups even when the final release checklist stops earlier at blocked preflight.
- `docs/release/smart_contract_production_audit.md` and `docs/release/production_readiness_checklist.md` mention current retained production evidence `generated_at` values, including blocked `preflight.latest.json`, and explicitly record missing setup evidence such as no saved production `chain.latest.json` and `nested_call_probe.latest_exists: false` so the static shell gate catches stale production setup writeups while Taira remains the first production prerequisite.
- The local acceptance bundle runs with every inherited `SORASWAP_*` variable plus generic chain/account-discriminator variables `CHAIN`, `ACCOUNT_CHAIN_DISCRIMINANT`, and `IROHA_ACCOUNT_CHAIN_DISCRIMINANT` cleared so a final checklist cannot accidentally leak live-public credentials, alternate tooling, public-only debug controls, account derivation overrides, or local coverage-shaping knobs into local tests.
- `deployments/testnet/preflight.latest.json` exists, records `target_environment: "testnet"` plus `chain.saved_snapshot_environment: "testnet"` and `generated_at`, has `status: "ready"`, records the current Taira `torii_url`, `chain`, and `block_1_hash` fingerprint from `chain.latest.json`, reports native Torii MCP HTTP `200` with `endpoint.health_issues: []`, records signer account visibility, account asset-listing availability, a positive fee balance, usable oracle key sources, `oracle_keypair_verified: true`, `nested_call_probe.latest_exists: true`, `nested_call_probe.matches_current_chain: true`, and `nested_call_probe.supported: true`, and has no blockers.
- `deployments/testnet/deploy.latest.json` exists, is `completed`, records `environment: "testnet"` plus `generated_at`, and records completed `preflight`, `compile`, `nested_call_probe`, `deploy`, `bootstrap_contract_state`, and `deployment_records_snapshot` phases.
- `deployments/testnet/nested_call_probe.latest.json` exists, records `environment: "testnet"` plus `generated_at`, matches the current Taira fingerprint, has `supported == true`, records the live `probe_asset.asset_definition_id`, proves that persisted `bytes` state round-trips on live Taira, proves that the minimal live `call_contract(...)` probe succeeded, and proves that the multi-hop nested AssetOps relay succeeded.
- `deployments/testnet/contracts.latest.json` exists, records `environment: "testnet"` plus `generated_at`, matches the current Taira fingerprint, covers every Kotodama contract source under `contracts/` exactly once, has every contract entry record `environment: "testnet"`, and has no stale contract keys for sources no longer in this repo.
- Every contract listed in `deployments/testnet/contracts.latest.json` has a matching `deployments/testnet/<contract_key>.deploy.json` record and `<contract_key>.manifest.json` file with `generated_at`, the same selected environment, chain fingerprint, `contract_key`, contract address, deploy nonce, code hash, ABI hash, successful deploy response, matching live instance evidence, and, for bundle deployments, a deployed per-contract bundle receipt for the same contract key/address/nonce/code hash/ABI hash; extra stale per-contract deploy records are not allowed. If `deployments/testnet/soraswap.bundle.deploy.json` is present, it must also be successful, timestamped, selected-environment-scoped, path-clean, free of unredacted sensitive diagnostics, match `chain.latest.json`, and match `contracts.latest.json` by key, address, nonce, code hash, and ABI hash.
- `deployments/testnet/trader_readonly.latest.json` exists, has `status: "completed"`, records `environment: "testnet"` plus `generated_at`, matches the current Taira fingerprint, references the current `contracts.latest.json` plus `deploy.latest.json` metadata, and proves that `view/batch` plus every required trader rollup route returned HTTP `200` on public Taira with the required query parameters: `authority` plus `limit` for rollup list routes, `authority` for the trader account route, and `authority`, `limit`, and `bucket_secs` for swap candles.
- `deployments/testnet/trader.latest.json` exists, has `status: "completed"`, records `environment: "testnet"` plus `generated_at`, matches the current Taira fingerprint, references the current `contracts.latest.json` plus `deploy.latest.json` metadata, proves that the same trader routes returned HTTP `200` before and after mutation with the same required query parameters, and records a committed signed `route_swap` with router contract, entrypoint, 64-hex transaction hash, numeric positive input amount, numeric nonnegative `min_out`, `input_is_base == 1`, and XOR decreasing while USDT increases. If a signed trader attempt fails after submission, `mutation.submitted_calls`, `mutation.latest_submitted_call`, and `public_write_health` are diagnostic only; the top-level report must still be `status: "completed"` before release.
- `deployments/testnet/smoke.latest.json` exists, has `status: "completed"`, records `environment: "testnet"` plus `generated_at`, and matches the current Taira fingerprint.
- `deployments/testnet/contract_console_smoke.latest.json` exists, has `status: "completed"`, records `environment: "testnet"` plus `generated_at`, and matches the current Taira fingerprint. If a console smoke attempt fails after submission, `contract_console_smoke.failed.latest.json` is diagnostic only; the completed latest report remains required before release.
- `deployments/testnet/trader_api_bundle.latest.json` exists, has `status: "completed"`, records `environment: "testnet"` plus `generated_at`, matches the current Taira fingerprint, and references the current `contracts.latest.json` plus `deploy.latest.json` metadata.
- `deployments/testnet/preflight.latest.json` is ready, warning-free, matches the current Taira `chain.latest.json`, accepts the current supported `nested_call_probe.latest.json`, and has `generated_at` no older than that probe.
- `deployments/testnet/rwa_compliance.latest.json` exists, matches the current Taira `torii_url`, `chain`, and `block_1_hash` fingerprint, records `environment: "testnet"` plus `generated_at` no older than the current ready `preflight.latest.json`, and is either `status: "not_applicable"` with `rwa_release_enabled: false` for a DEX-only release with no RWA launch activity, or `status: "completed"` with control-character-free, non-placeholder, non-local/wildcard, non-reserved-domain external references for `issuer_approval_ref`, `legal_review_ref`, `compliance_policy_ref`, `nav_source_ref`, and `redemption_terms_ref` plus no optional `notes` value that would change under the shared redactor. The RWA recorder and immediate phase guard require the same release-ready preflight basics as the checklist, including mutation consent, native MCP HTTP `200`, empty `endpoint.health_issues`, signer/account/fee readiness, usable oracle key sources, `oracle_keypair_verified: true`, and current supported nested-call probe metadata.
- `deployments/testnet/deploy.latest.json` is completed and no older than the current ready preflight, `deployments/testnet/contracts.latest.json` is completed for the same chain and no older than the deploy report, and the deploy report's `deployment_records_snapshot.detail.snapshot` path names the current contracts timestamp. Immediate release phase guards require the same current release-ready preflight baseline before deploy, RWA, smoke, contract-console, trader, or trader API evidence can advance.
- `deployments/local/chain.latest.json`, `deployments/local/deploy.latest.json`, `deployments/local/contracts.latest.json`, and `deployments/local/smoke.latest.json` exist after local acceptance, come from the full isolated run, record `environment: "local"` and `status: "completed"` where applicable, cover the local Kotodama contract set exactly once with complete address, nonce, code-hash, ABI-hash, and per-entry chain-fingerprint snapshot evidence, keep contracts no older than deploy and smoke no older than both deploy/contracts, have deploy's deployment-record snapshot detail point at the current contracts timestamp, carry matching non-empty local chain/deploy/contracts/smoke fingerprints, and contain no raw local `/Users/...`, `/tmp/...`, `/private/tmp/...`, `/var/folders/...`, `/private/var/folders/...`, or `file://` local-path diagnostic forms. The local smoke producer stops instead of writing retained evidence when it cannot derive the current local chain fingerprint. The local smoke report must also carry full-scope critical perps liquidation, intent/vault/operator/margin, RWA, DLMM hook, and conditional-escrow trigger mutation hashes, rejection evidence, stable state snapshots, and trigger evidence proving every expected SoraSwap trigger is registered with the required active trigger set plus epoch-auction close evidence. If `deployments/local/soraswap.bundle.deploy.json` is present, it must also be successful, timestamped, selected-environment-scoped, path-clean, free of unredacted sensitive diagnostics, and match local `contracts.latest.json` by key, address, nonce, code hash, and ABI hash; the optional local aggregate bundle may omit `chain_fingerprint`, but any bundle fingerprint it records must match local contracts.
- `artifacts/telemetry/defi_2026_primitives_latest.json` exists, records `generated_at`, contains no raw local `/Users/...`, `/tmp/...`, `/private/tmp/...`, `/var/folders/...`, `/private/var/folders/...`, or `file://` local-path diagnostic forms, and proves the 2026 primitive model is launch-ready with filled intents, bounded solver fees, positive n3x vault accounting, bonded and unjailed solver operators, filled hook orders, margin health above threshold with no liquidations, and unfrozen RWA state with NAV/share and redemption-queue evidence.
- `make compile` must leave exactly one `.to`, one `.manifest.json`, one `.interface.json`, one `.source-map.json`, and one `.budget.json` under `artifacts/compiled/` for each manifest contract; stale generated outputs for removed contract keys are treated as compiled artifact drift.
- `docs/interface_specs/generated.md` must match `iroha.contracts.toml` contract headings and generated interface paths.
- `trader_readonly.latest.json.generated_at >= contracts.latest.json.generated_at` and `>= deploy.latest.json.generated_at`.
- `trader.latest.json.generated_at >= contracts.latest.json.generated_at` and `>= deploy.latest.json.generated_at`.
- `smoke.latest.json` references the current `nested_call_probe.latest.json`, `contracts.latest.json`, and `deploy.latest.json` metadata by `generated_at`, status, selected environment, and chain fingerprint, and its embedded `readonly_verification` records `environment: "testnet"` and references the same current chain/contracts/deploy snapshots.
- `smoke.latest.json`, `contract_console_smoke.latest.json`, `trader_readonly.latest.json`, `trader.latest.json`, and `trader_api_bundle.latest.json` reference the current `contracts.latest.json` and `deploy.latest.json` metadata by `generated_at`, status, selected environment, and chain fingerprint, and include producer-side `snapshot_check.status: "completed"`.
- `smoke.latest.json.generated_at >= contracts.latest.json.generated_at` and `>= deploy.latest.json.generated_at`.
- `contract_console_smoke.latest.json.generated_at >= contracts.latest.json.generated_at` and `>= deploy.latest.json.generated_at`.
- Trader evidence must stop the gate when any required trader route does not return HTTP `200` on the public node, including the post-mutation route reprobe in `trader.latest.json`, any account-scoped route missing `authority`, any list route missing `limit`, and any swap-candle probe missing `bucket_secs`.
- Testnet smoke evidence includes router execution, launchpad executor activation, farms slot accrual, and the active derivatives write-path suite, including 64-hex transaction hashes for perps queue/recover/requeue/liquidate coverage with numeric positive liquidation position counters plus recorded keeper reward and owner residual.
- Contract-console evidence includes proof submit plus bridge message submit through the deployed `bridge.sccp_bridge` contract.
- RWA legal and issuer artifacts remain outside this repository; the release gate enforces the evidence hook and chain fingerprint, not the legal process itself. `make release-taira` and `make release-production` validate the five external RWA reference env vars before any numbered public release phase only when `SORASWAP_ENABLE_RWA_RELEASE=1`, and production does this before its isolated Taira prerequisite. Missing, placeholder, local/wildcard endpoint, reserved-domain, or control-character references cannot reach a signed RWA release mutation.
- `contract_console_smoke.latest.json.bridge.submission_expectation == "apply"` is acceptable when proof/message both reach exact `status_kind` values of `Applied` or `Committed`.
- On the `apply` bridge path, `contract_console_smoke.latest.json.submissions.proof_submit.tx_hash_hex` and `message_submit.tx_hash_hex` must both be 64-hex transaction hashes.
- A skipped standalone bridge proof submission is not acceptable on the `apply` path; it is tolerated only for the replay-reject fallback where the cached message rejection proves duplicate/consumed semantics.
- `contract_console_smoke.latest.json.bridge.submission_expectation == "replay_reject"` is acceptable when cached SCCP evidence proves the governed route and the deployed bridge rejects the replay path with decoded replay/duplicate/consumed/proof-overlap semantics, or with the current generic bridge-contract `assertion failed (constraint violation)` rejection when Taira does not preserve the original contract string.
- Bridge evidence must prove governed route provenance (`route_provenance[0] == 1`) before any production claim.
- Bridge evidence must prove a proof-driven `finalize_inbound` message submission (`proof_driven_settlement == true`) and must prove no caller-supplied settlement payload was submitted (`settlement_payload_supplied == false`).
- If `preflight.latest.json` reports `endpoint.mcp_http_status != "200"`, native Torii MCP is not release-ready and the gate must stop before signed live actions.
- If `nested_call_probe.latest.json` reports `state_bytes_roundtrip_supported == true` and `nested_call_supported == false`, the public Taira runtime is specifically failing on nested `call_contract(...)` rather than basic pointer/state codecs, and the gate must stop there.
- If `nested_call_probe.latest.json` reports `state_bytes_roundtrip_supported == true`, `nested_call_supported == true`, and `nested_asset_ops_supported == false`, first verify `probe_asset.asset_definition_id` exists on the live chain. If it does, the public Taira runtime is specifically failing on nested AssetOps relay across `call_contract(...)` boundaries, and the gate must stop there.
- If `nested_call_probe.latest.json` reports `supported == false`, the public Taira runtime is not release-capable for SoraSwap’s active router/launchpad/derivatives surfaces and the gate must stop there.
- If `trader_readonly.latest.json` or `trader.latest.json` reports missing/non-`200` trader routes, missing required route query parameters, stale snapshot metadata, or a signed swap without numeric `route_swap` contract evidence proving XOR decreases and USDT increases for the base-input path, the public Taira trader rollout is not release-ready and the gate must stop there.
- If `smoke.latest.json` embeds stale readonly snapshot metadata or reports non-hex or placeholder transaction hashes for the required perps, intents, vaults, operators, margin, or DLMM hook write paths, the public smoke did not prove committed mutations against current readonly evidence and the gate must stop there. For RWA, default DEX-only release mode must prove the no-RWA state snapshot, while enabled RWA release mode must prove the committed lot/redemption transaction hashes and state transition.
- If the normal bridge apply path reports missing, placeholder, or non-hex proof/message transaction hashes, the contract-console bridge proof did not prove committed public submissions and the gate must stop there.
- If any public release artifact records the wrong selected environment, the evidence may have been copied across `testnet` and `production` runs and the gate must stop there.
- If `trader_api_bundle.latest.json` is not `status: "completed"`, is missing the exact expected trader route method/path/adapter set in either the report or the parsed CID probe response, lacks matching SoraFS pin/registry receipts, has a `content_cid` that does not derive from the pinned manifest id, reports nonnumeric or mismatched CID probe attempt/success/manifest-match counts, does not reference the current `contracts.latest.json` plus `deploy.latest.json` metadata, reports `deployment_record_check.status != "completed"` because deploy-record or manifest evidence drifted, reports `snapshot_check.status != "completed"` because current deployment snapshots were missing or stale at publication time, or has a `generated_at` older than those snapshots, the trader API CID may have been republished against stale deployment evidence and the gate must stop there.
- The only acceptable fix for that specific blocker is a public Taira runtime rollout from `../iroha`, followed by `../iroha/configs/soranexus/taira/verify_soraswap_rollout.sh --allow-testnet-mutations` with its default local SoraSwap regressions enabled: the universal-deploy router regression and the `iroha_core` three-hop nested transfer canary. Do not add a SoraSwap-side non-nested fallback.

## Release And Rollback
- Mainnet cutover happens only after the exact Taira artifact set is green with no exceptions.
- The signed SoraSwap RC must contain a real `config/production/cutover-trust-policy.json`, not the placeholder example. It pins distinct SSH Ed25519 trust anchors for independent security and operations approvers, an independent HTTPS monitoring origin, and the fixed 1,800-second/30-second/61-sample observation policy. The runtime `config/production/cutover-approval.json` remains ignored, mode `0600`, single-link, and expires within the policy window; it must carry valid signatures from both roles, bind the exact production chain/RC/Iroha bundle and binary checksums, five distinct signer/oracle/admin/treasury/bridge authorities, the approved fee minimum and operational control references, zero unresolved Critical/High findings, the expected validator/oracle/balance/readonly-route sets, and the deterministic trader API identity. Missing real trust anchors or any unsigned/external operational input is a production blocker, not a value the repository may invent.
- Production phase 12 performs the readonly cutover observation before the checklist. `deployments/production/observation.latest.json` must be mode `0600`, non-test, and freshly regenerated with exactly 61 samples spanning at least 1,800 seconds. Every sample must use the same approved cryptographic peer set and exact chain/RC/Iroha/deploy/contracts binding, interpret `/status/blocks` as committed/commit-QC height, allow at most a one-height canonical lead, require all validators to agree on canonical/commit-QC/highest-QC and commit-QC to equal highest-QC, require committed finality to advance, and keep public/direct queues, oldest queue age, proposal queues, lane backlog, API failures, and readonly route failures at zero. All approved oracle feeds must remain fresh, the signer fee must remain above its signed minimum, other watched balances and the trader API CID must remain stable, and monitoring snapshots must be fresh and monotonically sequenced.
- A shared derivatives regression is a hard stop at an external control boundary. The observer must never claim it paused perps, options, or cover. It reports that the coordinated pause is required and exits without completed evidence; operators must independently verify the real all-derivatives pause and obtain a new unexpired approval before restarting cutover.
- Production must reuse the same deploy, smoke, trader, RWA compliance, preflight, and checklist layout as Taira. Use `SORASWAP_ALLOW_PRODUCTION_MUTATIONS=1 make release-production` for the production wrapper family after the Taira artifact set is green; the production runner validates production config, consent, oracle keys, and RWA external references only for `SORASWAP_ENABLE_RWA_RELEASE=1` before the isolated Taira prerequisite, then enforces Taira ordering by running `SORASWAP_PUBLIC_ENV=testnet SORASWAP_RELEASE_ENV=testnet make release-checklist` before any production mutation phase, and each production phase must pass the same immediate evidence guard before the runner advances. When the full runner stops during setup, it reports the retained production preflight status, generated timestamp, blockers, warnings, and health issues if `deployments/production/preflight.latest.json` exists, but that summary is diagnostic only. The deploy evidence is only usable when its preflight, compile, nested-call probe, deploy, contract-state bootstrap, and deployment-record snapshot phases all completed. The standalone `make release-production-checklist` target also revalidates the Taira artifact gate after clearing every inherited `SORASWAP_*` variable plus generic chain/account-discriminator variables, while skipping the duplicate local acceptance rerun for that prerequisite check; when that prerequisite fails, it reports the same retained production preflight summary.
- The full production runner and standalone production checklist both clear every inherited `SORASWAP_*` variable plus generic chain/account-discriminator variables before the Taira prerequisite check, then pass only the explicit testnet release selector needed by the inner checklist.
- Production preflight records faucet status only as diagnostics; it must block on a missing or unfunded signer instead of relying on faucet or self-registration recovery.
- Public Taira runtime rollouts must follow `docs/release/taira_operator_runbook.md` before SoraSwap release evidence is trusted.
- If the bridge path falls back to caller-supplied settlement payloads, the release gate has failed.
- If any shared-derivatives regression appears, roll back the whole derivatives claim together.
