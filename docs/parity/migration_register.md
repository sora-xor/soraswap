# SoraSwap Migration Register

This register tracks behavior-level migration from the legacy reference implementation into this Kotodama repo.

Status values:
- `stub` - repo placeholder only
- `adapted` - intermediate Kotodama shape exists, but the module is not release-eligible and must be resolved to `ported`
- `ported` - intended behavior is present in this repo with executable local acceptance, signed Taira evidence, and matching interface/state docs
- `blocked` - excluded from the production claim until the named `../iroha` capability or acceptance gap is closed; `make release-checklist` fails while any row remains `blocked`
- `reference-only` - kept for comparison, not part of the deployable surface

| Reference area | SoraSwap area | Status | Notes |
| --- | --- | --- | --- |
| SCCP bridge hub | `contracts/bridge/sccp_bridge.ko` | ported | Nexus-side SCCP registry and settlement scaffold now records per-asset vault bindings plus governed-route provenance on chain. Bridge owner and proof authority are explicit singleton state, and proof-managed `finalize_inbound` / `activate_route_governed` require the proof authority so release evidence stays proof-driven end to end. |
| T3 hub | `contracts/n3x/n3x_hub.ko` | ported | Renamed to `n3x`; the singleton basket hub exposes init-time basket targets, caller-bound mint/redeem flows, fee reserves that are not redeemable basket backing, owner-only fee claims, and typed basket/fee snapshots, and the current surface is covered by executable local acceptance plus signed Taira mint/redeem evidence. |
| DLMM | `contracts/dlmm/dlmm_pool.ko` | ported | Single-pool Kotodama DLMM includes deterministic multi-bin walk, immutable risk config at init, seeded multi-bin liquidity, explicit `position_id` LP accounting, per-bin fee claims, and direct same-contract execution entrypoints for the router-bound path. Router-facing swaps now require full-fill-or-revert so router custody cannot retain unused partial-fill input. Factory/registry parity and helper/NFT wrappers remain outside this repo slice. |
| DLMM router | `contracts/dlmm/dlmm_router.ko` | ported | Router config remains init-only, but the contract now binds a deployed pool and executes same-transaction `call_contract(...)` swaps through `route_swap(...)`. Release evidence requires executable Taira swaps against live seeded pool state, not quote-only views. |
| Launchpad | `contracts/launchpad/{sale_factory,liquidity_executor}.ko` | ported | Fixed-price sale flows, claim inventory, refund accounting, executor-driven DLMM seeding, and typed sale/allocation/activation snapshots are present. Factory owner capture is explicit through `init_factory()`, direct `contribute(...)` hard-rejects, claim windows use `block_height()`, and the dedicated executor contract is the only release-eligible seed route. |
| Referral | `contracts/referral/registry.ko` | ported | Init-time registry config, routed child/parent accrual splits, member-owned claim accounting, thresholds, and typed child-plus-parent snapshots are present and covered by executable smoke. |
| Automation | `contracts/automation/job_queue.ko` | ported | Jobs model queued/running/paused/done scheduling with retry limits, retry delays, executor binding, recurring cron-style reschedule, run counts, and typed snapshots, and the current lifecycle is covered by executable smoke. |
| Farms | `contracts/farms/farm.ko` | ported | Reward funding, owned stake positions, slot-based accrual, global reward-index accounting, reward debt, claim, unstake, and typed farm snapshots are present. Release evidence now requires multi-slot fund/accrue/claim/unstake coverage instead of the older simplified rate-per-stake scaffold. |
| Shared derivatives risk vault | `contracts/risk/risk_vault.ko` | ported | Canonical bucket ledger for buckets `1=perps`, `2=options`, `3=cover`; includes liability lock/release, capped payouts, utilisation guards, surplus accounting, bucket automation telemetry, and the shared controller surface used by perps/options/cover through ABI v1 same-transaction contract-to-contract calls. |
| Perps | `contracts/perps/perps_engine.ko` | ported | Market-based engine with signed oracle JSON payloads, monotonic oracle slots, `block_height()` freshness checks, automatic bounded liquidation passes, queue-first recovery semantics, keeper-plus-owner residual settlement, automation telemetry, and shared-risk wiring to `risk_vault`; user collateral and payout flows route through bucket `1` instead of a standalone perps-owned custody account. |
| Options | `contracts/options/{manager,factory,vault,shout_option,outperformance_option}.ko` | ported | Single-contract tickets were replaced by a manager/factory/vault/product stack for shout and outperformance flows; signed oracle JSON drives series settlement and shout marks, the factory is the user-facing orchestration layer, `OptionsVault` is accounting-only, and settlement/utilisation/liability state routes through `risk_vault` bucket `2`. |
| Cover | `contracts/cover/policy_manager.ko` | ported | Parisian cover manager with policy registration, signed observation payloads, monotonic oracle slots, stale/degraded observation resets, and vault-routed claim flow; premiums, liabilities, expiry release, and payout settlement route through `risk_vault` bucket `3`. |
| Solver intents | `contracts/intents/settlement_router.ko` plus generic `DeFiInstructionBox::SubmitIntent` and `DeFiInstructionBox::SettleIntent` records in `../iroha` | ported | First-release intent router records owner-bound open/cancel/fill state with solver fee, deadline, nonce, and fill output. Native Iroha generic DeFi ISIs provide the canonical metadata-backed ledger record surface for submit/settle events. |
| Tokenized vaults | `contracts/vaults/manager.ko` plus generic `DeFiInstructionBox::RegisterVault` and vault-request records in `../iroha` | ported | Vault manager supports vault registration, share accounting, async redeem request/claim, and position views. Launch scope covers the `n3x` strategy rail and metadata-backed generic native vault checkpoints. |
| Bonded operators | `contracts/operators/registry.ko` plus generic `DeFiInstructionBox::RegisterOperator` and heartbeat records in `../iroha` | ported | Operator registry tracks service labels, bond requirements, bonded amount, heartbeat health, fee accrual, and jail state for solvers, keepers, oracles, relayers, and executors. |
| DLMM hooks | `contracts/dlmm_hooks/hook_manager.ko` plus generic `DeFiInstructionBox::ConfigureAmmHook` and hook-execution records in `../iroha` | ported | Hook manager records capability-gated hook policies and hook-owned limit/TWAMM order execution checkpoints while leaving the deployed DLMM pool math as the production AMM core. |
| Portfolio margin | `contracts/margin/portfolio_margin.ko` plus generic `DeFiInstructionBox::RegisterMarginMarket` and margin-account records in `../iroha` | ported | Portfolio margin adapter registers markets, records collateral/exposure deltas, computes account health, and exposes a liquidation route for the shared perps/options/cover/RWA rollout. |
| Tokenized RWA markets | `contracts/rwa/market.ko` plus native RWA plus generic `DeFiInstructionBox::RegisterRwaMarket` and NAV records in `../iroha` | ported | RWA market adapter binds share/NAV assets, controller NAV reporting, redemption request/settlement, and market state to the existing native RWA provenance and compliance lot model. |
| CLMM | N/A | reference-only | Not deployable in SoraSwap v1. |
| XYK | N/A | reference-only | Useful as a math comparator only. |
| SigmAMM | N/A | reference-only | Not deployable in SoraSwap v1. |

## Open TODOs
- Replace the current `position_id`-keyed DLMM LP scaffold with the final helper/NFT-oriented position surface if helper/NFT wrappers become necessary on Sora Nexus.
- Add real conflict-aware state layout docs and deploy-time contract ids beyond the current bootstrap defaults.
- Keep the sibling `../iroha` ABI v1 `call_contract` path in sync across codegen, host/runtime dispatch, Torii payload normalization, and CLI helpers as the derivatives graph evolves.
- Keep the bridge production claim tied to the proof-authority Taira path; direct raw-input `finalize_inbound(...)` debug calls cannot be release evidence because non-authority callers are rejected on chain.
- Add the parallel production deployment family only after the Taira artifact set, smoke evidence, and contract-console proof evidence are green with the exact same evidence layout.
- Keep perps, options, cover, and `risk_vault` on the shared integrated gate: regressions should move the whole derivatives group back behind the acceptance checklist instead of treating the modules independently.
